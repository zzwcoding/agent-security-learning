# 红队工具链依赖(阶段 47)——与 agent 运行时镜像无关,不进 requirements.txt/lock
# 主 venv(.venv):进程内 PromptTarget 要 import agent,PyRIT 必须同环境
pyrit==1.0.1        # 注意:要求 transformers>=5,与 llm-guard 钉死的 4.51.3 冲突,共存实证见 record 0043
anthropic==1.3.0    # 仅校准靶 import coding-agent 时需要(模块顶层 import,实际走 openai 客户端)
# garak 独立 venv(.venv-garak,只打 HTTP 面):
#   uv venv .venv-garak --python 3.12 && uv pip install --python .venv-garak/bin/python garak==0.16.0
