# 每阶段新增依赖在这里追加并锁定版本;全部依赖(含间接)的锁定随 Docker 阶段补齐
langchain==1.3.17  # create_agent 所在(原 langgraph.prebuilt.create_react_agent,底层仍是 LangGraph 图)
langchain-openai==1.6.0
langchain-mcp-adapters==0.3.2
mcp==1.29.1  # 锁 1.x:2.0 起 in-SDK FastMCP 改了 API(见 RESOURCES.md)
httpx==0.28.1  # fetch_server 直接依赖
langgraph-checkpoint==4.2.0  # InMemorySaver 直接依赖(对话历史)

