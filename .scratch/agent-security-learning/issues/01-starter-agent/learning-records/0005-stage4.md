# 0005: 阶段 4 —— LangGraph ReAct 接管工具调用

**日期**:2026-08-27

**做了什么**:装 `langgraph==1.2.11` + `langchain-mcp-adapters==0.3.2`;`agent.py` 加 `MultiServerMCPClient` + `build_agent()` + `ask()`(astream values 模式,沿途打印工具调用/返回)。实测:自然语言让模型自主调 list_dir / read_file / write_file,落盘内容验证一致。

**实测发现**:
- `langgraph.prebuilt.create_react_agent` 在 langgraph 1.x 已弃用 → 改装 `langchain==1.3.17` 用 `create_agent`(底层仍是 LangGraph 图,spec 的 "LangGraph ReAct" 不变);requirements 里 langgraph 不再是直接依赖,已换成 langchain
- ToolMessage.content 是 MCP content block 列表不是字符串 → 加 `_text()` 归一化后打印可读
- MultiServerMCPClient 每次调用才拉起 server 子进程(非长连接)

**结论**:阶段 4 跑通。待用户验证并说"提交"。
