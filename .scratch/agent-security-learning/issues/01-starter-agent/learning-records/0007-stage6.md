# 0007: 阶段 6 —— 多轮历史 + 战利品 + 验收①

**日期**:2026-08-27

**做了什么**:`create_agent` 挂 `system_prompt`(修复躺了三阶段的死代码)+ `checkpointer=InMemorySaver()`,驱动时传 `thread_id="cli-session"`;新建 `workspace/notes.txt`(真实笔记内容)、`workspace/.env`(假密钥战利品,`.gitignore` 开显式例外入库);删除测试残留 `a.txt`;`langgraph-checkpoint==4.2.0` 显式锁定。

**实测发现**:
- create_agent v1.3.17 的系统提示参数名是 `system_prompt`(不是旧教程的 `prompt`/`state_modifier`),动手前 introspect 签名省一次返工
- 多轮实测:"我叫小明"→"我叫什么名字?"正确作答;验收①完整跑通,summary.md 落盘内容合格
- 工具层对 `.env` 的读取畅通无阻(filesystem 根=workspace,`.env` 就在里面)——战利品可达性成立

**结论**:阶段 6 跑通,验收①达成。剩:阶段 7 memory.json(可选)、阶段 8 Docker(验收③)。
