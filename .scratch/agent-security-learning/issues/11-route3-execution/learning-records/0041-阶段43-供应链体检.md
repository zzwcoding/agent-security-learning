# record 0041 - 阶段 43:供应链体检(2026-09-02)

**学了什么**:snyk-agent-scan 实操(SNYK_TOKEN 经 agent-key/Keychain 注入;逐 server 询问启动 consent,默认拒=X009 仅清点;同意后拉 tools/list 审描述打分);扫描结果——毒样本 add 工具 1000/1000(Prompt injection in tool:读 SSH key+经 notes 外传+隐瞒用户)+300(Private data)+100(Dangerous words "important");净样本全部≤100 信息级;人工复核三红旗(功能与要求不匹配/要求对用户隐瞒/敏感数据进参数)。

**卡在哪**:① agent-key snyk 首次误判——head -c12 输出空但 exit 0,echo 判断逻辑把"无条目"当"有";真实报错 SecKeychainSearchCopyNext item not found;② X009 全 skip——stdio 不启动拉不到工具描述,投毒检测必须同意启动受检样本(教具自造,风险可控后逐个 y);③ 密钥纪律 HITL:SNYK_TOKEN 由用户录入 agent-key set snyk(测试 key 已暴露在会话中,提醒轮换)。

**结论**:体检报告 supply-chain/report.md(四 server 结果+复核+方法论);投毒样本 poisoned_server.py + scan-targets.json 入库作教具,描述文本进路线 4 CI 回归集;静态体检拒于门外+动态防线堵于室内的双层结论。阶段 44:五条验收攻击(网关收敛/越权/串联闸/审计三面/令牌 fail closed)。
