# record 0034 - 阶段 36:Agent 改走网关 + TS 第二消费者(2026-09-01)

**学了什么**:MultiServerMCPClient 换 streamable_http 条目(url+headers 一张 Bearer 票)即完成 Agent 侧收敛;工具名切换为网关命名空间前缀(TOOL_DATA_CLASS 等配置同步换键);TS 官方 SDK(StreamableHTTPClientTransport + requestInit.headers)约 60 行成为第二消费者,与 Python 侧同门同票同目录。

**卡在哪**:
1. run-agent.sh 内嵌多行 python -c 的嵌套引号被 shell 撕碎——字典被逐字段拆开喂给 python(SyntaxError×4,每行一个字段),Agent 拿到空 token → 网关收到的请求是 `Bearer `(httpx Illegal header)。解法:铸币抽成独立脚本 mint-gateway-token.sh + `<<'EOF'` 零插值 heredoc。
2. (探路发现,非坑)langchain StructuredTool 的 coroutine 返回 tuple(content, structured),不是 MCP 原始响应——展示代码要按 tuple 解。

**结论**:端到端证据三件——① dummy token 被 401 拒(认证闸活);② 真 Agent 轮次:LLM 决策 `filesystem-list-dir` 经网关执行返回 workspace 清单(直连 stdio 在 agent.py 已拔除,git diff 即证据);③ ts-second-agent 同门列出 6 工具并成功调用。LLM 全链 = 凭证代理(5055,用户已在跑,按纪律不抢端口)+ Keychain + Langfuse 200。阶段 37 起给两个消费者配真实身份差异(运维位/只读位)。
