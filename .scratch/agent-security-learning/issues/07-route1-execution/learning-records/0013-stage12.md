# 0013: 阶段 12 —— 输入护栏上线,直接注入全拦

**日期**:2026-08-27

**做了什么**:venv 装 `llm-guard==0.3.16`(连带 torch 2.13/transformers,锁文件 63→118 包);`agent.py` 主循环挂 PromptInjection 扫描器(+6 行);真实 CLI 回归。

**实测发现**:
- 预扫描探针(8 条文本):卡通 jailbreak、经典英文 jailbreak、有效 payload A/B、间接注入两条、记忆投毒文本**全部 score=1.0 拦截**;善意请求 0.01 放行。扫描器认的是意图模式,"说人话"骗不过它。
- CLI 回归:语料 01 三条 payload(卡通 + 自然 A + 自然 B)全部 `🛡️ 输入护栏拦截(分数 1.0)`,零工具调用、无 PWNED 文件;善意请求正常走 ReAct(list_dir 真实调用)。
- 拦截点在进入 ReAct 图和 checkpointer 之前——恶意输入不进记忆。
- deberta 模型首次运行自动从 HuggingFace 下载,本机走 MPS(Apple Silicon GPU),单条扫描约 0.3s。
- 已知噪音:llm_guard 有个无害 SyntaxWarning(正则转义);torch 进锁文件后镜像将显著变大,留阶段 15 处理。

**结论**:直接注入类攻击在输入侧被确定性拦截。间接注入尚未防(用户请求善意,扫描器看不见毒)——那是阶段 13 的事。待用户验证、说"提交"后收尾。
