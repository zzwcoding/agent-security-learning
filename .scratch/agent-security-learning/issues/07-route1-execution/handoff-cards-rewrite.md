# 交接说明:新窗口改写知识卡片(新写法规则)

> 用法:把下面分割线之间的内容整段贴进新窗口。

---

用 knowledge-cards 这个 skill(项目级,在 `.agents/skills/knowledge-cards/SKILL.md`,先读它)完成一个改写任务:把已有的 104 张知识卡片按新的写法规则全部改写一遍。

## 背景

仓库 `/Users/divh/Downloads/安全评估agent`。知识卡片已生成完毕:
- 碎片 38 个:`.scratch/agent-security-learning/知识卡片-碎片/*.cards.md`(每篇 lesson 一个文件,内含 1-6 张卡)
- 合成终稿:`知识卡片/路线1知识卡片.md`
- PDF:`知识卡片/路线1知识卡片.pdf`

用户验收后发现写法问题:卡片是"写给经历者看的实验记录",带文件路径/行号/测试编号(T1)/阶段黑话,没经历过项目的人看不懂。skill 里已沉淀新的"写法规则"一节(先读 SKILL.md 的「写法规则」)。

## 任务

1. **改写**:按写法规则重写全部 38 个碎片文件里的 104 张卡——
   - 卡片标题(`###` 行)、归属 lesson、事实内容**保持不变**,只改写表达方式
   - 每张卡自立可懂:术语先定义再使用;"我们的办法/对策"只留一句话;禁止路径/行号/测试编号/阶段编号;实测发现写成通用结论式
   - 事实存疑时回查原始 lesson(路径在每个碎片的标题里可推断;主线在 `.scratch/agent-security-learning/issues/07-route1-execution/lessons/`,平行窗口项目在 `攻防矩阵复刻/lessons/`、`执行工具复刻/lessons/`、`NeMo-Guardrails学习/lessons/`)
2. **执行方式**:子 agent 串行(并发 1),每轮派发 2-3 个碎片文件改写,主 agent 抽查验收后再派下一批——不要并行,不要一次全给
3. **合成 + PDF**:改写完后重新合成终稿(规则见 SKILL.md 产出约定:按教学顺序拼接、碎片一级标题降二级、文档头保留),再用 skill 第四步的命令重出 PDF,`pdfinfo` 验证页数
4. **验收标准**:随便抽一张卡,拿给"没做过这个项目的人"能读懂;全文 grep 不到 `config.yml:`、`agent.py:` 行号、T1/T2 这类实验坐标

## 约束

- 不改卡片的选择(哪几个概念成卡)和事实(数字、结论),只改写法
- 先改写 3 个碎片给我看效果,我确认后再跑全量(skill 的试行纪律)
- 提交节奏听我的:我说"提交"才 commit

---
