# 已知缺口清单(路线 1 验收③)

> 每条标注:缺口是什么 → 为什么路线 1 不修 → 归属路线(附备料方案)。

| # | 缺口 | 实测证据 | 归属 | 备料方案 |
|---|---|---|---|---|
| 1 | **网络 egress 未限**:加固容器内仍可自由出网,密钥可经 http_post 外泄 | 容器内 urllib 拿到外网真实 HTTP 404(record 0016) | 路线 2 | microsandbox egress 白名单(票 04 已定选型) |
| 2 | **记忆装载无校验**:memory.json 无签名/无来源标记,毒记忆照装 | 投毒脚本一击即中;NeMo 对账:rail 全钉在消息链路上,装载在链路外(对照结论 §2.2) | 路线 3 | 装载时过一遍注入检查 + 来源标记(chapter2 D3 思路)+ 完整性校验 |
| 3 | **执行层无闸**:语义正常的恶意调用("帮我删 xxx")不是注入,扫描器无话可说;write_file 参数侧无扫描,密钥可经写文件落地 | 对照分析 §2-3(执行工具复刻) | 路线 3 | **串联闸**:先 D4 规则(目标须在本轮用户消息,免疫注入零成本)再 LLM 法官(评后果);fail-closed 审批 |
| 4 | **格式规范伪装毒漏判**:叙事式毒在分类器块级仅 0.05 | record 0014;NeMo 窗口实测 self-check 语义自检可拦(5.2s/952 tokens) | 路线 4(红队种子)+ 路线 3(语义自检接线) | 工具返回通道加 LLM 语义自检(自写 action);payload 入 CI 回归集 |
| 5 | **分类器误报/漏报率**:善意句式被误拦(0.99),高熵串误拦 | record 0014/0020 | 路线 4 | 红队持续对抗 + 阈值标定流程 |
| 6 | **护栏逐行设防**:拆行投毒=两条独立输入,各自过闸 | record 0020(阶段 19 发现) | 路线 4(种子)/ 路线 3(会话级判定) | 会话级聚合判定待研究 |
| 7 | **记忆投毒毒源未防**:毒效被 L3 兜住,但毒记忆仍在,每轮试图触发 | record 0015 | 路线 3 | 同缺口 2,合并处理 |

## 路线 4 CI 回归集第一批种子

1. 语料 01/02/03 全部 payload(判定标准见 `02-防御回归.md`)
2. save_memory 工具内污染路径(chapter2 精读发现,比篡改文件更隐蔽)
3. 良性偏好 + 延迟触发形态(chapter2 记忆注入原文,分类器 -1.0 盲区)
4. 格式规范伪装 v3 及其变体(分类器 0.05 漏判)
5. 拆行投毒(阶段 19 发现)
