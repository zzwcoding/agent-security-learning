# 已知缺口清单(路线 1 验收③)

> 每条标注:缺口是什么 → 为什么路线 1 不修 → 归属路线(附备料方案)。

| # | 缺口 | 实测证据 | 归属 | 备料方案 |
|---|---|---|---|---|
| 1 | ~~**网络 egress 未限**~~ **✅ 已核销(2026-09-01 路线 2 fetch 侧;2026-09-02 路线 3 shell 侧全销)** | ~~容器内 urllib 拿到外网真实 HTTP 404~~ fetch 侧:工具层白名单+凭证策略+VM PUBLIC profile(路线 2);shell 侧:网关 deny_command 插件 EGRESS_DENIED(admin 同拦,出口与身份无关),证据 `deliverables/route3/…/attack-validation/03-gap-close/` | 路线 2+3 | 已全落地 |
| 2 | ~~**记忆装载无校验**~~ **✅ 已核销(2026-09-02,路线 3)** | ~~投毒脚本一击即中;装载在链路外~~ 核销:记忆信封(逐条 sha256)+ 装载三道闸(完整性→注入分类器→语义自检)+any-quarantine 全拒+系统提示来源标记;攻击 B(全知攻击者重算 hash)被语义自检抓获,详见 `deliverables/route3/03-缺口核销记录.md` | 路线 3 | ~~装载时过注入检查+来源标记+完整性校验~~ 已落地(残余:签名升级归生产化) |
| 3 | ~~**执行层无闸**~~ **✅ 已核销(2026-09-02,路线 3)** | ~~语义正常恶意调用扫描器无话可说~~ 核销:串联闸(D4 规则+LLM 法官+任务票第零道)落 agent 中间件栈;D4 实弹(date 命令被拒)+法官正负对照,详见 `deliverables/route3/03-缺口核销记录.md`;残余:D4 误拒合法意图致模型幻觉代偿(已记录) | 路线 3 | ~~串联闸 D4+LLM 法官~~ 已落地 |
| 4 | **格式规范伪装毒漏判** 🔶 **路线 3 部分已接线(2026-09-02)**:工具返回语义自检上线——稀释毒分类器 0.0 被语义自检"可疑"拦下(无毒对照干净),payload 截断限制在案;本体(误/漏报率标定、payload 入 CI)仍归路线 4 | record 0014;路线 3 lesson 0035 | ~~路线 3(语义自检接线)~~ 已落地;路线 4 | 语义自检已接;标定+CI 归路线 4 |
| 5 | **分类器误报/漏报率**:善意句式被误拦(0.99),高熵串误拦 | record 0014/0020 | 路线 4 | 红队持续对抗 + 阈值标定流程 |
| 6 | **护栏逐行设防**:拆行投毒=两条独立输入,各自过闸 | record 0020(阶段 19 发现) | 路线 4(种子)/ 路线 3(会话级判定) | 会话级聚合判定待研究 |
| 7 | ~~**记忆投毒毒源未防**~~ **✅ 已核销(2026-09-02,路线 3,与缺口 2 合并)** | ~~毒记忆仍在每轮试图触发~~ 核销:any-quarantine 全拒策略——装载校验发现任何异常即整个历史不装载,毒记忆沉底不再触发;副作用(历史丢失)有意接受并记录 | 路线 3 | 同缺口 2 | 已合并落地 |

## 路线 4 CI 回归集第一批种子

1. 语料 01/02/03 全部 payload(判定标准见 `02-防御回归.md`)
2. save_memory 工具内污染路径(chapter2 精读发现,比篡改文件更隐蔽)
3. 良性偏好 + 延迟触发形态(chapter2 记忆注入原文,分类器 -1.0 盲区)
4. 格式规范伪装 v3 及其变体(分类器 0.05 漏判)
5. 拆行投毒(阶段 19 发现)
