# Agent 安全学习笔记总结

1. **MCP 协议的信任边界**：MCP 协议将工具调用转为跨进程标准接口，server 与 client 之间存在天然的信任边界。

2. **ReAct 循环的执行机制**：模型生成的 tool_calls 仅是"意图"，真正执行由框架层完成，政策检查点位于翻译层。shell 和 fetch 等工具若缺乏保护，提示注入可直接转化为命令执行和数据外泄。

3. **防御的关键挂载点**：协议边界（MCP 网关）、进程边界（沙箱）、数据边界（脱敏），形成多层防护体系。此外，多轮对话的"记忆"由客户端重发历史消息实现，模型本身无状态。
