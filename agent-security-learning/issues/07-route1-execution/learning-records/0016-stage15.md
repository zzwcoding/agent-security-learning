# 0016: 阶段 15 —— 容器加固落地,爆炸半径实测锁死

**日期**:2026-08-27

**做了什么**:Dockerfile 重写(torch 走 CPU 官方索引、非 root `USER agent`、护栏模型构建期烘进镜像、模型层与代码层按变化频率排序);`docker-run.sh` 加六项加固参数;容器内冒烟 + 直接边界测试。

**实测发现**(全部留证):
- 身份:`whoami` = agent,uid=1000;`su - root` 认证失败。
- 只读:`touch /app/PWNED` → `Read-only file system`(阶段 9 战利品 B 在容器内失效);`/tmp`(tmpfs)可写。
- 降权:`CapInh/Prm/Eff/Bnd/Amb` 全零。
- 缺口实录:容器内 urllib 拿到 api.minimaxi.com 真实 HTTP 404——egress 畅通,记为已知缺口 → 路线 2。
- 全功能冒烟:三层护栏在容器内行为与宿主一致(同一条 FP 句式同样被拦);挂卷 memory.json 写入成功(3482 字节落回宿主)——`chmod 666 memory.json` + `chmod -R a+rwX workspace` 解决了非 root 容器用户写挂卷的权限问题。
- 镜像 4.96GB:CPU torch 也省不下多少(双 deberta + spacy + transformers);onnx/蒸馏优化空间记账。
- 踩坑:首次构建把模型烘焙放在 COPY 代码之后,改 agent.py 会重下 1GB 模型——重排层顺序(依赖→用户→烘焙→代码)后,二次构建 30.8s 内只重跑代码层。
- `/call` 只连 filesystem server(阶段 3 遗留),容器边界测试改用 `docker run --entrypoint bash` 直测,更干净。

**结论**:票 02 方案 A 全部落地(非 root ✓ 只读 ✓ tmpfs ✓ 限额 ✓ 降 cap ✓ 网络保留 ✓egress 记缺口 ✓);codex sandbox_mode 三档精读写进 lesson 0015(对照结论:codex 网络默认关,比我们严;approval_policy 独立维度值得路线 3 借鉴)。待用户验证、说"提交"后收尾。
