# 0009: 阶段 8 —— Docker 容器化,验收③达成

**日期**:2026-08-27

**做了什么**:`uv pip freeze` 生成 `requirements-lock.txt`(63 包全量锁定);写 `Dockerfile`(python:3.12-slim,锁文件层与代码层分离)、`.dockerignore`(挡 .venv/memory.json/summary.md)、`scripts/docker-run.sh`(key 运行时注入 + workspace 挂卷)。Docker daemon 起初未运行,`open -a Docker` 拉起。

**实测发现**:
- 容器内完整跑通验收①(读→总结→写,summary.md 经挂卷宿主可见),`run_command`、`http_get`(200)也在容器内真实触发 → 验收③达成
- 自动化跑容器要 `-i` 不要 `-it`(非 TTY 下 -t 报错)
- 核心代码行数:agent.py 135 + config.py 17 + 三个 server 94 = **246 行 < 300** ✓
- **用户实测抓到真 bug**:容器里记忆失效——`memory.json` 写在容器可写层,`--rm` 退出即焚。修复:`docker-run.sh` 加 `touch memory.json` + 挂卷(不 touch 会挂出同名目录);`load_memory()` 加空文件守卫(0 字节 JSON 会崩 `json.loads`)。两个容器会话实测:第二个容器正确答出第一个里说的名字

**结论**:issue 01 三项验收全部达成 + 容器记忆修复。待用户验证、说"提交"后收尾。
