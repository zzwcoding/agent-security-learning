# 0006: 阶段 5 —— shell + fetch 两个 server 补齐

**日期**:2026-08-27

**做了什么**:新增 `mcp_servers/shell_server.py`(`run_command`,subprocess shell=True,裸奔)和 `mcp_servers/fetch_server.py`(`http_get`/`http_post`,httpx,裸奔);`MCP_SERVERS` 改为循环生成三条目;`httpx==0.28.1` 显式锁定(此前是间接依赖)。实测:一句话触发 list_dir+run_command+http_get 三调用;POST 经 httpbin.org 回显验证 → 验收②(三工具真实调用)达成。

**实测发现**:shell server 的 cwd 是 agent 根目录,`wc -c notes.txt` 找不到 workspace 里的 notes.txt,模型被两种文件视图绕糊涂 → 工具隔离边界不统一,保留作教具,路线 1 处理。

**结论**:阶段 5 跑通。待用户验证并说"提交"。
