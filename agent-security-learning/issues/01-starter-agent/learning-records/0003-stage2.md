# 0003: 阶段 2 —— 接入 LLM 单轮问答

**日期**:2026-08-27

**做了什么**:实测两家供应商——`kimi` Keychain 条目被 `api.moonshot.cn` 拒(Invalid Authentication,印证它是 OAuth token),`minimax` 一次调通 → 定 MiniMax-M2。新增:`config.py` 三个 `LLM_*` 环境变量、`agent.py` 换 ChatOpenAI 单轮调用(langchain-openai==1.6.0 锁定)、`scripts/run-with-keychain.sh` 从 Keychain 注 key 启动。阶段 1 纯教学注释已轮换为阶段 2 注释。

**实测发现**:MiniMax-M2 是推理模型,`<think>...</think>` 思考块直接混在 content 里输出;单轮无记忆(连问指代词会丢上下文)符合预期,留给阶段 6。

**结论**:阶段 2 跑通。启动:`scripts/run-with-keychain.sh`。待用户验证并说"提交"。
