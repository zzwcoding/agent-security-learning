# 0002: 阶段 1 —— 项目骨架 + CLI 回声循环

**日期**:2026-08-27

**做了什么**:建 `starter-agent/`(`config.py` / `agent.py` / `requirements.txt`,共约 35 行,纯标准库);根 `.gitignore` 补 `.venv/`;`uv venv .venv` 建环境;管道输入验证回声与 `/quit` 退出均正常,`workspace/` 目录随启动自动创建。

**卡在哪**:无。

**结论**:阶段 1 跑通。启动命令:`cd starter-agent && .venv/bin/python agent.py`。待用户验证并说"提交"。
