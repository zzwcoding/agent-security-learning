# record 0039 - 阶段 41:哈希链证据日志 + 参数级分级(2026-09-02)

**学了什么**:哈希链最小实现(entry_hash=sha256(prev+canonical_json),sort_keys 规范化,append-only jsonl);独立验证器(重算全链+断点定位);参数级 data_class(参数模式匹配命中升级→落回工具级默认);审计中间件一次遍历双写(Langfuse 观测面 + 本地证据链,两面对照:观测可变、链不可抵赖)。

**卡在哪**(两个都进了 lesson):① 单链验证通过、双链必断——写入器漏拼 prev 前缀(sha256(canonical) vs 验证器 sha256(prev+canonical)),prev="" 时两者巧合相等;教训=测试必须覆盖链长>1 的真实形态,巧合通过比失败更危险。② seek 回退找链尾在单行小文件上越界(Errno 22)——教学规模直接 read_text 尾行,不过早优化。③ 实验噪声:文件在追加中尾部变化导致 diff 对象错位——验证实验要锁定同一条目。

**攻击实证**:篡改第 1 条 params→断;删除第 1 条→后条 prev 对不上断;全链 2 条验证通过(修复后)。notes.txt 测试残留已清。

**结论**:审计三面齐——Langfuse(可观测)/audit_trails(网关平台面,已在攒)/哈希链(不可抵赖锚点);data_class 升参数级(凭证/出网/删除模式)。阶段 42:短时令牌(现在 mint 脚本的 60 分钟管理员票 → 演进为任务级+最小 scope)。
