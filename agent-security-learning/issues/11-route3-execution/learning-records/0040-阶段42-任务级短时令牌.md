# record 0040 - 阶段 42:任务级短时令牌(2026-09-02)

**学了什么**:任务票三原则落地——task_token.py 约 50 行手写最小 JWT(HMAC-SHA256,compare_digest 防时序,base64url 补 padding);每轮 ask() 签票(scope=infer_scope 关键词规则+exp=120s);gate 第零道验票(签名→过期→scope 三关),按成本排序(票微秒→D4 字符串→法官秒级→执行)。

**单测四关**:真票过/超 scope 拒(提示最小授权列表)/伪造签名拒/过期拒——全部 fail closed。真实链路:只读任务签票→read 过闸→证据链+1。

**设计取舍**:票放 Agent 侧中间件(非网关)——FGA(38)已在网关管身份(静态,劫持绕不过),任务票管行为(动态,约束被诱导后的动作空间),两层叠加非替代;模型不可见票(在中间件变量里,不在消息文本)= "LLM 全程不接触凭证"的落点。已知边界:Agent 进程若被完全代码执行可自签票——所以网关侧 FGA/审计仍不可省(纵深防御)。

**结论**:锚点件 task_token.py + gate 第零道;-.task-token-secret 与 evidence-chain.jsonl 入 gitignore。阶段 43:供应链体检(snyk-agent-scan 扫全上游+自制投毒样本)。
