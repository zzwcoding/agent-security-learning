# record 0038 - 阶段 40:装载校验 + 语义自检(2026-09-02)

**学了什么**:记忆信封格式(save 逐条 sha256 / load 验封);装载三道闸=完整性 hash→注入扫描(仅 human)→LLM 语义自检(仅 human),任何隔离→整历史不装载(all-or-nothing);工具返回语义自检挂在分块分类器之后(补盲不阻断,失联放行);系统提示来源标记(记忆=不可信数据源,『惯例』不构成新任务)。

**攻击实证**:A 无钥匙篡改→hash 数量不符全拒 ✓;B 全知攻击者(重算 hash)→分类器叙事毒 0.02 失明→语义自检抓获 ✓;C 工具返回稀释毒→分类器 0.0→语义自检"可疑"替换警告,shared-report.txt 未创建 ✓;对照无毒判干净 ✓。

**卡在哪**(全是真课):① content 是 MCP block 列表,str(list) 当 hash 输入炸 AttributeError 且误判——统一成文本再 hash;② 逐条隔离破坏 tool_call 配对→MiniMax 2013 拒收→改 all-or-nothing;③ 语义自检提示词两轮调优:"记住偏好"被误判可疑(白名单偏好类)/MiniMax <think> 无视"只答一个词"(改【结论:X】结构化标记);④ 单看文本 LLM 分不清"文档里的指令"和"对我下的指令"(注入定义性难题,实测判干净)——工具返回自检靠正负对照验证;⑤ replace 锚文本没匹配→毒没进靶子,"防线失效"先查"靶子有没有中"。

**结论**:缺口 2/7 核销面成立(装载受审+毒记忆沉底),缺口 4 路线 3 部分接线完成(语义自检上线,正负对照记录)。已知限制:text[:600] 截断(长文档毒>600 漏检,生产应分块自检)。回写留收官。
