# record 0031 — 阶段 32:白名单对照讨论 + 隔离光谱精读(2026-09-01)

## 完成内容

1. **对照讨论**(lesson 0027):精读 `chapter6/async-agent`(实验 6-2 Flux)的进程级白名单实现——`resolve_job` 可执行档位枚举、`shell=False` exec、`python -I` 隔离模式、SHA-256 执行回执。结论:进程级管"能跑什么",microVM 管"跑的地方能碰到什么",正交不竞争;选择判据 = 任务面可枚举性 + 威胁模型假设强度。借鉴点:command 的 SHA-256 进审计观测(路线 3+ 待办池)。
2. **精读笔记**(lesson 0028):Firecracker design doc(一进程一 microVM、5 microVMs/核/s、极简 virtio 设备、"vCPU 启动即视为恶意"、jailer 降权、**"不做任何网络过滤,过滤是宿主层责任"**)+ gVisor 架构指南(Sentry 用户态内核零 syscall 直通、双墙原则、Gofer 文件代理、ptrace→systrap→KVM 平台演进、纯 Go 工程纪律)。核心原理:攻击面 = 暴露给不可信代码的代码量。
3. **顺手修**(阶段 31 演练发现):`filesystem_server.py` 路径归一化——首段与工作区同名时剥掉(`workspace/notes.txt` 不再拼成 `workspace/workspace/...`),越界检查后置不变。stdio 验证:双写法等价、`../` 与绝对路径逃逸照拒(`/tmp/verify_fs_fix.py`)。

## 过程发现与修正

- **chapter 编号误记**:票 08/MISSION/RESOURCES 写的 `chapter5/async-agent` 实际在 **chapter6**(chapter5 无此目录;5-12 的 permission-embedded-data-objects 是数据层授权主题,属路线 3 预习材料)。已在 MISSION 行 32 注记 + RESOURCES.md 修正。
- 按既定决策(票 08 Answer 第 4 条)**未落地** async-agent 白名单——对照讨论即交付物。

## 编号

lesson 0027、0028;record 0031。验证脚本 /tmp(依赖级)。
