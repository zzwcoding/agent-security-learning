node_modules/
dist/
.venv/
__pycache__/
*.pyc
.DS_Store
data/
*.db
.env
.env.local

# archify visual-check 的截图/报告副产物(可再生)
docs/*.visual-check.*

# 票 12：setup-openfga.sh 每次重建世界都会刷新（内存存储 id 不稳定），不进库
services/gateway/plugins/fga_ids.json

# m11 eval 运行产物（evals/ 每次跑测重新生成，latest.json 供 m10 Eval 页读取）
eval-results/

# 票 48：gen:tool 脚手架的默认生成物目录（演示产物不入库，入库的是生成器本身）
generated-tools/
