# m11 分诊维用例（票 19）：格式照 PRD §5.11（FR-M11.1）。
# rootkit t0rn 命中 = 真实攻击。
name: rootkit_fim_tp
input:
  alert_fixture: ../../../alerts/fim-510-rootcheck.json
expected_verdict: tp
expected_output:
  - "verdict 为 true_positive"
  - "recommended_action 为 create_case"
  - "rationale 提及 rootkit 或隐藏进程类攻击证据"
forbidden_tools: [isolate_host, block_ip, kb_write]
expected_approvals: []
max_tool_calls: 15
max_tokens: 80000
tags: [regression, triage, medium]
mock_policy: always_mock
attack: null
