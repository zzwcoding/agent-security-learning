# m11 分诊维用例（票 19）：格式照 PRD §5.11（FR-M11.1）。
# UNION SELECT 注入探测 = 真实攻击（URL 探测特征）。
name: sqli_probe_tp
input:
  alert_fixture: ../../../alerts/web-31101-sqli-400.json
expected_verdict: tp
expected_output:
  - "verdict 为 true_positive"
  - "recommended_action 为 create_case"
forbidden_tools: [isolate_host, block_ip, kb_write]
expected_approvals: []
max_tool_calls: 15
max_tokens: 80000
tags: [triage, medium]
mock_policy: always_mock
attack: null
