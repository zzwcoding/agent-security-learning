# 阶段 7 收官体检报告（2026-09-09）

> 两个审计窗口（A 三类对账 / B 结构扫描）只读产出，L0 合并。依据 sdd-flow SKILL.md 阶段 7 三条款 + 核心原则 7。
> 状态：候选清单，待用户拍板；选中的转 issue 回阶段 4。

## 总评

模块纪律整体健康：测试绝大多数打在 seam 上，fixtures/tickets 跨语言契约是其余缺口该长成的样子；回补票 23-27 清偿的框架欠账全部核实在场，无回退（对账一-6 正向确认）。真正的问题集中在两类：**数据形状契约没有机器锁**（其中一处已经实际炸了）和 **边界纪律全程未机器化**（规则节与闸双缺席）。

## A 组：已炸 / 硬欠账（建议立即转票）

- **A1 [对账三-13] latest.json 形状三方漂移，Eval 页拦截率渲染不出**——票 22 产出 `defense_interception.by_face{...}`，web 读 `attack_block_rate`（旧形状），PRD §6-M11 样例是第三种形状。web eval.ts 恒得空集。已炸的集成缺口。处置：PRD 样例按实现改 + 补「evals 产出→web 消费」双端契约测试。
- **A2 [对账二-1/2] 边界规则节缺席 + CI 边界闸缺失**——阶段 2 产出 1b 与阶段 6 硬交付物双欠账。审计已盘出 R1-R8 候选规则表（含逐条现状），可直接落卡。处置：回阶段 2 补节 + 阶段 6 建闸（仿 check_specs.py 几十行自写脚本先例）。
- **A3 [对账三-15] alert wire 全链无锁**——ingest→M2→agent 三段各有本地测试但链条无 fixture 锁；已实际咬过：hostname observable 缺口（票 13 偏差①，FR-M2.4 归并空转）。处置：转票做全链 fixture 断言。

## B 组：承诺悬空——二选一（补票 或 记 ADR 砍）

- **B1 [对账一-1] Langfuse observability profile**：ADR 0001/PRD v1.1 承诺「可选 profile」，compose 零落地。
- **B2 [对账一-8] Wazuh real-wazuh profile（P1）**：PRD 承诺，无实现无裁决。
- **B3 [对账一-5] LangChain.js 部分落地**：编排面由 LangGraph.js 承载（票 23），LLM I/O 走凭证代理直连（票 27）——建议一句话 ADR 收口为正式决策。
- **B4 [结构-15] investigation/enrichment 生产面无入口**：RUN_KINDS 只放行 alert_flow/knowledge_flow/chat_flow，PRD 消息旅程步骤 7-8 在产品上不可达（仅 evals 直构）。二选一：alert_flow TP 分支链上 enrich→investigate，或卡+README 记「v1 刻意 eval-only」。
- **B5 [对账一-4] compose 的 agent 无数据卷**：runs/checkpoints 容器层易失，m3 卡「杀进程重启恢复」演示在 compose 一键起时不成立。补卷或改卡。

## C 组：接口文档对齐（改卡，量小，可打包一票）

- **C1 删/改幽灵接口**：m1 卡 `POST /internal/replay`（删，定案 replay.ts）、m10 卡「MSW 打桩」→「fetch stub」、m9 卡 task_token.py 措辞。
- **C2 补卡未声明接口 8 条**：POST /alerts、PATCH /alerts/:id、reopen、cases/:id/observables、GET /events 形状、/internal/used-tokens×2、/internal/runs 扩展参数、/api/v1/auth/login 钉路径；healthz 统一豁免记一笔。

## D 组：数据形状契约补机器锁（转票，可合并）

- **D1 SSE 事件类型双端锁**（对账三-14 = 结构-3，三份手抄已漂移过一次）+ **verdict 词表跨服务锁**（结构-4，四处各持）——建议合并一票。
- **D2 guards 响应形状/通道枚举跨语言锁**（对账三-16）+ **注入语料共享锚**（结构-10，py/TS 两套已实际漂移）——仿 fixtures/tickets 先例，建 fixtures/guards 或共享语料 fixture。
- **D3 token-ports TS 侧 wire 形测试**（结构-13，整模块零测试）+ **RealChatLlm 降级分支测试**（结构-14）——测试补齐，S 规模。

## E 组：越界引用——L0 裁决建议（用户可否决）

- **E1 evals→agent 内部大面积直引**（对账二-4）：决策 #10「快道单测级注入」客观要求进程内组装。**L0 建议：ADR 豁免**，限「组装入口清单」（runner/scenarios/judge/assertions 五文件），禁触 db/store 内部写路径；随 A2 建闸时落豁免清单头两条。
- **E2 testkit 直引 case-backend 内部**（对账二-5，跨可部署单元，被 evals 转手扩散）：**L0 建议：补票改造**（夹具数据+真 REST），测试代码豁免只给 E1。
- **E3 ingest 测试反引 scripts/replay**（对账二-6）：一行改法（子进程跑），随 A2 闸补。

## F 组：低优先候选（可不做/后置）

- F1 [结构-6] gated() 验票包装六处重复（安全语义集中度最高，M）——值得做但非欠账。
- F2 [结构-1/2] run kind 三张平行表 + 9 文件触点（kind 描述符注册表，M）。
- F3 [结构-8] 三客户端 fail-closed/超时模式抽共享件（S；已现 reason 标签口径漂移）。
- F4 [对账一-2/3] openfga 钉 digest、msb 版本记录（S）。
- F5 [结构-7/9/11/12] m2.ts call()×4、ChatSeam×3、token-ports fetch 块×2、McpTool×2（S 微，顺手清理级）。
- F6 [结构-5] evals scenarios.ts 1040 行拆分（M）。
- F7 [对账一-7] tsx 挪 devDep、web/dist 入 gitignore（lint 级，不开票）。

## 已驯服/刻意拍板（非候选，记录防重复审计）

py-TS 双验票实现（契约测试锁，ADR 0001）、params_hash 双语言实现（共享测试锚）、web sse.ts vs chat.ts（伪重复）、Fake/Real LLM 双 adapter（真 seam 正例）、薄 REST 壳（卡面明写）、手写 hash 路由（m10 拍板）、846 行 store.ts（深实现）。

## G 组：遗留标记回收（第四条对账，2026-09-09 执行）

扫描：27/27 票全文通读，55 条标记——已闭合 30（后续票兑现或已有裁决记录），未闭合 25（归并 21 线头）。三选一定稿如下。

### G0 L0 追认（当场闭合，无需票）
| 标记 | 裁决 |
|---|---|
| [17-3] FR-M7.4「结论一致」操作化为「结论仍正确」 | **追认**（PRD 原文口径，replay eval 用例照此实现） |
| [17-4] FakeTriageLlm R1 扩 env_fact | **追认**（测试 stub 对齐真实 KB 种子） |
| [26-1] digest 钉 arm64 子 manifest 而非 index | **追认为常设口径**（containerd store 实证限制）；升级跟踪以 index digest 前进为准 |
| [26-2] contextforge 升级跟踪两查 | **本轮已执行**：新仓仍 DENIED、旧仓 latest index `sha256:31cf1d45…` 未前进→无需动作；口径：每轮体检跑一次 |
| [27-1] m5 卡 LLM adapter 行 | **确认无需改**：m5 卡声明的是 SIEM adapter，LLM 面已由 m4 卡同步覆盖 |
| [09-2] /internal/replay 幽灵接口 | **L0 裁决：删卡**（票 09 定案 replay.ts 为唯一通道），归 C 组文档对齐票执行 |
| [27-1 元发现] 票 19/20 缺实现记录节 | **L0 已补记**（见两票文末，依据窗口回报回填） |

### G1 归属仍成立（该票去修 → 并一张 m1 小补票候选）
[13-1] m1 补 hostname 映射一行（FR-M2.4 归并空转）；[15-2] m1 补 TLP 溯源（wazuh.ts:95 硬编码 tlp:2）

### G2 归属已失效 → 转候选清单
- **G2-1 [11-1] 焚毁表跨进程读口未接**（安全相关，建议置顶）：verify-ticket 生产读仍"待 M2"，跨进程重放靠 executed_at+TTL 兜底
- **G2-2 [10-1]+[08-1]+[09-3]+[21-②] worker/链路审计汇入 M2 一弦**：HttpAuditSink 未换、FR-S5 两路汇入未落、webhook FAILURE 审计、evals 缺 m9 凭证金丝雀全链路断言（INV-4）
- **G2-3 [10-2]+[11-2]+[18-①] run 异步化一弦三标记**：同步直跑/APPROVAL_TIMEOUT/异步化后续票，三处同线头
- **G2-4 [07/13/14] ToolManifest 机制票不存在**：三票标记悬空，L0 读随任务票过闸的"更严口径"待对表
- **G2-5 [14-2] add_task_log 两头没动**：工具面仍声明、执行仍报错——二选一收口
- **G2-6 [14-3] 调查循环未接 guards tool_output 扫描**
- **G2-7 [13-2] SOC1 确认票 close_alert 入口缺失**（FP/BTP 关单建议无人可执行）
- **G2-8 [14-1] invest 具名 eval fixture 未落 evals**（票 22 无 investigation 域）
- **G2-9 [17-6] case.closed 自动触发提炼 + 告警事件自动拉起 run 未接**
- **G2-10 [21-1] compose 网关 SOC_HMAC_KEY 注入无主**（fail-closed by design，弱）

### G3 差用户拍板（砍 = 记 ADR，与 B 组同批）
- [24-1] FR-S4.2 脱敏 mapstore（票 24 点名本轮呈报）：补实现 or ADR 砍改投影
- [09-1] FR-M1.6 Wazuh logtest 真容器模式（= B2 同源）：立票 or ADR 砍
- [09-4] pipeline_pending/死信队列：**L0 建议确认覆盖性替代**（outbox 同事务落库使该场景架构上不存在），与 B1/B2 砍项打包成 ADR 0003 待批
