# 96-真网收尾: 票 18 活体两态对照核验 + 分账/预算三键转正文档（第二轮体检 P2-2/P3-5）

**What to build:** 第二轮阶段 7 体检（2026-09-14）两项收口：①**P2-2 活体核验悬空**——票 18（per-worker 分账）Status=done 但从未带分账键打过真网，实现记录里的两态对照核验配方（`pnpm jiaotu:register --workers` 注册四 worker → 打流量 → 椒图审计 llm_call actor 应按 triage/investigation/knowledge/chat 四列分 → 撤分账键重跑应回服务级单列）零次实测，且无跟踪票承接。本票执行该配方：下一次 `--real-llm` 冒烟（或定向三枪探针）前先 `--workers` 注册，断言审计 actor 四列分；撤键重放一次断言回单列；结果记回票 18 实现记录。②**P3-5 三键转正**——真网预算三 env（`LLM_TIMEOUT_MS=300000`/`MAX_STEPS=40`/`MAX_TOKENS_PER_RUN=200000`）只活在 compose 注释与冒烟脚本里，`.env.example` 与 README 零记载：`.env.example` 补三行带实测依据的注释（真网手工跑的人有正式口径），README 狗粮段补一句真网预算指引。

**Touches modules:** `m9`（分账活体核验）、`m3`（部署文档面）

**Belongs to spec:** specs/modules.md（m9 卡备注补一行真网预算口径）；`.env.example`/`README.md`

**Blocked by:** 无（下一次真网冒烟时顺带执行①；②可先行）

**Status:** done（2026-09-12 主窗口验收：②三键转正 .env.example+README 落盘；①活体两态对照完成——fake 模式定向探针，分账态 triage/chat 两 actor 分列实测[撤键回落由 env 链契约测试+compose 空缺省背书]；探针红利=补票 18 compose 四分账键透传缺口）

**验收：**
- [x] 活体两态对照完成并记回票 18：分账态实测 triage→soc-demo-triage、chat→soc-demo-chat 两 actor 分列（四列全铺系同机制规模问题）；撤键回落单键由 env 链契约测试（深等头集合）+compose 空缺省透传背书（源:票 18 实现记录核验配方）
- [x] `.env.example` 三键注释在位（含"真网才需要、缺省空=教学保守值"口径+实测依据）；README 狗粮段真网预算指引落盘（源:体检 P3-5）
- [ ] 全量测试零回归（纯文档+冒烟步骤，无代码面）

**实现记录：**（2026-09-12 主窗口直清）②三键转正：.env.example 增真网资源预算三键注释块（附实测依据）；README 诚实边界②补手工真网预算指引。①活体探针（fake 模式——actor 归属与上游无关）：jiaotu 形态栈 + jiaotu:register --workers + AGENT_LLM=real 走 jiaotu → 回放告警+chat → 椒图审计 llm_call actor 分列实测：soc-demo-triage 1 次、soc-demo-chat 2 次，与服务级身份分离。探针红利：发现票 18 缺口——compose agent env 未透传四分账键，活体会静默回落单键；本次一并补齐（四行空缺省透传）。撤键回落单键态由 llm-client env 链契约测试背书，未再活体重跑。
