# 60-teaching-supplement-diagram-alignment: 教学文档优化改写 + 每场景一张大图（场景 2-8）（P1）

**What to build:** 学习者跟场景 1 文档+大图学 1.1-1.4 的卡点复盘（2026-09-11，47 问归类两轮）已沉淀为机制：learn-by-rebuild"叙述完备性七原则"、soc-diagram-conv"图文对齐约定+大图职责分工"、`lessons/scenario/TERMS.md` 术语表。本票把机制落到**场景 0 新增 + 场景 2-8 存量文档优化改写与场景大图**。

**范围裁定（2026-09-11 用户拍板，含当日两处修正）：**
- **场景 1 的教学文档与大图保持原样不动**（已学完/已画过，作为旧风格参照系保留）。
- **教学文档的定位是"优化改写"，不是打补丁式增补**——用户原话"原来写的有点看不懂"。按七原则逐条对照**重写看不懂的地方**：被吞的中间步骤补时序接缝、只讲"做什么"的补"为什么/不做会怎样"、术语就地重解释改为回指 TERMS 同一条、多层概念（票四层等）补分层预告。硬约束：引用代码块逐字保留（行号校准只在指定文件做，引用内容零改动）、比喻体系只用 TERMS 存量那家、不改生产代码。
- **大图是一个场景一个 HTML，不是 8 个场景合进一张**——场景 N（N=2..8）新建 `lessons/scenario/N-big-picture.html`，只画该场景的全部步；场景 1 的大图沿用旧文件 `8-scenarios-big-picture.html` 零改动。跨场景全局地图的职责归 0-0.md（速览段的"全局数字"也归 0-0，场景大图速览段只写本场景数字）。
- **执行模式：主窗口把控进度，串行派发全新子 agent，一个场景一次任务。**

**任务清单（依赖序 = 执行序）：**
- **任务 0 · 场景 0**：新增 `lessons/scenario/0-0.md` 全景导览（编制表 9 容器 core + 3 个 profile 可选容器诚实标注 / 常驻循环表 3 循环 / 身份切换图 / 全局数字 / 与大图分工声明——入门靠 0-0，回查靠各场景大图）。不建 HTML。
- **任务 2 · 场景 2**：2-1..2-5 七原则优化改写 + 2-1..2-4 行号校准（票 50/51 衍生，lesson-lint.py 跑绿，引用内容零改动）+ 建 `2-big-picture.html` 画 S2 五步（速览段+节点明细表随图首建）。
- **任务 3 · 场景 3**：3-1..3-5 优化改写 + 3-4 行号校准 + `3-big-picture.html` 画 S3 五步。
- **任务 4 · 场景 4**：4-1..4-7 优化改写 + `4-big-picture.html` 画 S4 七步（审批链是安全节点密集区，🛡️ 清单逐个挂）。
- **任务 5 · 场景 5**：5-1..5-5 优化改写 + `5-big-picture.html` 画 S5 五步。
- **任务 6 · 场景 6**：6-1..6-4 优化改写 + `6-big-picture.html` 画 S6 四步。
- **任务 7 · 场景 7**：7-1..7-4 优化改写 + `7-big-picture.html` 画 S7 四步。
- **任务 8 · 场景 8**：8-1..8-5 优化改写 + `8-big-picture.html` 画 S8 五步（收官）。

每任务完成后：主窗口验收（lesson-lint 机检逐篇绿 / 📍 节点 ID 与该场景 HTML 节点逐一对应 grep 核对 / 新 HTML 的 JS 段与场景 1 图逐字节一致 / 行号校准零内容变更 / 场景 1 文档与图 diff 为零）→ 精确 add 提交（格式 `教学增补场景X:…`）→ 再派下一场景。

**Touches modules:** 无（纯 lessons/scenario/ 教学文档 + 各场景大图 HTML）

**Belongs to spec:** lessons/scenario/00-导览总纲.md（文章模板七原则改写）

**Blocked by:** 无

**Status:** ready（场景 1 排除 + 文档定位改写 + 大图每场景一个 HTML，2026-09-11 用户拍板）

**验收：**
- [ ] 0-0.md 就位：编制表/循环表/身份切换图/全局数字四块齐全，与大图分工声明在列
- [ ] 场景 2-8 每站有 📍 节点行，节点 ID 与该场景 HTML 实际节点名逐一对应（grep 核对）
- [ ] 每场景一张大图：2-big-picture.html ~ 8-big-picture.html 各覆盖本场景全部步（S2-S8 共 35 步）；安全节点红描边+🛡️ 按清单挂全；每张自带速览段+节点明细表
- [ ] 场景 1 文档与 8-scenarios-big-picture.html/.md diff 为零（范围裁定红线）
- [ ] 2-x/3-4 行号校准后 lesson-lint.py 全绿；引用内容零改动
- [ ] 改写后文档遵守七原则本身（自指合规）；术语回指 TERMS 同一条
- [ ] 总纲学习日志逐任务记录；票 60 逐任务勾选

**实现记录：**（待填）
