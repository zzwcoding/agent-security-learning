# 交接包：编排循环主线战役（L0 总窗口）

```
任务: 编排循环主线战役——bench 收尾 → 机制层 → 工具与内容 → 扩展 → 体检 → 教学链
角色: L0 总窗口（新窗口以本交接包入场，不是 L2）
项目: /Users/divh/Downloads/安全评估agent/soc-demo
```

## 0. 背景（一段话）

soc-demo v1 已收官（69 张票，evals 33/33）。编排循环（假设驱动多 agent 动态编排，m14）的约定层已完成：**票 70**（PRD v1.2 §13 逼问定稿）→ **票 71**（m14 卡 + R10-R12 边界三新条 + 页面映射 + 架构 v5 源 JSON 冻结，ADR 0005）→ **票 72**（`specs/orchestration-loop.md` 定稿，验收测试 T01-T23 全绑测试标识）。开发链 73-83 与教学链 84-89 已挂好并回填 Spec 绑定。本战役 = 按序把它们全部做完。

## 1. 主窗口先读（最小清单，按序）

1. `CONTEXT.md`（术语 + 语义核心 INV-1~11 + hypothesis 五态）
2. `docs/prd.md` §13 全章（分层铁律/机制水位线/五业务/逐维结论/页面映射）
3. `specs/orchestration-loop.md`（验收测试 T01-T23 是唯一验收真源）
4. `specs/modules.md`（m14 卡 + 边界规则 R1-R12 + 页面映射节）
5. `docs/adr/0005-architecture-v5-orchestration-loop.md`
6. 本战役全部票：`.scratch/tickets/issues/{64..69,73..83,84..89}*.md`
7. sdd-flow skill（`/Users/divh/.agents/skills/sdd-flow/SKILL.md`）——全程纪律来源

## 2. 主窗口纪律（sdd-flow L0，不可妥协）

- **不直接写实现代码**。一切实现走派发：一票一个**全新子 agent**、**前台**、**严格串行**——一票收口（含体检）才派下一票，哪怕依赖图允许并行也不并行。
- **关票四条件**（缺一不关）：① 该票验收框逐条有证据；② 子 agent 回报附**测试输出原文**（通过数/失败数）+ code-review 结果（自主档派发失败自实现时 code-review 必跑）；③ 收尾回写五样齐全（spec 出入/modules.md 同步/CONTEXT 术语/lessons 施工日志/架构投影）；④ 双闸真跑：`python3 tools/check_specs.py` + `python3 tools/check_boundary.py`（在 soc-demo/ 下）全绿。
- **提交节奏**：每票收口后**等用户说"提交"才 commit**（沿用用户习惯），commit message 按仓库惯例"票 N: 一句话（关键数字）"。
- **状态即产物**：每票收口即翻 Status、填实现记录（含偏差声明）；主窗口维护战役进度段（本文件 §7）。
- **升级不就地**：子 agent 报"需要改接口/越界/框架冲突"→ 停下回报，主窗口回阶段 2/3 走变更，**不许就地绕过**。

## 3. 执行序列（六阶段）

### 阶段 A：bench 收尾链（票 64-69，现有 ready 票）

顺序：64 → 65 → 66 → 67 → 68 → 69（注意 64/65 各有一张**同号发现票**并入处理，票内已注明）。模式/红线按各票自带 铁律；这些是压测与防线实验票，验收以各自票为准。派发简报照 §4 模板，先读只放该票 + m13 卡。**发现新问题时开发现票插入序列**（64/65 即先例），不就地修。

### 阶段 B：机制层（票 73-77，严格串行）

73 骨架（轮次链六节点 + fanout/await_children 事件唤醒 + run-kinds 注册 + 审计/SSE 接线）→ 74 planner → 75 judge+gap → 76 两票票务 → 77 预算双闸。**红线全员适用**：LangGraph.js 真用（禁手写替代、禁改 compileFlowGraph 串行链拓扑）；循环拓扑落 dispatcher 层；await_children 禁轮询（T19 静态断言）；INV-3/8/10/11 引用不复述。

### 阶段 C：工具与内容（票 78-80，严格串行）

78 四 SIEM 维度工具 → 79 狩猎内容包（模板 + 菜单子集 + weknora 三工具 Memory stub）→ **80 应急取证模板（架构验收件）**。80 的零增量断言（diff 不触 m14）若失败：**不是 80 返工，是阶段 B 回炉**——主窗口开回炉票或回阶段 2 裁决，这是分层铁律的试金石。

### 阶段 D：扩展（票 81-83，严格串行）

81 紫队 eval → 82 Web 狩猎页 → **83 条件票**：weknora 外仓（`/Users/divh/Downloads/安全评估agent/weknora复刻`）未完成「1.5 HTTP API 幕 + 第二幕 SOC 实体图谱 + 改线版 PLAN.md」前**挂起不派发**，主窗口每阶段收口时问一次用户外仓进度。

### 阶段 E：架构体检（阶段 7 四对账，主窗口亲自做）

全部开发票收口后，主窗口跑体检（可用 improve-codebase-architecture skill 辅助扫描），**四对账必做**：
1. **技术栈对账**：PRD C1-C10 表逐行对 lockfile/compose（含 hunt_flow 的 LangGraph.js 真用、weknora 客户端未点名不算欠账——stub 是 spec 定案）
2. **边界对账**：边界闸活着 + R1-R12 豁免逐条复核（R10-R12 无豁免，查票 73-79 施工有没有绕道）
3. **接口契约对账**：modules.md 各卡公开接口 vs 实际暴露面（双向：幽灵接口/未声明接口）；**重点：m2 假设五端点与卡面对账**、页面映射六行逐行复核
4. **遗留标记回收**：73-83 实现记录里"暂缓/留后续票/记票"逐条三选一
产出候选清单 → **用户拍板** → 主窗口开票 → 子 agent 串行修复 → 复跑体检至收敛。

### 阶段 F：教学链（票 89 → 84-88，严格串行，互动档）

顺序：89 TERMS 词条 → 84 S9 → 85 S10 → 86 S11 → 87 S12 → 88 S13。特殊纪律：
- **learn-by-rebuild 互动档**：分步教学，**用户说"下一步"才推进**；每场景文章落 `lessons/scenario/<S>-<步>.md`，格式对齐既有 8 场景（TERMS 比喻/亲手验证/捣乱实验/场景题/📍 大图节点锚点/文件:行号 行号实测非凭记忆）。
- **大图：soc-diagram-conv skill**（`/Users/divh/Downloads/安全评估agent/.agents/skills/soc-diagram-conv/SKILL.md`，出图前必读）——**一个场景一张** `lessons/scenario/N-big-picture.html`（S9=9-…S13=13-），mermaid 暗黑配色、"图文对齐约定"8 条全遵守（节点明细表四栏/一条边一个动作/跨容器红虚线/子图三段标题/速览段三小块/文档→图单向锚点/不做悬停 tooltip/回补上一步明细）。
- 安全节点红框 + 🛡️ + INV 编号（planner 防菜单外选择 INV-11、子票闸 INV-3 等新安全节点按 skill 清单一并登记）。
- `00-导览总纲.md` 场景清单五行随各场景票收口翻 ✅；全局数字（总步数等）归 `0-0.md` 更新。

## 4. 派发简报模板（每票照此发给子 agent）

```
任务: <票号+一句话目标>
角色: L2 编码
模式: 自主（spec 已定稿 + 验收可测 + CI 存在；64-69 按各票既有口径）
项目: /Users/divh/Downloads/安全评估agent/soc-demo
框架红线: <抄该票铁律节；全员必含：LangGraph.js 真用禁手写替代（73-77）>
边界红线: 默认按 specs/modules.md「边界规则」R1-R12；本票特有限制 <抄票内边界红线句>
先读: <最小清单：specs/orchestration-loop.md 的 T 行 / 本票 / 触及的卡节，禁止全仓扫读>
调用: tdd（测试先于实现、写 seam 处）
产出: <票内 What to build 的产出路径>
验收: 该票验收框逐条 + spec 验收测试表 <T 编号> 对应条目
回报: "<票号>完成，产物在 <路径>，偏差: <有/无+细节>，测试: <测试输出原文摘要——通过数/失败数>，双闸: <check_specs/check_boundary 输出尾行>"
```

## 5. 风险预案（主窗口处置卡）

| 情况 | 处置 |
|---|---|
| 80 零增量断言失败 | 阶段 B 回炉：开回炉票（改 m14 抽象）或回阶段 2 用户裁决；80 保持 open |
| 子 agent 报框架/环境冲突 | 停下回报 L0 三选一裁决（改环境/换获取/记 ADR 换实现），不就地手写 |
| bench 链（A 阶段）发现新问题 | 开发现票插入当前序列（64/65 先例） |
| weknora 外仓未就绪 | 83/88 挂起，问用户外仓进度，不阻塞主线 |
| 双闸红了 | 不关票；先修闸或修代码，红着进主干 = 违规 |
| 用户中途改需求 | sdd-flow 变更路由：PRD 翻逼问中 → CONTEXT 先行 → 受影响 spec 翻草稿 → 重走 3→4→5 |

## 6. 完成定义（战役收口）

- 73-83 全 done（83 可挂起但有明确外仓条件记录）；体检四对账收敛；84-89 全 done；0-0.md 与导览总纲更新；`lessons/scenario/` 有 S9-S13 五套（文章+大图+场景题）；evals 在 33/33 基础上新增狩猎 scenario 全绿；双闸绿；用户说"提交"的 commits 按票组织完毕。

## 7. 战役进度（主窗口维护）

- [x] 约定层：70 PRD v1.2 / 71 架构层+ADR 0005 / 72 spec 定稿（2026-09-12 前窗口完成，commit 549a92b + b5f3aec）
- [x] 阶段 A：bench 收尾 64-69（✅ 2026-09-13 全收口：64 f703594/65 69df7ae 前窗口、66 04cc935、67 d5f7ff5、68 6dacadc、69 051802a；L0 档位修正 66=sustained 2/4/6/8 按 §六分母；留观挂账 1 条=68 演示档尾部 16 run 未终态（阶段 E 回收）；狗粮线同号发现票 64-investigation提示面漏扫 / 65-chat分类prompt候选硬约束仍 ready 未施工——属票 59 真网线，不在本战役，待用户指派归属）
- [x] 阶段 B：机制层 73-77（✅ 2026-09-13 全收口：73 eace177（含 L0 裁决 autorun 补线+parksOnEvents 追认）/74 fb73f03/75 1fff48d/76 8e8c02a（R2 hunting rig 追认）/77 补线收口；拆票洞两处补：m2 假设实体并 73、T10 补绑 77；挂账移交阶段 E：case_id 承载正名、预算档位真执行体复测、5 处既有 lint、evals flake 观察）
- [x] 阶段 C：工具与内容 78-80（✅ 2026-09-13 全收口：78 1dfcecd 四 SIEM 工具/79 06d12c9 内容包+真执行体接线/80 零增量验收通过=循环抽象成立，check_zero_increment.py 已接 CI；PRD §13.4b 全部工具位销账）
- [ ] 阶段 D：扩展 81-83
- [ ] 阶段 E：体检 + 修复票
- [ ] 阶段 F：教学链 89 → 84-88
