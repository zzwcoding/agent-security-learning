# 0049 · 阶段 2（进行中）：模块划分图 + 架构图 v4 + 导览式验收

## 三问

- **终极目标**：票 18 技术设计——把 PRD 的 M1-M12 落成可施工的模块边界与接口，并让用户在图上掌控全局。
- **为什么这样做**：验收质量取决于用户真看懂，所以不丢长文档，改"导游式"逐节点过图。
- **解决什么麻烦**：长文档没人读、口头决议漂移、图和文两张皮。

## 本阶段产出

- `specs/modules.md`：12 张模块接口卡（职责/公开接口/依赖/seam/测试计划）+ 依赖无环全景图
- `docs/architecture-v4.html`：系统架构图 v4（archify，源 spec 同目录 .json）；**点节点弹自定义内容卡**（arch-notes.json）
- `docs/architecture-m1~m7-internal.html`：M1-M7 内部结构图（技术栈/部署/铁律三卡，底部折叠进「要点」弹窗）
- `docs/arch-notes.json` + `scripts/inject-arch-notes.mjs`：**本地增强层**（注入全部 architecture-*.html；重 deliver 后重跑即恢复），做三件事：① **节点内容弹窗**——点任意图节点，arch-notes.json 有内容（支持 ALIAS 别名，如内部图 `m2`→`case-backend`）就弹自定义内容卡（含内部结构图链接），没有就完全无反应（archify 内置"语义护照"被拦截；旧 `nodes/<id>.html` 详情页已废弃删除）。卡片样式（2026-09-07 二次调整）：**左上角无蒙层悬浮卡、按住标题栏可拖动**、✕/Esc 关闭——不用居中弹窗+蒙层（用户要求不挡图）② **三卡折叠**——底部 3 张要点卡收进工具栏「要点」按钮弹窗 ③ **版面增强**——隐藏 SVG 图例、导航条上顶、viewBox 裁死边距+内容缩小（DENSITY=1.3，绕内容中心）、svg pAR=none 画布最大化铺满窗口

## 关键决定

- **M4-M7 不独立成服务**：agent 服务内子模块（子图），无独立 HTTP 面，prompt 契约视同接口。
- **M2 纯叶子 + EventBus 走 SQLite outbox**：不上 Kafka（留 seam，换实现不换接口）。
- **M9 拆部署不拆卡**：TS 验票闸在 agent，铸币/凭证代理在 gateway，扫描在 guards；票面用 `fixtures/tickets/` 跨语言契约。
- **occurrences 计数**：用户提出，重复告警 occurrences+1 不新建（PRD 变更 4）。
- **架构图验收方式**：导览式逐节点过（已沉淀为 sdd-flow 的 references/guided-review.md）。

## 当前进度（跨会话续学读这里）

- 已带用户过完的节点：**告警源、M1 告警接入**（含回放载体、occurrences 讨论）、**M2 案件后端**（讲解 + 内部结构图 `docs/architecture-m2-internal.html`；讨论了 Fastify≈FastAPI、SQLite/better-sqlite3 三选型理由、M1=入口归一化/M2=档案室+台账的本质定位——已补进对应详情页）、**M3 supervisor 编排**（内部结构图 `architecture-m3-internal.html`；复核确认决策记录 #4/#12：LLM 超时统一 60s、token 预算 50k/run，已回写 PRD 正文与 M3 卡）、**M4 分诊**（内部结构图 `architecture-m4-internal.html` 交付，9 项校验全过；讨论了"子图/同进程室友/独立模块"三层归属；顺手修复详情页机制漂移——m2/m3 链接与正文段落回收入 `arch-notes.json` 真源，inject 脚本 LINKS 补齐 m2/m3/m4）
- 内部图默认策略修订：地基/枢纽模块（≥3 内部件或依赖起点）默认主动画，普通叶子点名才画（guided-review.md 第 5 条）
- **M5 调查**（用户点名补内部结构图 `architecture-m5-internal.html`，9 项校验全过；讲了多步工具循环与 M4 的本质区别：max_steps/防打转/上下文治理三条缰绳；图上把 mock SIEM+KB 合并为「工具后端」单节点，seam 细节留卡片）
- **M6 富化**（用户点名补内部结构图 `architecture-m6-internal.html`，一次过 9 项校验；讲了证据送检实验室定位、TLP/PAP 确定性闸门、一次性 microVM 与第四攻击面）
- **M7 知识沉淀**（用户点名补内部结构图 `architecture-m7-internal.html`；讲了闭环飞轮、人审为入库唯一通道 D8、REST 面挂 M2 的归属决策、replay 提速验证）
- **gateway 网关（2026-09-08 过完）**：内部结构图 `architecture-gateway-internal.html`（6 节点 6 边 1 区域 3 卡，showcase 过；三容器并排 = ContextForge RBAC 渠道面 / OpenFGA 三态裁决 / 自写小 FastAPI 铸币+凭证代理；agent 4 条出边按 RBAC·FGA·铸票·LLM 代理分工，验票闸明确画在 agent 本体不在网关）。讲解要点：铸币时刻①②落点、task_token/proxy 是路线 3 实测件参数化搬入（ADR 0001 拆件复用）、RBAC 管"看得见"与 FGA 管"用得了"分工
- **visual-check 与注入层打架（2026-09-08）**：archify visual-check 的 waitForStableLayout 采样器对注入版 HTML 永不收敛（"layout did not reach stable dimensions"，9 张全部如此；干净副本可过）——不影响真实浏览（无头 Chrome 截图、用户交互均正常）。核验姿势改为：对 /tmp 干净副本跑 visual-check 验几何 containment，对注入版用无头 Chrome 截图核版面
- **guards 防护微服务（2026-09-08 过完，用户点名要详细内部图）**：`architecture-guards-internal.html`（9 节点 9 边 1 区域 3 卡，showcase 过；双管道结构：llm-guard 扫描道=通道策略表+scanner+阈值标定，Presidio 脱敏道=RFC1918 识别器+映射表；外部 agent/M12 CLI/M11 Eval-CI）。讲解要点：四通道（间接注入主攻面）、fail-closed 由调用方兜底（可切「仅标记」降级）、脱敏只做出域边界（映射不进审计）、标定流程（毒种家族正例+善意负例标本集入 CI）。返回边只留必要——score+action 契约写进弹窗，防走廊拥堵
- **showcase 可读性红线（2026-09-08，guards 图踩坑）**：desktop-readability 检查要求节点 sublabel 投影 ≥6px；sublabel 字号有 9px 上限，viewBox 宽 >1379 时缩放 <0.674 必挂——修法是**缩短 sublabel / 加宽节点**，不是缩 viewBox 内容。另：节点中心未对齐会让短竖边出 10px 抖动段（micro-segment），对齐中心即直；route 走廊离 region 边框太近会判 border-run（留 ≥30px）
- **M8 上下游补边（2026-09-08，用户问"M8 上下游在哪"暴露主图欠账）**：v4 补 `m8-copilot→gateway「RBAC 可见性 + FGA 三态裁决」`（右侧 x990 走廊上行、gateway 底边进入，避开 m6 与 m3-to-gateway）；`web→M3` 标签改为「REST + SSE 全量消费（对话 /chat 走 M8）」点名对话入口。刻意不补：web→m8 独立边、m8→case 追问边（web/case/m3 间走廊被主管线边占满，showcase 无可路由路径——由 web→M3 标签+弹窗承载，记保留偏差）。m8-copilot 弹窗重写：上下游一段讲清（上游 Web 对话页 / 下游 guards→gateway→审批铸票 / 数据 M2 只读面）
- **进行中：chroma（Chroma 知识库——人审后的经验）**

- **M10 技术选型拍板：Ant Design 5（2026-09-08，过 web 节点时用户拍板）**：表格/抽屉/Tag/Steps 用 antd 开箱组件不自己撸；不改变「不引状态管理库」与六页面范围锁死。四处同步：PRD §M10 实现机制 + C7 行 + 变更记录第 6 条、modules.md M10 卡技术选型行、arch-notes web 弹窗
- **六个导览节点弹窗重写（2026-09-08）**：gateway/guards/chroma/sandbox/eval/web 统一为「干什么（业务）+上下游+技术选型（为什么选它）+刻意不做+汇报点」结构；chroma 弹窗写入混合检索暂不做的理由与触发条件（SOC 知识多精确标识符，库上规模后 FTS5+RRF 半天可加）
- **导览节点全部过完（2026-09-08）**：gateway→guards→chroma→sandbox→eval→web 六节点+M8 上下游补边完成；等用户「过审」后进票 18 收口（modules.md 机读化/PRD 归位/语义核心）→ to-tickets 拆票
- **用户「过审」（2026-09-08）→ 票 18 收口完成**：① **modules.md 机读化**——12 卡重构为闸门契约格式（`## 模块名` 单 token m1-m12 + `目录:` 行 + `### 依赖` 的 `- 模块:` 反引号边），原 H3 卡的全部内容保留进 公开接口/依赖/Seam 与测试/备注 四节；依赖边按"拆部署不拆卡"折叠（gateway/guards/chroma 的调用归 m9/m7 卡），m11 依赖 m1-m10，校验无环过。② **PRD 归位**——`deliverables/route5/product-handbook.md` 复制为 `docs/prd.md`（加 `状态: 已定稿` 行，gate 查 [待确认] 清零），原件顶部加指向说明不再更新。③ **CONTEXT.md 语义核心**——状态机 4 实体（alert/case/run/kbentry，枚举照 PRD §5）+ 不变量 INV-1..10（fail-closed/票据一次性/worker 无 L2 票/凭证不可见/KB 人审闸/occurrences/事件不丢不重/审计完整/验签不信文本/非法转移 409）。`tools/check_specs.py` PASS（6 警告=规划中模块目录未建，合法）
- **第 3 步拆票完成（2026-09-08，to-tickets）**：22 张票发布到 `.scratch/tickets/issues/NN-slug.md`（一票一文件，全部 ready-for-agent）。分四波：第 0 波无阻塞 5 张（01 CI 守门/02 票面契约/03 m2 数据地基/04 guards/05 m12 CLI）→ 第 1 波安全主链 4 张（06 铸币/07 验票闸/08 凭证代理/09 m1/10 m3 薄径/11 审批回路）→ 第 2 波 gateway 与 worker 5 张（12 三容器/13 m4/14 m5/15 m6/16 沙箱攻击面）→ 第 3 波 6 张（17 m7/18 m8/19 eval 骨架/20-21 Web 上下/22 eval 全维）。每张验收条目逐条注明源（模块卡测试计划/PRD FR/INV-x/决策 #）；Touches modules 全部单 token m1-m12，spec gate 校验过。粒度决策：m2 一张（确定性代码可一窗完成）、eval 拆 19/22 两张（跟流水线长）、16 沙箱攻击面独立、m12 与主线完全独立。拆分经用户确认后发布
- **学习节奏约定（2026-09-08，用户拍板）**：开发期用户不跟代码（票序是施工序非学习序）；**全部票完成后**按「六幕剧本（PRD §8）+ 一条告警的数据流」做走线导览——从 webhook 入口 curl 到 SSE 出结果，每段对着架构图弹窗+模块卡读对应代码。L0 义务：完工验收时产出走线导览（每幕 curl 什么、预期事件流、对应读哪段码）
- **待办：阶段 5 实现（L2 编码窗口）**——从 frontier 开工：01/02/03/04/05 五张无阻塞票

- **子图比例修正（2026-09-07，"字变瘦"根因）**：pAR=none 会把画布拉到窗口比例，而各子图内容天然比例不同（m6 裁剪后 4.3:1、m1/m5 2.8:1），比例差全变成文字横向压扁。修法（用户拍板：改图不改画布）：对 m1/m2/m4/m5/m6/m7 六张 spec 做垂直重排——节点位置按 sy 散开（m6 达 2.14 倍）、节点大小与文字不动，meta.viewBox 高度改为 vbW/2.15，使每张图画布比例统一到目标值；重交付后 pAR=none 的残余拉伸≈0，文字恢复原比例。m3/v4 比例本就≈2.15 不动。注入层 TARGET=2.15 归一化保留为兜底
- **主图结构性修订（2026-09-07，用户拍板）**：主图单节点「M3-M8 编排与 workers」拆开——M3 supervisor 与 M4/M5/M6/M7/M8 在大图上组成「agent 服务(LangGraph.js :3003 · M3-M8 同进程)」区域；supervisor 在顶向 workers 扇出（worker 之间刻意不连线，对应"worker 不直接对话"）；M8→guards「输入预检·注入扫描」、eval→M4「fixture 评测驱动」、M2→M4「读告警」、M7→M4「KB 检索注入」、web→M2「GET 只读查询」、新增**审批人（值班长）节点**→M3「L2 批复·resume」；node 详情页 agent-core 拆成 6 页（m3-supervisor/m4-triage/m5-investigation/m6-enrichment/m7-knowledge/m8-copilot）。showcase 9 项过。**已知保留偏差**：M5/M6 的 M2 数据边、M4 的 guards 扫描边主图不补（M2 子图已聚合为"M3/M4-M7"由 case↔M3 边代表；guards 共用由 M9 分组+子图权威）；「案件关闭触发」保留在 M7 tag
- **M3 子图重做（2026-09-07，主图拆分后的必然动作）**：旧版"③ workers 子图"单节点过时（M4-M7 主图已拆）——重写为 15 节点 16 边：runs 入口（POST /internal/runs→202）、events 事件总线（自增 id·Last-Event-ID 补发）、graph 主图（supervisor 调度·interrupt/resume）、**M4/M5/M6/M7 四个 worker 子图节点竖列**、验票闸 M9-S2（M4-M7 工具调用共用，聚合边 m7→gate）、checkpointer（信封 hash 链）、budget（三条缰绳）；外部 M2/gateway/审批人/M10 Web/外部工具。gateway 节点删除（graph 左端口拥塞无解；铸票/LLM S1 已在主图 m3→gateway 边与 M4/M5 子图呈现）。showcase 9 项过。经验：via 拐点必须落在端口正交方向上，端口号以校验器诊断为准，手算必错
- **子图 I/O 对账 + 遮挡排查（2026-09-07）**：7 张子图全渲染检查。修 2 处遮挡：①注入脚本 viewBox 裁剪 bug——密度放大原绕 viewBox 原点，cropY>0 时底部溢出被裁（M1 底排被切），改为绕内容中心放大 + 裁边计入 boundary pad（外扩 30/34）；②M2「只读查询直查」标签游离区域外，labelAt 钉回实际走廊 y226。M3-M7 无遮挡
- 待办：全节点过完，等用户「过审」→ 全过完用户说"过审" → 票 18 收口（modules.md 机读化 / PRD 归位 / 语义核心）→ 第 3 步 to-tickets 拆票

---

## Agent 产品加问对账（2026-09-07，sdd-flow 新清单附录五问）

> 结论：**五条全部有答案**——前四条早已落在 PRD/模块卡，第五条（输出校验兜底）逐 worker 也早已写在 PRD 各模块「异常与边界」节。无新增待确认项。

| 加问 | 答案位置 |
|---|---|
| 行动边界 | M9 验票闸 + `requires_approval` 参数级判定（PRD §6-M9-S2 / FR-S2.3）+ L0/L1/L2 分级（§12）+ M4 物理无 L2 票 |
| 人在回路 | 审批铸票 ApprovalToken + 值班长角色 + M3 审批 interrupt（§6-M3/M9） |
| 可追溯 | 五要素审计 FR-S5.1 + AuditEntry schema（§5.6）+ checkpointer 信封 hash 链（§6-M3） |
| 可审计 | 审计流页面（M10 六页面之一）+ SSE audit 事件（§6-M9-S5） |
| 输出校验兜底 | M4：不合 schema 重试 1 次 → `uncertain` + 人工待办；M5：重试 1 次 → 降级自由文本 + 标记；M6：TLP/PAP 超限 fail-closed 拒绝；M7：人审驳回即终态不重试；M8：命中注入拒答 + 置信度低澄清反问；M3：LLM 超时重试 1 次 → run 置 `Failed` |

## PRD 残留「待定：阶段 B 决策」对账（票 18 收口前逐条关闭）

> **更正（2026-09-07）**：此前把 6 条列为"待拍板"是读漏了——PRD 附录「待定项决策记录」里 **12 条全部已于 2026-09-04 拍板**（v1.0 冻结）。真正的问题只是**正文 12 处「待定」标记是滞后措辞**，与附录决策矛盾——正文像账本、附录像凭证，两处对不上。已于当日逐条回写正文（PRD 变更记录第 5 条），**无决策变更**；#4（LLM 超时统一 60s）与 #12（token 预算 50k/run）过 M3 节点时经用户复核确认。
>
> 教训：对账要读到附录表格正文，不能只看标题行。

| 决策记录 # | 内容 | 正文回写处 |
|---|---|---|
| #2 | `requires_approval` 首版固定规则表，不抽象 DSL | §6-M9-S2 表格 cell |
| #3 | Ticket TTL 统一 900s | §6-M9 票面注释（已进 CONTEXT.md） |
| #4 | LLM 超时统一 60s，留 per-node env 口子 | §6-M3 异常与边界 |
| #5 | `max_steps=20` | §6-M5 异常与边界 |
| #6 | KB 检索 `top-k=5` | §6-M7 FR-M7.3 |
| #7 | `LLM_MODEL=minimax-m2`，judge 先同款 | §6-M11 FR-M11.2 + §12 配置表 |
| #8 | Presidio 豁免 RFC1918 内网段 | §6-M9-S4 |
| #9 | SSE 事件自增 id 落盘 + `Last-Event-ID` 补发 | §6-M10 实现机制 |
| #10 | eval CI 快慢两道 | §6-M11 实现机制 |
| #11 | M12 llm-guard CLI 本地内嵌 | §6-M12 实现机制 |
| #12 | `MAX_TOKENS_PER_RUN` 默认 50k/run | §12 资源兜底配置表 |
- **子图节点弹窗全覆盖（2026-09-07，用户要求：业务讲清楚+技术选型讲清楚）**：arch-notes.json 从 16 条扩到 64 条，7 张子图全部节点（约 45 个）都有弹窗内容，外部实体节点走 ALIAS 复用主图同实体内容（webui→web、m1→ingest、m3/workers→m3-supervisor、human→approver、m4→m4-triage、kb→m7-knowledge、ticketgate→gate）。**顺带改了 3 个跨图同名不同义的节点 id**：m5 gate→ticketgate、tools→toolexec，m6 gate→tlpgate，m7 m4→retrievers（boundary wraps 同步改+重交付）——id 全局索引弹窗内容，同名必撞。经验：子图内部节点的 id 命名从一开始就要全局唯一
