# 0049 · 阶段 2（进行中）：模块划分图 + 架构图 v4 + 导览式验收

## 三问

- **终极目标**：票 18 技术设计——把 PRD 的 M1-M12 落成可施工的模块边界与接口，并让用户在图上掌控全局。
- **为什么这样做**：验收质量取决于用户真看懂，所以不丢长文档，改"导游式"逐节点过图。
- **解决什么麻烦**：长文档没人读、口头决议漂移、图和文两张皮。

## 本阶段产出

- `specs/modules.md`：12 张模块接口卡（职责/公开接口/依赖/seam/测试计划）+ 依赖无环全景图
- `docs/architecture-v4.html`：系统架构图（archify，源 spec 同目录 .json）；**点节点跳详情页** `docs/nodes/<id>.html`
- `docs/architecture-m1-internal.html`：M1 内部结构图（技术栈/部署/铁律三卡）
- `docs/arch-notes.json` + `scripts/inject-arch-notes.mjs`：节点注解机制（改注解改 JSON 重跑脚本；重 deliver 主图后必重跑）

## 关键决定

- **M4-M7 不独立成服务**：agent 服务内子模块（子图），无独立 HTTP 面，prompt 契约视同接口。
- **M2 纯叶子 + EventBus 走 SQLite outbox**：不上 Kafka（留 seam，换实现不换接口）。
- **M9 拆部署不拆卡**：TS 验票闸在 agent，铸币/凭证代理在 gateway，扫描在 guards；票面用 `fixtures/tickets/` 跨语言契约。
- **occurrences 计数**：用户提出，重复告警 occurrences+1 不新建（PRD 变更 4）。
- **架构图验收方式**：导览式逐节点过（已沉淀为 sdd-flow 的 references/guided-review.md）。

## 当前进度（跨会话续学读这里）

- 已带用户过完的节点：**告警源、M1 告警接入**（含回放载体、occurrences 讨论）、**M2 案件后端**（讲解 + 内部结构图 `docs/architecture-m2-internal.html`；讨论了 Fastify≈FastAPI、SQLite/better-sqlite3 三选型理由、M1=入口归一化/M2=档案室+台账的本质定位——已补进对应详情页）
- 内部图默认策略修订：地基/枢纽模块（≥3 内部件或依赖起点）默认主动画，普通叶子点名才画（guided-review.md 第 5 条）
- **进行中：M3 supervisor 编排**——内部结构图已交付 `docs/architecture-m3-internal.html`（9 项校验全过，agent-core 详情页已挂链接），讲解进行中
- 待办：M4-M7 workers → gateway → guards → chroma → sandbox → eval → web → 全过完用户说"过审" → 票 18 收口 → 第 3 步 to-tickets 拆票

---

## Agent 产品加问对账（2026-09-07，sdd-flow 新清单附录五问）

> 结论：**五条全部有答案**——前四条早已落在 PRD/模块卡，第五条（输出校验兜底）逐 worker 也早已写在 PRD 各模块「异常与边界」节。无新增待确认项。

| 加问 | 答案位置 |
|---|---|
| 行动边界 | M9 验票闸 + `requires_approval` 参数级判定（PRD §6-M9-S2 / FR-S2.3）+ L0/L1/L2 分级（§12）+ M4 物理无 L2 票 |
| 人在回路 | 审批铸票 ApprovalToken + 值班长角色 + M3 审批 interrupt（§6-M3/M9） |
| 可追溯 | 五要素审计 FR-S5.1 + AuditEntry schema（§5.6）+ checkpointer 信封 hash 链（§6-M3） |
| 可审计 | 审计流页面（M10 六页面之一）+ SSE audit 事件（§6-M9-S5） |
| 输出校验兜底 | M4：不合 schema 重试 1 次 → `uncertain` + 人工待办；M5：重试 1 次 → 降级自由文本 + 标记；M6：TLP/PAP 超限 fail-closed 拒绝；M7：人审驳回即终态不重试；M8：命中注入拒答 + 置信度低澄清反问；M3：LLM 超时重试 1 次 → run 置 `Failed` |

## PRD 残留「待定：阶段 B 决策」对账（票 18 收口前逐条关闭）

> **更正（2026-09-07）**：此前把 6 条列为"待拍板"是读漏了——PRD 附录「待定项决策记录」里 **12 条全部已于 2026-09-04 拍板**（v1.0 冻结）。真正的问题只是**正文 12 处「待定」标记是滞后措辞**，与附录决策矛盾——正文像账本、附录像凭证，两处对不上。已于当日逐条回写正文（PRD 变更记录第 5 条），**无决策变更**；#4（LLM 超时统一 60s）与 #12（token 预算 50k/run）过 M3 节点时经用户复核确认。
>
> 教训：对账要读到附录表格正文，不能只看标题行。

| 决策记录 # | 内容 | 正文回写处 |
|---|---|---|
| #2 | `requires_approval` 首版固定规则表，不抽象 DSL | §6-M9-S2 表格 cell |
| #3 | Ticket TTL 统一 900s | §6-M9 票面注释（已进 CONTEXT.md） |
| #4 | LLM 超时统一 60s，留 per-node env 口子 | §6-M3 异常与边界 |
| #5 | `max_steps=20` | §6-M5 异常与边界 |
| #6 | KB 检索 `top-k=5` | §6-M7 FR-M7.3 |
| #7 | `LLM_MODEL=minimax-m2`，judge 先同款 | §6-M11 FR-M11.2 + §12 配置表 |
| #8 | Presidio 豁免 RFC1918 内网段 | §6-M9-S4 |
| #9 | SSE 事件自增 id 落盘 + `Last-Event-ID` 补发 | §6-M10 实现机制 |
| #10 | eval CI 快慢两道 | §6-M11 实现机制 |
| #11 | M12 llm-guard CLI 本地内嵌 | §6-M12 实现机制 |
| #12 | `MAX_TOKENS_PER_RUN` 默认 50k/run | §12 资源兜底配置表 |
