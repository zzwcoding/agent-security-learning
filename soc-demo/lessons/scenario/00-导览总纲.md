# 场景导览总纲 · soc-demo 学习阶段

> 教学法：learn-by-rebuild 场景导览模式（skill 2026-09-09 新增节）。学透的定义 = 覆盖矩阵 100% + 关键链路亲手复现（含捣乱实验）+ 场景题闯关通过。
> 节奏：互动档，一次 1-2 步，用户说"下一步"推进。文章落 `lessons/scenario/<场景>-<步>.md`。
> 红线：学习阶段不改生产代码；发现 bug 记本文件"发现的问题"节转票，不顺手改。

## 场景清单与分步（8 场景 41 步）

### 场景 1：一条 TP 告警的一生（主干，6 步）
- [x] 1.1 布景与推单：起最小栈、replay 推告警进 webhook（ingest 入口/字段校验 422/severity 映射）
- [x] 1.2 去重与落库：M2 upsert（INV-6、occurrences、outbox 事件同事务）
- [x] 1.3 自动拉起与铸票：autorun 轮询 alert.created → POST /internal/runs（run-kinds 注册表、gateway /internal/mint 任务票）
- [x] 1.4 分诊六节点（上）：LangGraph 执行器、load_alert、kb_check（chroma）、merge_check（verdict 锁）
- [x] 1.5 分诊六节点（下）：self_audit、verdict_llm（LLM 经凭证代理出站 fake/real）、outcome（建案/关闭建议）；wrapUntrusted 处 guards 注入扫描
- [x] 1.6 收官透视：TP 建案链上 case_flow、SSE 事件流对账、web 审计流页看全程

### 场景 2：调查与富化（5 步）
- [x] 2.1 case_flow 拉起与调查子图入口（investigate_case、related_alerts）
- [x] 2.2 工具循环与三条缰绳（siem_query time_window 契约、防打转、上下文治理/spill）
- [x] 2.3 调查报告（findings 引用真实工具输出、只提建议不动手、零 L2）
- [x] 2.4 富化：TLP/PAP 确定性闸门、analyzer fixture 表、artifacts 去重回写
- [x] 2.5 timeline 装配与 web 呈现（两种报告条目、字段透视）

### 场景 3：知识沉淀与再利用（5 步）
- [x] 3.1 关案触发：case.closed → 提炼子图（ASP 门控、LLM 草稿）
- [x] 3.2 KB 账面：proposed 草稿、kb/proposals REST
- [x] 3.3 人审闸：kb_write L2 → interrupt → 批准 approved 进 chroma / 驳回检索面查无
- [x] 3.4 检索面：MemoryKb vs ChromaKb、top-k、INV-5
- [x] 3.5 反哺闭环：同类告警再推 → 命中知识 → btp 快速关单

### 场景 4：一次高危动作的审批之旅（7 步，安全卖点）
- [x] 4.1 L2 建议从哪来（调查报告建议 / chat 意图）
- [x] 4.2 意图闸三态（OpenFGA 四角色四族、soc1 100% deny）
- [x] 4.3 interrupt 挂起与审批卡（awaiting_approval、SSE approval_required、信封链盖印）
- [x] 4.4 值班长批准：gateway /internal/mint 铸 ApprovalToken（HMAC 三段、TTL 300s、params_hash）
- [x] 4.5 resume 与验票：verify-ticket 六 reason、跨进程重放 403（used_tokens 读口）
- [x] 4.6 焚毁与一次性：executed_at、used_tokens、审计链
- [x] 4.7 保质期与队列：APPROVAL_TTL 过期作废、run_jobs 秒回与消费循环（异步化形态）

### 场景 5：攻击者来了（5 步）
- [x] 5.1 告警注入：alert_field 四植入位、guards llm-guard 拦截、verdict 不被带偏
- [x] 5.2 chat 注入拒答：user_input 通道、DENIED 审计
- [x] 5.3 毒 KB：RAG 投毒提案、人审驳回、检索面查无（INV-5/9）
- [x] 5.4 毒 analyzer：microsandbox egress/env/一次性（能力探测）
- [x] 5.5 凭证金丝雀：SECRETS 只活在网关与出站瞬间、全可观测面 grep（INV-4）

### 场景 6：PII 脱敏与反查（4 步）
- [x] 6.1 Presidio 管道：识别器（含中文自定义）、RFC1918 豁免、占位符替换
- [x] 6.2 mapstore 落盘：占位符→原文、重启不丢
- [x] 6.3 受控反查链路：角色白名单（duty_lead/admin）、soc1 403、每查必审计、原文不进可观测面
- [x] 6.4 web 反查交互与敏感面口径

### 场景 7：新增一个工具（4 步）
- [x] 7.1 脚手架：gen:tool 生成空壳工具
- [x] 7.2 登记：manifest 行、三方一致性（manifest ≡ TOOLS ≡ A.1）
- [x] 7.3 闸的生效与拒绝：登记后过闸、不登记 403 no_ticket、INV-3 无 L2
- [x] 7.4 边界闸与体检：check_boundary、四对账、"测试零删除"纪律

### 场景 8：系统自证（5 步，收官）
- [x] 8.1 evals：33 用例、确定性断言器、judge 不进门禁
- [x] 8.2 审计流与 Eval 页：web 呈现三维报告
- [x] 8.3 MCP 体检：给陌生 server 做 audit（官方 SDK 双 transport）
- [x] 8.4 compose 从零起栈：九服务、digest 钉、.env 口径、可选 profiles（observability/real-wazuh）
- [x] 8.5 CI 等价命令与本地三闸（spec gate/boundary/lint+test）

## 覆盖矩阵（场景 × 代码/中间件/配置）

| 组件（自研▼/开源▲） | S1 | S2 | S3 | S4 | S5 | S6 | S7 | S8 |
|---|---|---|---|---|---|---|---|---|
| ▲Fastify（ingest/case-backend/agent） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| ▲FastAPI（guards/gateway） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ |
| ▲better-sqlite3（SQLite 三库） | ✓ | | ✓ | ✓ | | ✓ | | ✓ |
| ▲LangGraph.js（StateGraph/interrupt/Command/checkpointer） | ✓ | ✓ | ✓ | ✓ | | | | |
| ▲@modelcontextprotocol/sdk | | | | | | | | ✓ |
| ▲llm-guard + Presidio（+spacy sm） | ✓ | | ✓ | | ✓ | ✓ | | ✓ |
| ▲microsandbox（msb 一次性 microVM） | | | | | ✓ | | | |
| ▲OpenFGA（官方镜像 + fga_check 插件） | | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ |
| ▲ContextForge（官方镜像） | | | | ✓ | | | | ✓ |
| ▲chroma（官方镜像） | ✓ | | ✓ | | | | | |
| ▲Vite/React/antd5（web） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ |
| ▲minimax-m2（经凭证代理，fake/real） | ✓ | ✓ | ✓ | ✓ | | | ✓ | ✓ |
| ▲microsandbox CLI/能力探测 | | | | | ✓ | | | |
| ▼ingest（wazuh.ts 映射/去重/m2client） | ✓ | | | | ✓ | | | ✓ |
| ▼case-backend（store/statemachine/kb/outbox/audit 写口/tasks） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ |
| ▼agent 编排（run-kinds/dispatcher/autorun/checkpointer/events/budget/audit） | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ | ✓ |
| ▼workers×5（triage/investigation/enrichment/knowledge/chat） | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ | |
| ▼verify-ticket + gated-call + tools-manifest | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ | |
| ▼token-ports（mint/burn/usedReader） | ✓ | | ✓ | ✓ | | | | |
| ▼gateway 自写三件（mint/proxy/fga 插件） | ✓ | | ✓ | ✓ | ✓ | | ✓ | |
| ▼web src（六页/sse 重连/close/reveal/eval 视图） | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | | ✓ |
| ▼evals（loader/assertions/runner/judge/rigs） | | ✓ | ✓ | ✓ | ✓ | | | ✓ |
| ▼scripts（replay/setup-openfga/冒烟脚本群/gen-tool） | ✓ | | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 配置：docker-compose + Dockerfile 群 + .env.example | ✓ | | | ✓ | | ✓ | | ✓ |
| 配置：CI（父仓四 job + eval-slow）+ check_specs + check_boundary | | | | | | | ✓ | ✓ |
| 配置：fixtures 契约群（tickets/sse-events/verdicts/guards/corpus/eval-report/tools.manifest） | ✓ | | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

## 文章模板（六节，详见 skill 场景导览模式节）

**风格铁律（先读）**：读者是信息化小白——通篇大白话，每个技术词首次出现配比喻，**比喻优先取自我们自己的 SOC 业务**（告警=报案电话、分诊=预检分诊台、任务票=保安队长的手令、焚毁=一次性密令用完即毁），泛生活比喻只在业务内找不到对应时才用；例子直接用本项目的真实数据（真告警、真 run_id）；每段写完自检"读者能用自己的话复述吗"，不能就重写。

操作（含位置感）→ 调用链（**ASCII 全景图后按"站"逐站带真实代码走**：每站 = 大白话叙述 → 代码/配置原文逐字引用（标文件+行号范围，仓库根相对路径 `soc-demo/…#L行号` 可跳转链接，行号实测核对，注解写块外不混进引用块）→ 繁琐细则转标注"伪代码"的块并给真实行号指针；样板 = `1-1.md`）→ 服务与中间件卡（≤2 张：自研/开源、配置、市面替代、为何选、业务上怎么选）→ 防线对号（INV/契约）→ 亲手验证（必选，命令+预期特征）→ 捣乱实验（必选）。

## 进度与学习日志

- 进度：**41/41 全部完成（八场景全满）+ 问题票 #52-#56 全部修复 done（2026-09-10：52 ruff双尺收口 c588894 / 55 vite proxy 走线 f8f592d / 56 RFC1918 豁免上移 cf7e4ab / 53 digest 总闸 3bdf88a / 54 accuracy 超时 3dedd05；串行派发子 agent 修复、主窗口逐票验收提交；web+guards 容器已重建吃进修复）**；**剩余待办（归用户）**：①场景 1-8 闯关题（各 3 道）作答，答错点记学习日志当前情补丁；②`git push` 推 workflows 上远端让 CI 真执勤（票 52 交付的本地就绪态）。**收官布景**：九服务全 healthy、data/ 零损失、agent/web/guards 容器已重建（25 行 manifest、pii proxy、票 56 修复上线）、eval-results/latest.json 为默认态（33/33，judge=not_evaluable）；测试基线：agent 494+3 / web 110 / guards 28 / gateway 43，全仓零删除。
  - 场景 4 收尾状态（2026-09-10）：七篇 4-1..4-7 全部提交（73d3660→本票）+ 新教具 `scripts/4-5-verify-demo.mts`。**主角审批链全程闭环**：case_000002 调查报告建议 isolate_host（timeline 1d982c59，零审批卡=建议/执行鸿沟实证）→ chat 意图开卡 apr_baf3f3a8（params_hash sha256:7f36978b…）→ 值班长批准铸 token ap_4a9d6e0d（九 claims/TTL 300s）→ resume 验票执行（mock_edr isolated）→ 焚毁+盖章+审计四连（create→approve(duty_lead)→execute→execute(used_token)），run_6ae4b2bf completed steps=6。**断链教具（永久留档）**：run_73353b26 信封链第 2 环被篡改 → 批准其卡 apr_73d3c6bd 后 resume 拒恢复（audit resume FAILURE checkpoint_tampered），run 永挂 awaiting_approval，token ap_d00d9edd 铸了没花且已过 300s。**TTL 教具**：apr_0d4cc044/run_0525627d（回拨 created_at 实验）→ 卡 expired、run failed(approval_expired)，expire 审计 actor=m3:dispatcher ttl_seconds=86400，两步迁移各留审计。**意图闸三态教具 run 群**：run_f7104aeb（soc1 只读 allow）/run_4facef5a+run_8bf9d02c+run_7c33e17b（deny 三态样本）/run_192a4446（FGA 停机 fail-closed）。**布景**：openfga/gateway/case-backend/agent 各 stop-start 至少一轮全部恢复 healthy（FGA 世界重建过、store/model id 已刷新）；审批卡台账共 6 张（场景 3 遗留 2 张 kb_write + 场景 4 教具 4 张 isolate/block：approved×3、rejected×0、expired×1，pending 0）；used_tokens 3 行；chat_flow run 累计 +10。**场景 5 接口**：guards user_input 通道已在 4.1（input_guard 拒答+DENIED 审计）和 4.2（意图闸对 role 字符串 fail-closed）预演；chat 注入拒答=5.2 主场。
  - 场景 3 收尾状态（2026-09-10）：五篇 3-1..3-5 全部提交（fde1f56→本票）+ 新教具 `scripts/3-4-kb-demo.mts`。**知识库三终局对照组**（账面 3 条）：kb_11d09bb0 approved+chroma 在（正门主角，env_fact centos7 演练）/kb_1f5f4196 approved 幽灵+chroma 查无（直连批准教具）/kb_99aa3d0c rejected（审批卡驳回教具）；chroma kb_entries count=1。**反哺环实证**：新告警 ed6e1eea（sourceRef 1682439999.7001，webhook 201）→ alert_flow run_b5198a76 命中 env_fact→R1 btp/rationale 逐字引用 KB 标题/close 建议→SOC1 close_flow run_66e2b653 关单 Closed+close_confirm(soc1) 审计。教具 run 另有：捣乱直拉 run_93d5e558(skip)/run_2d50d20c(not_found failed)、第二次沉淀 run_ab6f7a18（驳回演示）、close_flow 拦截 run_56ecece4(短 id not_found)/run_2f45b13e(close_advice_missing)。布景变化：**case_000004 已 Closed（不可重开）**、告警 16 条（+ed6e1eea）、outbox 游标 17、审批卡全决（无 pending）。
  - 场景 3 步 3.3 布景状态（2026-09-10）：**主角草稿 kb_11d09bb0 已正门入库**（审批卡 apr_0f984f53 批准 approver=duty_lead→token ap_3cac74f3 铸/验/焚 used_tokens 落账→chroma count 0→1；run_0d96d113 completed steps=2；M2 审计五连 create→approve(duty_lead 真人)→approve(kb_entry 镜像)→execute）。**驳回教具**：直拉第二次沉淀 run_ab6f7a18→新卡 apr_b52adedd 驳回→kb_99aa3d0c rejected（reject_reason=审批卡驳回）、chroma 仍 1、run completed（事件 642-643 重进即出无 tool_call）。**账面三终局对照组齐**：approved+在 chroma（正门）/approved 幽灵+不在（绕门 kb_1f5f4196）/rejected+不在（驳回）。重复批准 409 实测。**3.4 接口**：kb_check 查询文本=[host,path,user] 拼接；ChromaKb fail-open（检索 0 命中/不可达=降级等价无 KB）；top-k=5；3.5 反哺用 env_fact 命中 R1→btp+close（replay 同类 centos7 告警即可）。
  - 场景 3 步 3.1 布景状态（2026-09-10）：**case_000004 已关案且不可重开**（case 状态机 Closed:[]；verdict=benign_true_positive，note=授权红队演练 DX-2026-09，由 New→InProgress→Closed 两步推转）。主 run `run_0d96d113-9e49-466c-bb7f-0704ca82db25` 停在 **awaiting_approval**（steps=1/tokens=96，事件 602-610：get_case→kb_propose 过闸→kb_write 开卡 approval_required）；审批卡 `apr_0f984f53`（kb_write，params 带草稿全文+params_hash sha256:1205a41a…）。M2 kb 账面 1 条 proposed：`kb_11d09bb0-57d9-442d-8487-129f308970c7`（env_fact，source=case_000004，proposed_by=agent:knowledge）；**chroma kb_entries count=0**（INV-5 实证）。outbox 游标 m2_outbox=16（event16=case.closed）。捣乱 run：run_93d5e558（case_000002 未关案直拉→completed skip，steps=2/tokens=0，knowledge_skip 审计 reason=no_verdict）、run_2d50d20c（case_999999→failed node_error:knowledge_distill，steps=0）。**3.2/3.3 接口**：审批卡待裁决（批准→M2 approve 账面+chroma upsert count 0→1；驳回→rejected 账面、检索面永不写入）；kbentry 终态再裁 409 可演示；3.5 反哺用 env_fact 命中分诊 R1（FakeTriageLlm llm.ts:71 known_change/env_fact→btp+close）。
  - 场景 2 收尾状态（2026-09-10）：五篇 2-1..2-5 全部提交（da54ff5→本票），新教具 `lessons/scenario/scripts/`（2-2 四缰绳演示、2-3 说谎报告演示）。布景：4 案 15 告警；case_000001 时间线 5 条、case_000002 时间线 10 条（含 bogus_kind/mystery_kind/两条捣乱 note，body 自带"捣乱"字样）、case_000003 时间线 7 条（两对直拉报告；artifact invoice_apr.zip dedup 是→内部实体跳过 11→12 级联）、case_000004 未动。教具 run：直拉 run_51882454/9cfc29da、guards A/B 对比 run_572f5bec(1 flag)/run_ce0559ef(0 flag)、捣乱 failed run_2176c481。发现的问题：tool_output 扫描 fail-open 缺口（已录转票）。
  - 2026-09-10 步 2.4 完成：文章 `2-4.md`（无 LLM 确定性管线/类型白名单/签名-亮票-密级闸分层/tlpPapGate 纯函数/FixtureAnalyzerTable+投毒 TI 案底/artifacts from: 溯源+M2 端 dedup 并 tags/对账式报告可 grep 标记；卡=Cortex 契约子集+TLP/PAP 选型、fixture 目录当情报库）。**布景金矿**：case_000003 的 hash 天然带 tlp=2/tlp=4 双行（两批告警不同 TLP 源）→ 同一值一放行（VT 5/70）一拒发（tlp_exceeded 逐字进报告+M2 DENIED 审计）；dedup 两连拍实测（invoice_apr.zip 否，新建→是，并 tags；内部实体跳过 11→12 级联）。布景：case_000003 时间线 7 条（2.1 直拉 1 对+本步补拉 1 对报告+system+调查链上 1 对）。
  - 2026-09-10 步 2.3 完成：文章 `2-3.md`（输出契约/parseReport 显式验型+source_tool∈executed 证据复核/重试一次后降级三重污点/renderReportMarkdown 双形态/write_timeline 亮票落库；卡=结构化输出把关+双形态 TheHive 先例）。**新教具**：`scripts/2-3-report-demo.mts`（S1 两次谎 web_search→llm_retry+report_degraded+structured null；S2 改口一次→重试通过非降级）。**活教材**：case_000002（sev3）报告建议 isolate_host 而 approvals=[]——建议/执行鸿沟实证；其 siem evidence 逐字含第三条注入样本（"ignore all previous instructions…"）。**捣乱 B 实测**：M2 REST timeline 写口 kind=bogus_kind 照收 201——kind 契约只在 agent 工具闸（TIMELINE_KINDS），内网信任口不设防（防线分层教学点，已在 2-3.md 记备忘）。布景：case_000002 时间线 7 条（3 报告+富化×2+2 条捣乱 note/bogus，body 自带"捣乱"字样）。
  - 2026-09-10 步 2.5 完成：文章 `2-5.md`（listTimeline 排序双保险/assembleTimeline 两路合流+不猜时间/KIND_META 未知 kind 透传/levelsOf 字段透视/<pre> 防 XSS+评级配色；卡=视图装配纯函数层+antd Timeline/Tag；捣乱 A mystery_kind 全链路实测设计+B 三道门物理化"不猜时间"）；文末附**场景 2 闯关三题**（三种失败命运/说谎报告/重复直拉幂等，带 details 答案）。web timeline.test.ts 4/4 实测过。
  - 2026-09-10 步 2.2 完成：文章 `2-2.md`（tool_loop 每圈五道关/max_steps 截断语义/防打转指纹/checkTimeWindow 契约/闸拒 vs 工具报错分岔/observe 三道门扫描-摘要-落盘/FixtureSiem 双口径+换件只换 adapter；卡=LLM 裁决+代码执行范式+上下文治理策略）。**新教具**：注入式演示脚本 `lessons/scenario/scripts/2-2-rein-demo.mts`（四缰绳全实测：S1 防打转 SIEM 只执行 1 次、S2 三违约 SIEM 执行 0 次、S3 12k 摘要/58k 落盘 hits_ref 58147B、S4 max_steps=2 截断 incomplete=true）。**发现 tool_output 扫描 fail-open 缺口**（guards 停机 A/B 实测：同案在线 1 flag/停机 0 flag 且无降级痕迹，run 照常 completed）→ 已录"发现的问题"转票。布景：guards 已恢复；case_000002 增 2 条 case_flow run（A/B 对比教具）、时间线 5 条。
  - 2026-09-10 步 2.1 完成：文章 `2-1.md`（双入口汇合 caseIdOf/铸票绑案/交接态/外壳交接节点+子状态隔离/load_case 锚点/plan/related_alerts 五关全路径；卡=交接节点编排形态+数据面拉回过滤 vs 下推）。**实测**：直拉 run_51882454(case_000001) completed/steps=2/tokens=192=6×32；复算 run_9cfc29da(case_000003) related_alerts 命中 11=12−1（2026 捣乱告警在窗外）；捣乱 A case_999999 202→failed(node_error,steps=0,kill 审计只在 M2)、B 400 case_id_required、C 重复直拉时间线再长一对（幂等不在链层）。**新布景教具**：case_000001 时间线 5 条（2 份调查+2 份富化报告）、case_000003 时间线 5 条、siem 语料注入样本被 flag(score 1)（fim 案与 web 案均现，2.2/场景 5 素材）。布景修正：告警实为 **15** 条（前记 17 有误：12 web-01+app-01+db-01+centos7），4 案不变。
  - 场景 1 收尾状态（2026-09-10）：六篇 1-1..1-6 全部提交（ca2324f→本票）。布景：15 事件全消费、17 告警（含 4 条捣乱新增）、4 案、cursor=15；agent=AGENT_LLM=fake、chroma 已恢复、gateway 已恢复。教具 run：主角 run_e7db506a（merge 分支）、建案型 run_0e4cddbb（fim-510 db-01，41 事件含链上调查）、failed(mint_failed) 一条、triage_skip 一条。闯关答案回收后记学习日志，答错点即场景 2 开讲的前情补丁。
  - 2026-09-10 步 1.5 完成：文章 `1-5.md`（self_audit 声明/wrapUntrusted+guards 三裁决/prompt 契约/fake 规则引擎 vs real 凭证代理/self_audit 对质/outcome 三结局；卡=minimax-m2+凭证代理）。**降级链实测**：AGENT_LLM=real 无 key → gateway 503 deny_no_secret → retry → uncertain/0/InProgress/tokens 0（已还原 fake）。布景 +1 告警（`1682432014.3112`，alert f42a4e72，uncertain InProgress）；游标 15。
  - 2026-09-10 步 1.4 完成：文章 `1-4.md`（执行器壳/信封链/makeGatedCall/verdict 锁/chroma 检索面；卡=LangGraph.js+chroma；捣乱 A 重拉已判定告警看锁、B 停 chroma 看 fail-open，全部实测）。**布景教训**：chroma 宿主端口是 18000（8000 被本机占用）；skipped run 步数照记（壳在节点出口记账）。布景现状：另有一主角重拉 run（7fef8881，completed+triage_skip）、捣乱 B 新告警 `6f1337e1-…`（web-31103 换源Ref，已并案）。
  - 2026-09-10 步 1.3 完成：文章 `1-3.md`（autorun 游标/裁决/正门 202/run_jobs 队列/gateway HMAC 签票；卡=HMAC 票型+run-kinds 注册表；捣乱 A/B/C 实测含 stop gateway → mint_failed）。**布景事件**：发现 agent 容器是旧镜像（早于票 40/44/47，缺 autorun/run_jobs）——已 `docker compose up -d --build` 全量重建对齐 HEAD，非代码 bug，未开票。布景现状：13 告警 → 13 个 alert_flow 全 completed，游标 13，gateway 13 次 mint 全 200；主角 run `run_e7db506a-9dcb-4f40-aa7c-d05b8aa83d02`（steps 8/tokens 64）；捣乱 C 留一条 failed(mint_failed) run 作教具。
  - 2026-09-10 步 1.1 完成：栈已起（data/ 已清空重建）、FGA 已重建、12 告警已入两层（replay 两遍 = 201×12 → 200 dedup×12）。主角告警 alert_id=`4e11d030-2339-48f3-98c7-f1a0a8bb100e`（sourceRef `1682431744.9001`）。文章 `1-1.md`；已提交 ca2324f。
  - 2026-09-10 步 1.2 完成：模式改为**主窗口直写**（用户确认比串行派发省 token）。文章 `1-2.md`：upsert/唯一索引/同事务三表/outbox；验证 replay 第三遍 occurrences 2→3、事件恒 12、audit diff 三条、捣乱 A/B/C 实测。布景现状：主角告警 occurrences=4（捣乱 A 又 +1）、多一条捣乱告警 `1682431744.9999`（alert_id `e7c26e93-…`）、事件 13 条。
- 学习日志：
  - 2026-09-11 场景 0 全景导览 0-0.md 就位（票 60 任务 0，复盘期补地图）。
  - 2026-09-10 票 54 修复（accuracy.test.ts 横切断言全量并发下偶发超时，8.5 收官三连红两次那条）：按票内推荐修法落地——`workers/triage/accuracy.test.ts` 横切断言大用例显式放大 testTimeout 5s→**15s**（test 级第三参数 `}, 15_000)`，写法同 run-dispatcher.test.ts 先例；vitest 3.2.7），注释交代为何（一个 test 里串行 triageOne 全部 11 条标注案，每案起真 case-backend 子进程 + guards 通道扫描 + 生产 HttpTriageM2，单跑实测 3.9-4.8s，默认 5s 无余量、全量并发偶发超时翻红）。**保留真管道横切守门语义**（不换 fakeScan、不动 maxWorkers），既有断言零删除零改动（纯参数，净 +6 行注释/1 行超时）。**验证**：单跑 14/14 绿（横切断言 3975ms）；全量三连跑 494 passed + 3 skipped 全绿（12.86s/12.49s/12.57s）——修的就是并发全量下的 flaky，三连全绿即核心验收；0 新增用例，基线只增不减达成。
  - 2026-09-10 票 53 修复（默认栈三枚官方镜像 digest 钉无执法者，8.4 捣乱 B 那条）：`compose-topology.test.ts` **纯追加"封条总闸"**——凡 `image:` 行必以 `@sha256:<64hex>` 收尾（全量扫描 compose 文本逐行编号，含 profiles 内服务；缺钉失败信息点名 `docker-compose.yml:<行号>\t<原行>`），新加服务写 `image:` 即自动被覆盖；票 37/38 逐枚断言一条未删（它们还锁镜像名+"不 build"，与总闸两层互补），另配"扫描非空"护栏防 compose 清空时总闸 vacuous pass。**自证**：改文本摘 chroma 钉→当场红点名 :124（同跑旧断言与 compose config 语义断言全过=捣乱 B 的"无人红"窗口被复现后由总闸单独闭合）→ `git checkout` 还原绿 18/18；agent 包 492→494+3skipped 只增不减；`docker-compose.yml` 本体零改动。8-4.md 文末备注（照 2-2 票 50 先例，正文零改动）。备查：全量跑中 triage accuracy"注入变体"用例出现过一次 5000ms 超时（单跑 14/14 绿、相邻两轮全量均绿，常态 ~4.8s 贴线）属既有 flaky，与本票无涉。
  - 2026-09-10 票 56 修复（192.168 段内网 IP 被误标 `<PHONE_NUMBER>`，6.1 捣乱 B 那条）：①豁免上移清单层——`services/guards/pii.py` 的 anonymize 在 `_drop_overlaps` 之后、mapstore record 之前对**最终实体清单**再过一遍 RFC1918（`RFC1918.match(text[span])` 锚定全文匹配：不问 entity_type，谁抢到内网 IP 的座都作废；公网 `110.0.0.5` 前缀陷阱不吃无锚点误伤；插在 record 之前=内网原文结构性无从入账）；识别器注册结构零改动（自家豁免留作第一道，清单层兜底第二道）；②新增 2 用例——三段+公网四用例一段文（10.20.30.40 / 172.31.99.5 / 192.168.1.100 全存活 + 198.51.100.7 照脱；既有用例的 `192.168.1.1` 短尾恰好躲开 3-3-4 电话形态，长尾 `192.168.1.100` 才补上盲区）+ mapstore 五占位符逐个查账（真手机对照组照收、无任何 `192.168.` 原文入账）；③如实交底：guards 容器只 bind mount `/app/data`、源码烤镜像，live 口径做不了，复验按预案降为本地函数级（探针修前 `192.168.1.100 → <PHONE_NUMBER>` 复现 → 修后三段全存活、公网照脱；生产 mapstore 只读核对仍 11 行、教具行 `' 192.168.1.100'` 未动），容器待 `docker compose build guards && up -d guards` 一次生效（主窗口交接）。guards 全量 26 → 28 只增不减、ruff 绿；6-1.md 文末备注（照 2-2 票 50 先例，正文零改动）。
  - 2026-09-10 票 55 修复（vite proxy 走线缺口，web 反查按钮 ：5173 必 404 那条）：①`services/web/vite.config.ts` proxy 表补 `"/api/v1/pii": { target: agent, changeOrigin: true }`（分叉规则注释同步）；②新增静态对账单测 `src/vite-proxy.test.ts`——读 proxy 表前缀集合 ⊇ src 运行时带引号发起的 `/api/v1/*` 前缀集合（剔除测试替身，注释不入集），摘行当场红点名 `[ 'pii' ]`、还原即绿；③端到端冒烟 200/403（duty_lead 拿回原文 / soc1 `pii_reveal_forbidden`，vite 自身 404 是空 body，JSON 响应即穿透实证）。**如实交底**：:5173 容器是镜像烤源码无 bind mount，restart 吃不到新配置，冒烟按预案用同配置临时 dev server :5174 等价完成、用完即拆，容器 ：5173 待 `docker compose build web && up -d web` 一次生效（主窗口交接）。web 全量 108 → 110 只增不减；6-4.md 文末备注（照 2-2 票 50 先例，正文零改动）。
  - 2026-09-10 票 52 修复（ruff 门禁双尺回绿 + 等价清单补漏抄）：guards 5 处 I001（app.py:13/conftest.py:7/pii.py:12/test_guards_contract.py:8/test_pii_mapstore.py:12，`ruff --fix` 自动排序 import 区块；动手前核对 pii_store 仅 os/sqlite3/time + DDL 常量、无 import 副作用，排序语义安全）+ gateway/test_proxy.py:192,195 两处 E741（推导变量 `l` 手改 `line` 含用点）。**验收口径=两代尺子各自全绿**：ruff 0.16.6（钉定版）与 0.6.9（`uv tool run ruff==0.6.9`）对 `services/guards services/gateway` 各自 `All checks passed!` exit 0；pytest 复跑 guards 26 + gateway 43 全绿，import/变量名改动零行为变更。`services/guards/requirements-dev.txt` 浮动钉 `ruff>=0.6` 改精确钉 `ruff==0.16.6`（0.16 起 isort 进默认规则集是这次翻红的根因，精确钉即冻结规则集，故未再加 ruff.toml 第二重配置面）。ruff 步补进 8-5.md 文末备注（照 2-2 票 50 备注先例：引用块引原文，正文零改动）。推 workflows 上远端执勤留给用户。
  - 2026-09-10 步 8.5 完成（**全教程 41/41 收官**）：文章 `8-5.md` + 闯关三题（judge 双保险方案踩线/契约 fixture 加字段流程/"谁执法·上一次红是什么时候"三问）+ 八场景收官寄语。**CI 四 job 本地等价全跑**：job1 spec gate PASS（0 警告）；job2 boundary PASS（0 越界 9/9 + self-test 18/18）+ lint/typecheck exit 0 + pnpm test 前两跑各 1 红（accuracy 偶发超时 P3 复现，单跑绿）第三跑全绿；job3 pytest guards 26 + gateway 43 绿、**ruff 半步红（新 P2，两代尺子各红各的）**；job4 `docker compose -f soc-demo/docker-compose.yml config -q` exit 0。**慢道等价**：pnpm test:eval 33 ran/33 passed 收官签名。**包账勘误**：7.4 交班注记包名错位，正确=agent 492+3 / ingest 41+1sk / case-backend 60 / web 108 / mcp-audit 14 / evals 99（33 用例+66 框架单测）/ guards 26 / gateway 43，合计 883 passed + 4 skipped。**交底大发现（P2）**：origin/main 17 文件旧 stub、workflows 未推送、CI 从未远端执勤——门禁真身是本地等价命令。**三闸喂脏**：A boundary R1 点名到行号红→删→绿；B CONTEXT.md 重复 INV-5 → "不变量 ID `INV-5` 重复声明" FAIL→还原绿；C 坏 override → compose config exit 1（真实退出码要绕开管道陷阱）。**S8 覆盖矩阵逐行核对**：✓ 行全部成立（Fastify=8.1 快道真 case-backend；FastAPI=judge 出站走 gateway+job3 pytest；better-sqlite3=8.2/8.4 容器只读核查；@mcp/sdk=8.3；llm-guard+Presidio=8.1 攻击取证+8.4 compose；OpenFGA/ContextForge=8.4 compose+8.3 体检捣乱；Vite=8.2 EvalPage；minimax=8.1 被测/裁判分离；ingest=8.1 replay 域；case-backend/agent 编排/web src/evals/scripts/compose/CI/fixtures 契约=各自主场步）；空格行核对维持：LangGraph（8.1 整体被测未拆节点，主场 S1/S2）、chroma（8.2 count 复点+8.4 compose，检索面主场 S3）、workers×5（行为被覆盖但未按 worker 拆）、verify-ticket/tools-manifest（8.4 tierOf 运行面探针触及，判定主场 S4/S7）、token-ports（S4 主场）、microsandbox 两行（S5 主场）。**八场景全景一句话**：S1 一条告警的一生（每站可指认）/S2 调查富化三条缰绳/S3 知识三终局对照组/S4 审批卡生命周期/ S5 攻防四植入位+金丝雀/S6 PII 户口两处/S7 登记先于代码三层能力面/S8 自证三件套（评估/契约/门禁）。**收官待办**：闯关答案回收（场景 1-8 各 3 题）、问题票统一开票（P2 ruff+CI 执勤、P2 vite proxy 走线、P3 accuracy 超时、P3 192.168 误标、P3 默认栈钉断言缺口；#50/#51 已修留档）。
  - 2026-09-10 步 8.4 完成：文章 `8-4.md`。**compose 纸面装配全实测**：config -q 过；九服务名册（compose-topology.test.ts:124-129 机器断言）；profiles 演示——observability 11 服务（9+langfuse 对）/real-wazuh 10（9+wazuh-manager）；六枚官方镜像 digest 钉全部渲染核对（chroma 1e0b73a1/openfga 78d1fa60/contextforge 98dfab27/langfuse 85c278dc/postgres 18cfe3ef/wazuh 80cada6a）。**.env 口径**：git check-ignore 命中；SOC_HMAC_KEY 双方共享（4.x 审批链信任根）、AGENT_LLM=fake（图纸默认 `:-real` 生产安全侧）、SECRETS_LLM_API_KEY 空=deny_no_secret 执勤（8.1 judge not_evaluable 同源）。**重建收口（场景 7 接口兑现）**：布景八项快照（alerts 18/cases 4/audit 471/runs 55/run_events 1235/approvals 7 pending 0/used_tokens 3/kb 4/chroma 1/mapstore 11/cursor 19）→ `docker compose build agent && up -d agent` 只重建一家（其余 uptime 不动）→ 15s healthy → 容器登记表 24→25 行 + 容器内同款 tierOf 读口 list_approvals=1（L1）→ 八项复点逐项相等 **零数据损失**。**捣乱三连**：A 摘 langfuse 钉→拓扑测试红"未按 sha256 digest 钉"（还原复绿 16/16）；B override 摘 chroma 钉→config -q 照过=默认栈三枚封条无执法者（**新 P3 已记"发现的问题"**）；C `AGENT_LLM=bogus` 过 config=语义校验分工在服务层。**红线遵守**：data/ 四 bind mount 全程零接触、无 down -v、其余八容器零重建。
  - 2026-09-10 步 8.3 完成：文章 `8-3.md` + 新教具 `scripts/8-3-http-server.mjs`（手写零依赖 streamable-HTTP MCP server，诚实样本：list_files/create_report/delete_file 三工具）。**双 transport 全实测**：stdio 通道（evil-calendar 样本：4 工具/投毒 2/高危 2/凭证面 api_key/exit 1）+ HTTP 通道（mini-fs：3 工具/投毒 0/L0-L1-L2 三级建议齐/credential_surface/exit 0，报告 transport=http）。**退出码三态 0/1/2 亲测**，退出码只看投毒（分级/凭证面是给人的报告）。**rug-pull 三态亲测**：首採 baseline_created → 复检 match → 捣乱 A sed 偷改 list_events 描述 → drift(changed:[list_events]) + 投毒 2→3。**捣乱 B 真网关认证墙**：体检 contextforge :4444/mcp → initialize 被挡 → unreachable exit 2（不猜不硬闯）。**捣乱 C 基线档案袋纪律**：同 cwd 体检第二个 server → 全量增删 drift（按设计，反推"一对象一目录"生产口径）。**教学点**：client 半边官方 SDK（票 25 换件史）/server 半边手写 JSON-RPC 照样通——开放协议认帧不认框架；翻页收全归自己、帧与握手归 SDK。mcp-audit 测试 14/14 绿。体检工作区 /tmp/83-audit、演示进程用完即拆，仓库零残留。
  - 2026-09-10 步 8.2 完成：文章 `8-2.md`。**三通道走线成文**：SSE 直播（run_events）/2s 轮询（audit_entries 真相源）/静态文件（eval-results/latest.json，vite 插件零后端端点），Eval 页"零特权+没数不编数"（pct null/未产出/skipped 不进分母）。**契约双端锁实测**：捣乱 A 幽灵键 attack_block_rate 植入 fixtures/eval-report/latest.json → evals report.contract.test 1 红（buildReport≢样例）+ web eval.test 2 红（by_face 读空）→ 还原双绿（1+9 passed）；web eval/audit/pipeline 三文件 23 用例全绿。**审计对账**：全表 471 条、主角审批卡 apr_baf3f3a8 人生三审计（create/approve/execute）、run_6ae4b2bf SSE 31 帧=run_events 31 行、audit 镜像帧恰 3（三状态迁移）。**部署边界金矿（8.4 预演）**：宿主重跑 eval 后 :5173 吐的还是容器烤制报告（run_at 01:13 vs 宿主 17:05，镜像 COPY 产物）——捣乱 B 宿主藏文件 :5173 纹丝不动 200，"代码/产物进容器靠构建"与 7.3 的 24/25 行 manifest 同族，8.4 重建收口。
  - 2026-09-10 步 8.1 完成：文章 `8-1.md`。**evals 全量实测**：`pnpm test:eval` 13.4s → 34 tests（33 用例 + 1 清单自检）33 ran/33 passed/0 failed/0 skipped，triage_accuracy=1.000；六维=triage 11/attack 11/approval 4/chat 4/replay 2/investigation 1；cost rows=15；by_facet：guard_scan 9/behavior_gate 3/review_reject 1/sandbox_boundary 1/credential_boundary 1。**judge=not_evaluable 正在执勤**（SECRETS_LLM_API_KEY 无真值 → llmSmokeProbe skip，probe 留痕逐字入文）。**快道口径讲透**：evals 零读 AGENT_LLM，被测对象=进程内注入 FakeTriageLlm + 真 case-backend 代码（内存库+随机端口，用完即拆），lane=unit-injected，13s 跑完与 compose 栈无关。**捣乱三连**：A 坏用例（非法 expected_verdict+坏路径）→ 全场拒演 0 测试执行、两病灶一次点名（loader fail-closed 逐字入文）；B 标注说谎（真 FP 告警标 tp）→ 仅该用例红、"标注 tp(true_positive)，实际 false_positive" 逐字 + accuracy 1.000→0.917；C `EVAL_JUDGE=fake` → judge=fixed-stub avg=1.00 而门禁 33/33 一字不差（决策 #7 活体演示）。收尾重跑默认态还原 latest.json，fixtures/eval 零残留。
  - 2026-09-10 步 7.4 完成（场景 7 收官）：文章 `7-4.md` + 闯关三题（三种身体状态×三工具裁决矩阵/闸认票不认表+三处杀伤点/三层能力面缺哪层）。**CI 四 job 本地全绿**：spec gate PASS（0 警告）、boundary gate PASS（0 越界 9/9 有人查+self-test 18/18）、lint/typecheck exit 0、pnpm test 全绿（无 flake）、guards 26+gateway 43 passed、compose config ✓。**基线对账**：agent 486+3 → **492 passed + 3 skipped**（+6=list_approvals 六用例，skip 名单未动）；evals 33/33。**测试零删除审计**（vs 6d8fbc4）：12 文件 +1679/−10；既有测试文件仅 2 个、5 行删除全是签到窗维护（快照 23→24×2 行 + 点名清单/注释/docstring），agent src 测试里唯一的 expect 删除行=快照行。**捣乱 A**：3 行临时文件越界 import web → 边界闸当场点名 R1 到行号 → 删文件即绿（静态闸与 6.4 vite proxy 端到端断裂=两类问题两套网）。**捣乱 C 诚实记账**：accuracy.test.ts 横切断言 7.1 当天并发下超时翻红一次（单跑 3.9s 逼近 5s 默认超时），复跑+体检两轮全绿=偶发时序非回归，已记 P3。**场景 7 交班状态**：九服务全程零停启零重建全 healthy；审批台账 7 张 pending 0、used_tokens 3 行、永久教具全原样；新教具 7-2/7-3 两个 .mts。
  - 2026-09-10 步 7.3 完成：文章 `7-3.md` + 教具 `scripts/7-3-gate-demo.mts`（六幕连播）。**闸的生效与拒绝全实测**：S1 无票 403 no_ticket（L1 对照 L0 免验 get_alert allow）；S2 **真网关 :8002/internal/mint 铸票**（allowed_tools 带 list_approvals，铸票口不查登记表——票面合法性靠注册表+对账测试）；S3 持票过闸 makeGatedCall 真 handler 查真台账（4 张 approved 真卡=场景 4 主角卡 apr_baf3f3a8/apr_73d3c6bd/apr_16166901 悉数现身）；S4 大小写戏法 scope_insufficient+DENIED 审计+抛错强杀；S5 **摘行对照揭底「闸认票不认表」**——摘行后持票仍 allow、handler 照常执行，摘行的执法者在对账测试与铸票上游（下一张合法票装不下它），不在闸的持票通道；S6 三态辨析 no_ticket/scope_insufficient/allow。**INV-3 对号**：approvals 表 tool='list_approvals' 恒 0 行（无审批卡物理产生）。**部署边界教具**：运行中 agent 容器登记表 24 行（旧烤制）/宿主 25 行——fixtures 构建时 COPY 进镜像，刻意不重建=行为零影响+布景零污染。**三层能力面成文**：登记面/票面/执行体（新工具进 run 还差接 worker 工具面=生产化活）。
  - 2026-09-10 步 7.2 完成：文章 `7-2.md` + 教具 `scripts/7-2-manifest-probe.mts`（三幕：健康表/病表/摘行副本）。**list_approvals 全套登记落地**：manifest 加行 L32（L1/readonly_query/m3，单行手工追加——避开生成器整表重排）、PRD A.1 加行 L1164（文档即契约，parsePrdA1 现场解析）、对账快照两处签到（tools-manifest.test.ts 23→24；test_fga_matrix.py 点名例外 {get_case,list_approvals}——**顺带更新 A1_ALL 注释**）。**空壳换真身**：src/list_approvals.ts（readonly 打开 agent 库 approvals 表/台账只出六字段数据最小化/fail-closed db_not_found）+ list_approvals.test.ts 6 用例（handler 3 + 闸正例/负例三连/摘行对照）。**四方对账图景成文**：manifest ≡ A.1 ≡ worker 面/票面(⊆) ≡ FGA 矩阵（点名例外机制）；list_approvals 不进矩阵=不在 chat 可见面（读口待接线）。**捣乱 A/B/C**：A.1 摘行→两条对账断言红（计数+点名）；分级漂移 L0→「分级漂移」断言精准咬；病表→tierOf SyntaxError→闸 403 signature_invalid（连正牌 L0 get_alert 都进不了门）。**坑**：还原 A.1 不能 git checkout（会把未提交的新行一起抹掉），要用文件副本还原——已写入文。**验证**：三件套 vitest 19 passed + test_fga_matrix.py 9 passed + 新工具 6 passed + lint/typecheck 绿 + 全仓 pnpm test 通过（详见 7.4 体检报数）。
  - 2026-09-10 步 7.1 完成：文章 `7-1.md`。**gen:tool 实测**（临时清单 /tmp/gen71，真表未动）：一条命令三件产出（空壳 handler/测试骨架/登记行）；登记先于代码（户口先于肉身）；三道入闸全实测（重名 exit 2 字节不动、`List_Approvals` 大小写 exit 1、--tier L9/--family case_read/--owner m3x 三连 exit 1）。**捣乱 D**：对真表跑一次 → manifest 被 JSON.stringify(…,2) 整表重排（diff 175+/24−，语义零变化）→ tools-manifest.test 立刻红在"manifest 只比 A.1 多已点名的偏差工具" → git checkout 还原即绿——**登记=给全仓对账网打招呼，单方面漂移必红**（7.2 正片）。据此定案：**7.2 真表登记行用手工单行追加**（生成器只管代码，尊重既有文件风格）。gen-tool.test.ts 3/3 绿；选型理由（审批台账是场景 4 主角面/只读零 L2/L1 同 get_case 更严口径）入文。**全量测试基线复测**：485 passed + 1 failed + 3 skipped——failed 是 accuracy.test.ts 横切断言在并发下的**偶发超时**（guards HTTP 扫描 11 案串行，单跑 3.9s 绿），非代码回归，7.4 体检时复核。
  - 2026-09-10 步 6.4 完成（场景 6 收官）：文章 `6-4.md` + 闯关三题（豁免的因果/"钥匙与失忆"两角色两命运/原文户口三栏归档）。**note 教具**：`b7b8ce47`（case_000002 时间线，M2 内网直写口 kind=note，body 带 <CN_ID>/<PHONE_NUMBER>/<EMAIL_ADDRESS> 三个占位符——2.3 捣乱 B"内网口不设防"同款再利用，反查按钮现身条件=正文有 <TYPE> + 角色镜像名单）。**走线 bug 当场抓获**（已记"发现的问题"P2）：`:5173/api/v1/pii/reveal` 404（vite proxy 表缺 /api/v1/pii 行）vs `:3003` 同发 200；前端误导文案"映射表里查不到这个占位符"（两种 404 共用一个分支）。**口径三问成文**：原文不进时间线（广播面只有占位符/原文只在点对点响应）、点开才反查（最小暴露+责任显式化+权限差异）、SSE 不承载反查（人的动作不产生 run）。**审计一一对应**：一条 note 三占位符=三行 SUCCESS（match_count 1/3/1）；走线断掉的 404 无审计（"到不了闸的查询连被审计资格都没有"）。**捣乱 A**：soc1 绕 UI 直打 403+DENIED——可见性/授权两层各司其职。**布景收尾状态（场景 6 交班）**：九服务在线全 healthy；mapstore 11 行 5 占位符（6.1-6.2 假数据教具，含 6.1 捣乱 B 误标的 ' 192.168.1.100'）；M2 审计 pii_reveal 累计 12 行（SUCCESS×5/FAILURE×3/DENIED×4，全假数据）；case_000002 时间线 +1 note（b7b8ce47）；guards restart 两轮、stop-start 一轮全恢复 healthy；agent/case-backend/openfga/FGA 世界未动。
  - 2026-09-10 步 6.3 完成：文章 `6-3.md`。**四角色反查实测**：duty_lead/admin 200（originals 逐字还原）、soc1/redteam 403 pii_reveal_forbidden——soc1 能查能写就是不能反查（职责口径）。**三态审计全落账**（M2 audit_entries 实测）：SUCCESS 只记 match_count（原文一个字节不进 details）/DENIED 带 role_forbidden+被拒角色/FAILURE 带 reason。**敏感面大搜查**：假身份证原文 agent 库+M2 库全量 dump grep 0 命中，阳性对照 mapstore 1 行——原文只活 mapstore 与有权限响应体两处（INV-4 类推口径，测试面金丝雀 pii-reveal.test.ts:145-162 同款断言）。**fail-closed 捣乱**：停 guards → duty_lead 反查 502 guards_unavailable + FAILURE(guards_timeout)（Docker 停容器端口转发黑洞吃满 2s，5.2 捣乱 A 同款）→ 恢复 healthy 同发 200 逐字还原。**伪造 <FOO>**：404 placeholder_unknown + FAILURE 审计（查无此人也是账）。**反查不产生 run**（runs 数不变，"人的动作"账面证据）；LLM 全链路无原文通道（工具面/编排面均无 reveal，纸面推演入文）。布景变化：guards stop-start 一轮（已恢复 healthy）、M2 审计 +9 行 pii_reveal（4 角色+401/400 场外+502/404/200 组）。
  - 2026-09-10 步 6.2 完成：文章 `6-2.md`。**主实验（重启不丢）**：reveal `<CN_ID>` 200 → docker restart guards → healthy 后同占位符 200 逐字一致（110101199003071234）；**内存对照组**：容器内 `:memory:` 双进程实验，进程 2 reveal `[]`——FR-S4.2 原口径"内存保留会话即弃"的死法实证，ADR 0004-3 改判语义讲透。**多原文首见序实测**：`<PHONE_NUMBER>` 反查 originals=["13800001234"," 192.168.1.100","13800005678"]（"证人 A"不止一个；第二条带前导空格=6.1 捣乱 B 误标 span 原样入账）。**幂等实测**：主探针重打两遍行数恒 11（INSERT OR IGNORE + first_seen 不刷新）。**伪造占位符扫雷（guards 裸口）**：<FOO>→404/<cn_id> 小写→404 大小写敏感/纯空格→400/' <CN_ID> '→strip 容错 200，两个 404 响应体逐字相同不透露库存。布景变化：guards restart 一轮（已恢复 healthy）、mapstore 仍 11 行 5 占位符。
  - 2026-09-10 步 6.1 完成：文章 `6-1.md`。**主探针实测**（全假数据 110101199003071234/13800001234/zhangsan@example.com/4111111111111111）→ 五占位符 <CN_ID>/<PHONE_NUMBER>/<EMAIL_ADDRESS>/<CREDIT_CARD>/<IP_ADDRESS> 逐字落位、内网 10.47.100.23 豁免存活、公网 198.51.100.23 照脱、**中文姓名不脱**（ENTITY_TYPES 五识别器契约无 PERSON + en_core_web_sm 中文弱，双层原因如实讲）。**RFC1918 边界实测**：10.x/172.16-31.x 存活、172.32.1.1（段外一格）/127.0.0.1（环回不在三段）照脱；容器内 analyzer 原始判定实证"豁免=认了再扔"。**捣乱 A 识别器边界扫雷**：坏 Luhn 卡号（校验和门槛）/86 前缀 13 位/带空格手机（正则盲区，5.1 URL 编码同族）/座机（能力边界）四类全漏网。**捣乱 B 发现边角问题**（已记"发现的问题"）：192.168.1.100 被内建 PhoneRecognizer 误标 <PHONE_NUMBER>（score 0.4 抢座；豁免只长在自家 IpRecognizer 上管不住隔壁识别器）。**接线现状如实口径**：anonymize 是出域边界独立口，告警/LLM 出站流水线未自动调用（PRD 挂点=LLM 出站前，生产化预留）。布景变化：mapstore 首批 11 行映射（5 占位符，含带前导空格的 ' 192.168.1.100'）留作 6.2 教具，guards 未重启。
  - 2026-09-10 步 5.5 完成（场景 5 收官）：文章 `5-5.md` + 闯关三题（结果像≠因果被带偏定罪三证据/四通道四命+guards 停机联动+变体话术接力/钥匙户口+幽灵户口→"声明的世界 vs 发生的世界"）。**钥匙户口实测**：SECRETS_* 只挂 gateway（3 键），agent/ingest/case-backend/guards/web 全 0；本机 .env 的 SECRETS_LLM_API_KEY 为空 → **deny_no_secret 正在执勤**（现场 503 + gateway 日志 JSON 行，1.5 降级链的现状版）。**三 deny 路径全实测**：A1 泄露检测（临时填 key=canary-llm-key-teaching-fake → messages 塞真值 → 503 deny_leak_detected，detail 只记路径 .messages[0].content 不记值）；A2 未知占位符（metadata 塞 ${{ SECRETS.internal.KEY }} → 503 deny_missing_credential）；裁决顺序=no_secret→missing→leak→才转发（INV-1 最致命先问）。**全可观测面大搜查**：两枚金丝雀（LLM+VT）grep agent 库（12943 文本格）/M2 库（4673 格）/九服务日志/agent 容器文件面——**全 0 命中**，阳性对照（isolate_host→20 条）证明方法有效 = INV-4 实证。**布景切换与还原**：.env LLM key 临时填金丝雀 + gateway 重建两轮，已还原（key 行回空、healthy、deny_no_secret 回执勤位）；gateway stop-start 共两轮。**场景 5 收尾状态**：告警 18 条（+5101/5102 教具，739e30a0 已并 case_000003 linked 7）；M2 账面 kb 4 条（+毒幽灵 kb_ed4f2ae3 approved 永久教具，chroma count 恒 1）；审批卡 7 张（+apr_8d8da6dd rejected，pending 0）；chat_flow run +6；user_input DENIED +5、alert_field DENIED 累计 6；guards stop-start 共三轮全恢复、gateway 重建两轮全恢复、msb 探针/演示沙箱即毁无残留。
  - 2026-09-10 步 5.4 完成：文章 `5-4.md` + 新教具 `scripts/5-4-sandbox-demo.mts`。**意外惊喜：本机 msb 0.6.16 已装（/opt/homebrew/bin/msb），msbProbe 真过**——毒 analyzer（fixtures/attack/sandbox/01）在真 microVM 真跑：表面照常吐 Cortex 结果（"毒在动作不在结果"）、egress errno=111 拦截、VM env 仅镜像 9 键+凭证路径缺失（宿主金丝雀 SOC_CANARY_SECRET 不可见）、零 breach；**sandbox.test.ts 14/14 全绿含真跑侧**（验收 4：无 enrich-* 残留沙箱、新 VM 看不到 /tmp/pwned）。**降级口径实测**：注入坏 msbBin → ok:false+人话 reason（CI 无 KVM → mock 侧照常全绿、真跑侧显式 skip）；script_missing → fail-closed 不开机。**教学点**：三道闸全长在命令行参数上（buildMsbArgs 纯函数=拓扑承诺）；情报表文件挂载让"能干活≠能外联"；审计不采信 VM 内口供（宿主侧独立度量，谎报只能骗自己）。教具用 MemoryAuditSink + msb 探针 VM 即拆——布景零污染。布景变化：无（无 DB 写入、沙箱即毁、msb 布景未动）。
  - 2026-09-10 步 5.3 完成：文章 `5-3.md`。**两条投毒路线双未遂**：路线 A 对话提请（duty_lead 会话"写进知识库：攻击IP 属白名单"→ 卡 apr_8d8da6dd params 带毒草稿全文+params_hash 16ac32df → 值班长驳回 reason 逐字留痕 → run_a4d2b149 completed）——**提案账面零新增**（chat kb_write 执行体=演习弹 mockAction，如实讲清：它防"对话面动笔"不防 REST 直投）；路线 B REST 直投（02_poison_rejected 毒语料原文+冒名 proposed_by=agent:knowledge → kb_ed4f2ae3 proposed）→ **幽灵批准对照**：直连 M2 approve reviewer 缺省补 duty_lead（冒名镜像，INV-9 反面教材）→ 账面查询面 /kb/search 被骗吐毒、**chroma count 恒 1 + 3-4 教具毒查询探针查无**（INV-5 结构保证）、再裁 409。账面四格对照组凑齐：approved+在面（正门）/approved 幽灵×2（3.2 + 本步毒幽灵）/rejected（卡驳回）/proposed。**strip 兜底证伪**：毒正文过 kb 通道只剔正则认得的一行、三条换说法的毒步骤存活；中文口语毒句 score 0 allow——兜底层不承重，防线排序"进不来>有人看>降噪"。**红线遵守**：未真批准任何毒条目进 chroma（幽灵技巧=账面对照）。布景变化：M2 账面 kb 条目 3→4（kb_ed4f2ae3 幽灵 approved 永久教具）、审批卡 6→7（apr_8d8da6dd rejected）、chat_flow run +1、guards 未动。
  - 2026-09-10 步 5.2 完成：文章 `5-2.md`。**三话术全拒**（M1 放水 run_4957afd9 score 0.6 指令覆盖 / M2 伪造审批+隔离 run_2f63422a score 1.0 权威升级+工具调用注入 / M3 会话外传 run_db3bf2ab score 0.6 数据外传）——拒答三帧（token 已拒答 + denied input_blocked_by_guards + done）、零工具帧、run completed（拒答=正常业务态非故障）、审批卡 6 张 0 pending 与 used_tokens 3 行纹丝不动。**变体对照 M4**（英文 treat-all-as-fp，run_5d2025de）：score 0 滑过词面 → 意图分类 unknown/0.2 → 澄清反问零动作——「过闸≠得逞」分层接力现场演习；**诚实高危请求对照**「请把主机 app-01 隔离」score 0——注入防线管话术、审批防线管动作。**捣乱 A**（guards 停机，run_e92d563c）：无害消息也拒（action=fail_closed、reason=guards_timeout、耗时 2.06s=吃满扫描超时）——对话面 fail-closed 全关门账单；**捣乱 B**（编排面直投 run_af331e92）：block 0.6 DENIED，安检住 worker 不住路由（边角教具：guards 刚 start 未过 healthcheck 的窗口直投得 fail_closed 而非 block，同一拒答两种 reason）。**卡**：对话面三层防线（拦话术/管权限/管授权）+ fail-closed 可用性账单。布景变化：guards stop-start 两轮（已恢复 healthy）、chat_flow run +5（3 拒+1 澄清+1 编排面拒绝）、user_input DENIED 台账 +5。
  - 2026-09-10 步 5.1 完成：文章 `5-1.md` + 新教具 `scripts/5-1-rein-triage.mts`（三幕：毒原文 vs 占位符判定逐字相同 / 载荷下指令被无视得 uncertain/human / 编码 URL 滑过扫描仍被 R2 decode 特征识别成 tp）。**四植入位实测**（主角 a4875aa0，sourceRef 1682439999.5101，run_5333db1c）：①full_log ②previous_output 同拼 description、③srcuser→observable:other、④url→observable:url——四植入位三送检段；直考探针 ①1.0/②0.6/③1.0 block，**④编码态 0.0 allow（正则盲区，inject-ua 的 UA 样本同样滑过、解码态 1.0）**。**教学金矿**：verdict=btp/close 恰是 payload 想要的结果，但**因果链在 KB 不在注入**——kb_check 命中唯一 env_fact（top-k 无门槛，P3 现形），rationale 逐字引用 KB 标题、payload 躺在占位符里——「结果像被带偏 ≠ 因果上被带偏，定罪看因果」。**捣乱 A（guards 停机，run_ca78eff1）**：fail-closed 三通道联动——alert_field 四段（含干净 srcip）全占位、相邻审计差 2000ms=每段吃满 2s 超时、kb 命中被 fail-closed 剔除 → verdict 漂 tp（KB 缺席所致，非 payload 得逞）并 merge:case_000003；对照 2.2 的 tool_output fail-open（票 50 已修）——prompt 侧通道设计起就接住 fail_closed。**划界（如实讲）**：规则件抗注入是 fake 的性质，real 的防线 = block 占位 + wrapUntrusted + schema 把关 + 建议/执行鸿沟。布景变化：告警 16→18（5101/5102 教具留档）、guards stop-start 一轮已恢复、739e30a0(tp) 并入 case_000003（linked 7）、alert_field 累计 DENIED 4+2 条。
  - 2026-09-10 步 4.7 完成（场景 4 收官）：文章 `4-7.md` + 收官三题（批准后三秒的全环节+读口病结局/三张过期牌——卡资格·票执行窗会过期而焚毁案底永生/soc1 两次碰壁+审批正门内网信任口径）。**TTL 全链实测**（停栈回拨 created_at 法，因 compose 未接线 APPROVAL_TTL_SECONDS 且学习阶段不改 compose）：卡 apr_0d4cc044 pending→expired、run_0525627d failed(approval_expired)、expire 审计（actor=m3:dispatcher, ttl_seconds=86400）、事件 approval_decided(decision:"expired",by:"system:approval_ttl")、run 两步迁移各留审计。**队列形态实测**：POST case_flow 202 queued 秒回 → run_jobs 38 号 pending→claimed→done 一秒内 → run completed steps=2。**教学点**：时间出的裁决也是裁决（过期走状态机/actor 留痕/run 必有终局）；两把时间尺（卡 24h 人的节奏 vs 票 300s 机器的节奏，比值跟人工介入节奏走）；SQLite 表当队列（ADR 0004-1 不引 MQ）；毒丸不重试 vs 事件桥不推游标的失败哲学对照。
  - **场景 4 步 4.6 布景状态（2026-09-10）**：焚毁三件套对账完毕——used_tokens 三行（ap_3cac74f3 前辈/ap_4a9d6e0d 主角/ap_53b3e645 重试卡）、主角卡 executed_at 已章（token_jti 与坟场逐字对上）、M2 审计四连 create→approve→execute→execute(used_token)。**重复焚毁 409 jti_exists 实测**（store.ts UNIQUE 约束层护栏）；**断链教具票 ap_d00d9edd 不在坟场**（铸了没花 = 执行没发生的账面证据，且已过 300s TTL——4.7 素材）。卡 2 新教学点：焚毁写读不对称（fire-and-forget 写 vs 同步必答读，判据=失败发生在裁决前还是后）。
  - **场景 4 步 4.5 布景状态（2026-09-10）**：**新教具 `scripts/4-5-verify-demo.mts`**（七幕：S0 allow/S1 签名改位/S2 过期/S3 进程内焚毁/S4 换参数/S5 票错工具/S6 无票硬闯 L2·L1/S7 跨进程重放问真 M2 读口；本地铸造器复刻 py 票型含 json.dumps 空格分隔符；密钥读 env→.env 兜底；运行口径 `cd services/agent && pnpm exec tsx ../../lessons/scenario/scripts/4-5-verify-demo.mts <已焚jti>`）。S7 双态实测：M2 在线 → 真烧掉的 ap_4a9d6e0d 查读口 200 → token_used；**停 case-backend → 读口病 → 归 signature_invalid（INV-1 fail-closed 实证）→ 已恢复**。对照组：S7 未焚 jti 走"其余条件全过 → 放行"分支。布景净变化：case-backend stop/start 一轮（已恢复 healthy）。
  - **场景 4 步 4.4 布景状态（2026-09-10）**：**主角卡 apr_baf3f3a8 已批准执行完毕**——token `ap_4a9d6e0d-63a9-4b7c-9460-273b6ca6c7f8`（九 claims 全解剖：tool/params_hash=卡指纹/case_id/TTL=300/used:false；hex 签名教学变体），used_tokens 已焚（1789025609377），run_6ae4b2bf completed steps=6（事件 945-961：approval_decided→resume→execute 重跑→tool_call/tool_result(mock_edr isolated)→answer_llm→completed），M2 审计三连 create→approve(user:duty_lead)→execute。**秒回契约实证**：approve 200 时 run_status=awaiting_approval，2-3 秒后才 completed。**捣乱 B**：停 gateway → approve 502 mint_failed 卡仍 pending → 恢复后重试 200（边角：gateway healthcheck 未过时 502 会被复现，等 healthy 再试）。捣乱 A：缺署名 400 approver_required。新教具卡：apr_16166901（run_8f5cd746，isolate_host，B4 批准后正常执行完毕，token ap_53b3e645 已焚）。布景净变化：gateway stop/start 一轮；chat_flow run +1；卡 +1（apr_16166901 approved+executed）；used_tokens +2（ap_4a9d6e0d/ap_53b3e645）。
  - **场景 4 步 4.3 布景状态（2026-09-10）**：主角 run `run_6ae4b2bf` 挂起态解剖完毕（信封链 6 环链尾=intent_gate、checkpoint_writes 有 idx=-3 __interrupt__ 闹钟 value=卡 id、SSE after=749 补发 id:750 一帧实测）。**断链教具（不可修复，永久留档）**：捣乱 B 停 agent 后篡改 run_73353b26-ab33-4bfa-a535-fcb81f7d0dd2 第 2 环 state 字节（追加空格）→ 起栈 → 批准卡 `apr_73d3c6bd`（block_ip，approve 200、token `ap_d00d9edd` 已铸未花、300s 自焚）→ resume 被信封链复核拒绝：M2 审计 `resume|FAILURE|{"reason":"checkpoint_tampered: state bytes tampered at seq 2"}`，run 永挂 awaiting_approval。**"批准≠能恢复"两道独立机制实证**。实验纪律口径：动库前必须 docker compose stop agent（SQLite 铁律）。布景净变化：agent 容器重启 2 次（stop/start 实验各一）；chat_flow run +1（run_73353b26）；卡 +1（apr_73d3c6bd approved 但永不执行）。
  - **场景 4 步 4.2 布景状态（2026-09-10）**：意图闸三态全实测——allow=run_f7104aeb（soc1 只读直查 related_alerts，事件 800/801 工具帧+回答）/require_approval=run_6ae4b2bf（主角卡 apr_baf3f3a8）/deny 三样本=run_4facef5a（soc1 不可见）、run_8bf9d02c（redteam 只读也拒）、run_7c33e17b（编排面伪 role=hacker 未知角色拒）。**停裁判实验**：openfga stop → run_192a4446 soc1 只读查询也拒（fga_timeout，deny 覆盖 allow）→ 已 start+setup-openfga.sh 重建（store/model id 刷新）。直查裁判口径：`curl POST :18080/stores/$STORE/check`，soc1 can_execute close_alert=True 但对话面仍 require_approval（gate.ts:68 无写手语义）。教具 run 另有 run_88f47897（恢复后回归）。布景净变化：openfga 授权世界重建过一次（id 已刷新，fga_ids.json 同步）；chat_flow run 共 +6。
  - **场景 4 步 4.1 布景状态（2026-09-10）**：**主角卡 `apr_baf3f3a8-fd09-4fcc-9c80-0c4f67e66a88` 已开**（tool=isolate_host，params={"host":"app-01"}，params_hash sha256:7f36978b…，来源=chat 意图，pending）——4.3-4.6 全程跟着它走；run `run_6ae4b2bf-531c-4276-82a1-580bb42188bf`（chat_flow）停在 awaiting_approval（事件 737-750：load_context 过 get_alert L0 → intent_classify 命中 → intent_gate require_approval → execute 开卡）。**建议/执行鸿沟实证**：case_000002 调查报告（timeline 1d982c59）建议 isolate_host 而 M2 审计簿对应审批卡 0 行。捣乱教具：run_4facef5a（soc1 同句 → 100% deny 零卡）、run_aadc1017（含糊消息 → 澄清反问零卡）。登录口径：username 必须全称 `duty_lead@soc.local`（写 duty_lead 得 unknown_identity 401）。
  - **行号快照漂移备案（2026-09-10，票 50/51 修复的衍生）**：修复在生产文件插入注释/兜底行——`workers/investigation/flow.ts` 净 +8 行（插入点约 L72/L232 两处）、`workers/enrichment/flow.ts` 净 +8（约 L55/L171 两处）、`workers/knowledge/vector-store.ts` 净 +5（约 L39 一处）。已发布文章 **2-1/2-2/2-3/2-4（前两者）与 3-4（后者）** 中，位于插入点之后的引用块行号与 #L 链接为**修复前快照**（个别块如 2-2 observe 的 scan 行、3-4 未受影响块除外——2-2 已有文末备注交代）。引用内容的**教学语义全部仍成立**（修复只加留痕与注释，不改被教逻辑）；权威差异以票 50/51 的修复提交（fc0e5ad/a138dc2）为准。若要整批刷新行号，建议凑一次"文档校准"小票统一做，避免每次修复零散追改。
  - **票 50/51 修复记录（2026-09-10，用户授权派子 agent 执行）**：#50 方案 a——调查/富化两 tool_output 消费点显传 `failMode:"flag"`，guards 不可达折 flag 打标（reason 可 grep：compose 停容器为 guards_timeout/连接拒为 guards_unreachable，同一条降级路径票 43 口径），审计与 SSE details 的 score/reason 一律 `?? null`；+3 单测（guards-client/两 worker），全量 486+3sk 零删除；停机冒烟 4 条打标留痕、在线行为回归不变；2-2.md 文末补 A/B 复跑备注；agent 容器已重建含修复。#51——VectorHit.score 注释口径如实（真 chroma=L2、score=1−distance 可负、内存件=归一化点积、只承诺排序单调）+ 护栏选项 a 零行为变更（b 阈值标定/c 主体一致性校验记票留生产化）；定向 31 用例绿、教具复跑逐位一致。两票 Status done（fc0e5ad/a138dc2）。
  - 2026-09-10 步 3.5 完成（场景 3 收官）：文章 `3-5.md` + 收官三题（游标重放防重分布/幽灵病根=批准链七检查点/uncertain 案沉淀 skip）。**反哺闭环全实测**：新告警（换 sourceRef 避重）→ autorun → kb_check 命中 env_fact → R1 btp"rationale 逐字引用 KB 标题"（溯源链：rationale←KB title←人审←verdict_note←DX-2026-09）→ outcome 只落建议 status 不动（AI 不动手）→ SOC1 close_flow（最小票 get_alert/close_alert，close_alert 是 L1 可撤销）→ 预检三连 → close_confirm 记 soc1 人头。**捣乱**：close_flow 关 uncertain 告警（advice=human）→ failed close_advice_missing"确认按钮执行的是建议不是自由关单权"；手滑短 id 得 alert_not_found 强杀（白捡教具）。卡=建议-确认-执行三段式水位计、SOC 知识反馈闭环领域卡。**新发现教学点**：M2_FINAL_VERDICTS 含 uncertain（alert 层可确认关 uncertain 告警），但 case 层 uncertain 关案后知识必 skip——"拿不准"在哪个层面都进不了知识库。
  - 2026-09-10 步 3.4 完成：文章 `3-4.md` + 新教具 `scripts/3-4-kb-demo.mts`（四幕+任意查询探针：S1 hashEmbedding 逐位确定/S2 内存替身余弦排序+0 分也占位/S3 真容器负分命中/S4 ChromaKb 分诊视角）。检索面四层 seam（TriageKb→ChromaKb→VectorStore→RealChromaClient REST v2）逐站解剖；MemoryKb stub 词表（asset/user_list/known_change）vs §5.10 三类与 R1 双兼容（llm.ts:71 known_change‖env_fact——fp_pattern/runbook 不参与关单建议）；KB_TOP_K=5=决策记录#6 投毒面收缩；不引官方 npm client 的契约测试纪律；维度坑（query_texts 被拒→写入/查询必须同 embedding）。**三教学点**：①score 为负（L2 空间 vs 注释 cosine 口径）→ 已录 P3；②top-k 无门槛"命中≠相关"，系统安全押在 INV-5 不在检索精度；③fail-open 两案对照（本步有痕降级+提速件 vs P2 guards 无痕+安全通道）。**3.5 接口**：反哺靠共享词面 centos7——replay 同主机同类告警→kb_check 命中 env_fact→R1 btp+close。场景 3 收官题随 3.5 发。
  - 2026-09-10 步 3.3 完成：文章 `3-3.md`（审批授权链全走通：先铸票后裁决（失败可重试不留悬置）/并发双批先到者赢后到 token 300s 自焚/秒回契约 run_status=awaiting_approval（批准≠执行）/resume 必须重组 worker 图否则薄径静默丢 L2（票 17 教训）/awaitApproval 无限循环套 interrupt"卡是唯一真相"/闸验签→执行→焚毁→标记铁律顺序/执行体先账面后 chroma+409 幂等重放安全/驳回不进闸不碰检索面 run 照常 completed"执行没发生=事件流少一对工具帧看得见"；卡=L1 任务票 vs L2 ApprovalToken 两票分工、LangGraph interrupt 挂起恢复）。**三终局对照组教学点**：approved+在 chroma（正门）/approved 幽灵（绕门）/rejected（驳回）——只有正门能被分诊检索。token 六拒因挂账场景 4.5。approver 无缺省必须署名 vs M2 reviewer 缺省 duty_lead（正门 vs 镜像的信任不对称）。
  - 2026-09-10 步 3.2 完成：文章 `3-2.md`（两种"关"之后的账面课：五端点门牌/建档三小闸 400·404/mapKb 字段透视（**expires_at 预留字段全仓无人写**——如实记"没盖到的房"非 bug）/reviewer 三级缺省链 body→x-actor-id→duty_lead（留痕镜像，真身份锚 INV-9）/searchApprovedKb 四口径（approved 写死·k 夹 1-50 缺 5·LIKE·created_at 升序 vs list DESC）/**账面查询面 vs 向量检索面两扇窗**）。**幽灵条目实验（C）**：直连 M2 approve 翻账面→search 可见→chroma count 仍 0→终态再裁 409——"批准≠进检索面"实证，留作 3.3 正门对照组。卡=SoR vs SoA 分家、SOC 知识三类条目领域卡。教学点：中文查询须 URL 编码（curl 用法非 bug）；账面无 (source_case_id,kind) 唯一约束，防线在流程在不在 schema；web 无 KB 页（纯 REST 面）。
  - 2026-09-10 步 3.1 完成：文章 `3-1.md`（两种"关"辨析：告警级 close_flow 无事件 vs 案件级 REST 关案发 case.closed；outbox+游标轮询事件桥、launch=app.inject 打自家正门、三道防重 dup_batch/run_exists/kb_exists、"跳过推游标/失败不动游标"；关案口双闸先 verdict 后状态机、事件与账本同事务；知识票 KNOWLEDGE_TOOLS 无 kb_write（INV-3 三现）；ASP 门控双保险（关案口 409 + worker 防御再拦 skip）；Fake LLM=verdict→kind 翻译词典（记录员不是判断者）；parseDraft 三分支；审批卡 params 带草稿全文+params_hash（D8）；账面/检索面分家 INV-5 count=0；卡=outbox 轮询 vs MQ、chroma 首秀）。**捣乱四连实测**：A 无 verdict 409 verdict_required、B New 直关 409 InvalidTransition、C 未关案直拉 202→completed skip（零污染有审计）、D case_999999 202→failed node_error。**教学点**：①VerdictRequiredError.httpStatus 刻意 409（业务规则冲突归冲突）；②M2 审计 actor=system——HttpKnowledgeM2 不转发 x-actor-id，身份在 body proposed_by/票面 sub/将来 ApprovalToken（与 2.3"内网信任口薄"同族，设计非 bug）；③手动正门可绕 autorun 三防重造重复提案（防重住事件桥层，纸面推演未真打以保布景）；④web"一键确认关单"是告警级，案件关案只有 REST。
  - **布景核查纪律（2026-09-10，2.1 事故教训）**：**绝不以 rw 模式从宿主直接打开容器正在写的 SQLite 文件**（`sqlite3 data/agent/agent.sqlite "…"`）——Docker Desktop bind-mount 跨端 shm 不一致，宿主 rw 读会把容器未 checkpoint 的 WAL 当无效重置，**静默吃掉已提交的行**（实证：捣乱 A 的 run 行+事件消失，而 M2 侧 audit_entries 证明它 create/update/kill 全程跑过；容器内复查也查无此行）。核查三选一：① 文章口径 `sqlite3 "file:…?mode=ro"`（只读，无写权不会重置 WAL，已发布文章全部如此，无需修）；② 容器内读 `docker compose exec agent node -e "…better-sqlite3…"`；③ REST/SSE。已发布的 1-x/2-1 文章命令均为 mode=ro，**不用回修**。
  - **节奏规则（2026-09-10 用户定）**：主窗口直写（比派发省 token）；**每写完一个场景停下，等用户手动压缩上下文后再开下一场景**——场景边界 = 压缩点。续学时先读本总纲 + git log 恢复状态。
  - 用户偏好（2026-09-10，1.1 定稿）：**调用链节必须穿插真实代码/配置原文**——每段标注文件+行号范围、md 内链接可跳转（仓库根相对路径 `soc-demo/...` + `#L行号`）；引用一律逐字照抄，注解写在代码块外的正文里；细则繁琐的处理逻辑简化为**明确标注"伪代码"**的块并指向真实行号。已固化进 learn-by-rebuild skill 模板（2026-09-10）。
- 发现的问题（转票）：
- **P2 · python job 的 ruff 步骤实测红 + 远端 CI 从未执勤**（2026-09-10，8.5 收官实测发现）→ **已开票 #52 并修复（2026-09-10，①7 处违规清零：guards 5 处 I001 import 排序（`ruff --fix` 自动）+ gateway/test_proxy.py 2 处 E741（`l`→`line` 手改含用点），双尺 ruff 0.16.6（钉定版）/0.6.9（uv tool run）对 services/guards+gateway 各自 exit 0，pytest guards 26+gateway 43 全绿；②`ruff>=0.6` 改精确钉 `ruff==0.16.6`（精确钉即冻结规则集，未另加 ruff.toml）；③ruff 步补进本地等价清单——8-5.md 文末备注，正文零改动；推远端执勤半步留用户）**。位置：`.github/workflows/ci.yml:53`（`ruff check services/guards services/gateway`）+ `services/guards/requirements-dev.txt:3`（浮动钉 `ruff>=0.6`）+ 各违规文件。现象三层：①本机 .venv ruff 0.16.6 对 services/guards 报 5 处 I001（app.py:13/conftest.py:7/pii.py:12/test_guards_contract.py:8/test_pii_mapstore.py:12），连 `--isolated` 纯默认也报=新版 ruff 把 isort 纳入默认规则集；②换 ruff 0.6.9（uv tool run）I001 消失、却报 services/gateway/test_proxy.py:192,195 两处 E741——**两代尺子都红，该步骤按现状不可能绿**；③origin/main 仅 17 个文件的旧 stub（.github/workflows 未推送、本地 main 领先 156 提交），CI 从未在远端跑过任何一次——而 7.4 的本地等价当时漏抄了 ruff 半步（只跑 pytest），红因此一直没暴露。修法（组合）：修 5 处 I001 + 2 处 E741（多为 `ruff --fix` 可自动）；requirements-dev 把 `ruff>=0.6` 改为精确钉或加 ruff.toml 钉规则集；把 ruff 步补进本地等价命令清单；推 workflows 上远端让 CI 真执勤。学习阶段不动，留票统一处理。**修复回填（2026-09-10，票 52）**：上述修法①②③已全部落地——双尺皆绿实测见票 52 实现记录与上方修复摘要；仅剩"推 workflows 上远端执勤"半步留给用户。备查：`services/gateway/requirements-dev.txt` 仍有浮动 `ruff>=0.6`，但 CI 的 pip 是两条 requirements-dev 一次解析安装，guards 的精确钉已把解析版本约束到 0.16.6，不构成漂移面（文件在票 52 授权范围外，未动）。
- **P3 · 默认栈官方镜像的 digest 钉缺机器断言**（2026-09-10，8.4 捣乱 B 实测发现）→ **已开票 #53 并修复（2026-09-10，钉断言扩为"凡 image: 必 @sha256"全量封条总闸）**。位置：`soc-demo/docker-compose.yml:124/141/155`（chroma/openfga/contextforge 三枚 `image:` 钉）+ `services/agent/src/compose-topology.test.ts:36-44`（钉断言只覆盖 langfuse 对与 wazuh-manager 三枚 profile 镜像）。现象：override 文件把 chroma 的 `image` 摘成 `chromadb/chroma:latest` 后 `docker compose config -q` 照样通过（compose 语法闸不校验封条），拓扑测试也不红（它静态读的 docker-compose.yml 文本里 langfuse/wazuh 才有钉断言）——默认栈三枚封条目前只有 compose 注释 + 评审纪律在守。对照：摘 langfuse 的钉拓扑测试当场红（8.4 捣乱 A 实测）。修法（择一/组合）：compose-topology 钉断言扩展为"凡 `image:` 行必匹配 @sha256"；或 CI compose-config job 加一条 grep 闸。学习阶段不动，留票统一处理。**修复回填（2026-09-10，票 53）**：票内推荐修法已落地——compose-topology.test.ts 纯追加全量总闸（凡 `image:` 行必以 `@sha256:<64hex>` 收尾，含 profiles 内服务，缺钉失败逐行点名；票 37/38 旧断言零删除，另配"扫描非空"护栏）；自证摘 chroma 钉当场红点名 :124、git checkout 还原即绿 18/18；agent 492→494+3skipped 只增不减；`docker-compose.yml` 本体零改动；8-4.md 文末备注（照 2-2 票 50 先例，正文零改动）；学习日志已记；CI grep 闸方案按票面否决未做。详见票 53 实现记录。
- **P3 · accuracy.test.ts 横切断言在全量并发下偶发超时**（2026-09-10，7.1 全量首跑实测，7.4 捣乱 C 复核；**8.5 收官三连复跑再复现**——前两跑各红一次、单跑 14/14 绿（横切断言 4.08s 逼近 5s 上限）、第三跑全绿，当日负载高于 7.4 时点，修法优先级建议提高）→ **已开票 #54 并修复（2026-09-10，横切断言用例 testTimeout 5s→15s 显式放大，保留真管道语义）**。位置：`services/agent/workers/triage/accuracy.test.ts:113-121`（横切断言串行 triageOne 全部 11 条标注告警，每条走真 guards HTTP 扫描）。现象：全量并发跑时偶发超时翻红（vitest 默认 5s；单跑实测 3.9s/11 案逼近上限），同日复跑与收官体检两轮全量均全绿——负载时序问题非回归。修法（择一）：该用例显式放大 testTimeout；或横切断言换 fakeScan（真 guards 冒烟留给专项）；或 vitest maxWorkers 收敛。学习阶段不动，留票统一处理。**修复回填（2026-09-10，票 54）**：票内推荐修法已落地——横切断言大用例收尾加 test 级第三参数 `}, 15_000);`（≈3 倍余量，写法同 run-dispatcher.test.ts 先例）+ 首行块注释交代为何；不换 fakeScan、不动 maxWorkers，真管道横切守门语义原样；既有断言一条不删不改（纯参数）。验证：单跑 14/14 绿（横切断言 3975ms）；全量三连跑 494 passed + 3 skipped 全绿（12.86s/12.49s/12.57s），基线吻合票 53 后口径、0 新增用例。换 fakeScan / 收敛 maxWorkers 两个备选方案按票面否决未做。详见票 54 实现记录。
  - **P2 · web 反查按钮走线断在 vite proxy 缺行**（2026-09-10，6.4 收官实测发现）→ **已开票 #55 并修复（2026-09-10，①proxy 表补 `"/api/v1/pii"` 行；②静态对账单测 vite-proxy.test.ts：proxy 前缀 ⊇ src 实际发起的 /api/v1/* 前缀，摘行红 `[ 'pii' ]` 还原绿；③冒烟 duty_lead 200 拿回原文 / soc1 403 穿透实证，web 108→110 只增不减；容器 ：5173 烤旧配置 restart 吃不到，待 web 镜像重建一次生效，详见票 55 实现记录③）**。位置：`services/web/vite.config.ts:47-58`（proxy 转发表没有 `/api/v1/pii` 前缀行）。现象：案件页"反查 PII"按钮调 `api.revealPii`，走同源相对路径 `POST /api/v1/pii/reveal`，经 vite dev 代理（:5173）时无匹配规则、请求落在 vite 自身返回 **404 空 body**——浏览器里按钮必失败，且前端 `request` 基座把它打包成 `ApiError(404,"unknown")`，`revealErrorText` 的 404 分支误报为"映射表里查不到这个占位符（可能来自 mapstore 建立之前的脱敏）"（代理断线的 404 与 agent 的 `placeholder_unknown` 两种 404 共用一个文案）。对照组：直连 agent `:3003/api/v1/pii/reveal` 同参数 200 正常（6-3/6-4 实测）。为什么测试没抓住：web 页面测试跑 jsdom，`fetchMock` 直接拦截 `/api/v1/pii/reveal` 回 200（`pages.test.tsx:516-527`）——测试替身把代理层整段绕过，单元全绿、走线断裂。修法：proxy 表补一行 `"/api/v1/pii": { target: agent, changeOrigin: true }`（与 auth/chat 同款）；建议同时补一条端到端走线冒烟（对 :5173 全部代理前缀各发一发、断言非 404）。教学已入 6-4.md 站 3 与捣乱 B。**修复回填（2026-09-10，票 55）**：修法已落地并升级为静态对账单测防再犯（比一次性冒烟更 CI 友好，照票内推荐）；6-4.md 文末备注（照 2-2 票 50 先例，正文零改动）；学习日志已记。
  - **P3 · 192.168 段内网 IP 脱敏时被误标为 `<PHONE_NUMBER>`**（2026-09-10，6.1 捣乱实验 B 发现）→ **已开票 #56（2026-09-10，修复串行派发中）**。位置：`services/guards/pii.py:44-46`（Rfc1918ExemptIpRecognizer.analyze 只过滤自家 IpRecognizer 的结果）+ Presidio 内建 PhoneRecognizer（score 0.4，点分隔凑出 3-3-4 电话形态）对 `192.168.1.100` 抢命中。现象：RFC1918 三段中 `10.x`、`172.16-31.x` 存活，唯独 `192.168.1.100` 被替换成 `<PHONE_NUMBER>`，原文（带前导空格）还进了 mapstore——违背决策 #8"内网 IP 是资产标识不脱"口径。修法（择一/组合）：anonymize 主函数在 `_drop_overlaps` 之后对**最终实体清单**再过一遍 RFC1918 过滤（豁免上移到清单层）；或给电话识别器加排除 IP 形态的负向前瞻。教学已入 6-1.md 捣乱 B。**修复回填（2026-09-10，票 56）**：方案 a 已落地——anonymize 在 `_drop_overlaps` 之后、mapstore record 之前对最终清单按 span 锚定匹配再过一遍 RFC1918（不问 entity_type，谁抢到内网 IP 的座都作废；识别器注册结构零改动）；四用例（三段存活+公网照脱）+ mapstore 不入账测试锁死，guards 26→28 只增不减、ruff 绿；6-1.md 文末备注（照 2-2 票 50 先例，正文零改动）；学习日志已记；guards 容器烤旧镜像，live 终验待主窗口 `docker compose build guards && up -d guards`（详见票 56 实现记录）。
  - **P3 · vector-store 检索 score 注释口径与实现不符**（2026-09-10，3.4 教具实测发现）→ **已开票 #51 并修复（2026-09-10，①注释口径如实[排序单调/可为负/不承诺余弦]+护栏选项 a 零行为变更；b/c 记票留生产化；定向 31 用例绿+教具复跑逐位一致）**。位置：`services/agent/workers/knowledge/vector-store.ts:39-40`（注释写"cosine 相似：越大越近…取 1-distance"）vs 真容器集合实际建在 **L2 距离空间**（chroma 集合配置 `"space":"l2"`，见 3.2 实测 collection JSON），`score=1-distance` 可为负（实测 -0.949）。影响：排序语义不受影响（越近 score 越大），但 score 数值不可按余弦解读；叠加 **top-k 无分数门槛**——库条目少时任何查询都"命中"全部条目，而 FakeTriageLlm R1 只看 kind 不看 score（llm.ts:71），"命中≠相关"。修法（择一/组合）：注释改口径如实说 L2；查询侧加 score 阈值；R1 加命中条目与告警主体（host）一致性校验；或切语义 embedding 模型。学习阶段不动代码，生产化路线已写进 3-4.md 站 6。
  - **P2 · guards 宕机时 tool_output 通道静默 fail-open**（2026-09-10，2.2 捣乱实验发现）→ **已开票 #50 并修复（2026-09-10，方案 a：两消费点显传 failMode:"flag"，details score/reason ?? null 兜底；门禁 486+3sk；停机冒烟 4 条打标留痕 reason=guards_timeout[票43 口径]，在线行为回归不变；2-2.md 已补备注；agent 容器已重建含修复）**。位置：`services/agent/src/guards-client.ts:47-49`（failMode 默认 block，不可达返回 `fail_closed`）+ 消费点只认 flag 分支：`workers/investigation/flow.ts:229-240`（observe）、`workers/enrichment/flow.ts:175-185`（同款）。现象：guards 停机后直拉 case_flow，工具输出（含真注入样本）不打标、不审计、无降级痕迹，run 照常 completed（A/B 实测：同案在线 1 flag / 停机 0 flag）。对照组：triage（flow.ts:99）与 chat（flow.ts:10）的 prompt 侧通道都显式处理 `block/fail_closed`，仅工具输出通道漏。修法二选一：tool_output 消费点显式传 `failMode:"flag"`（不可达→flag+reason，保留打标语义）；或消费点显式处理 `fail_closed`（记 guards_unreachable 降级审计）。边角：flag 路径取 `decision.score`，failMode=flag 不可达时 score 为 undefined，一并处理。
