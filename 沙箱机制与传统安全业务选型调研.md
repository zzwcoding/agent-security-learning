# 沙箱机制与传统安全业务选型调研

> 生成:2026-08-31。基于路线 2「堡垒」阶段 19-32 的实测与精读(lessons 0019-0028、attack-validation 四次攻击验收证据)。
> 用途:安全公司开发智能化传统安全业务的 agent 时,沙箱层的选型参考。与《Agent开发分层与语言选型.md》《LLM-Agent安全学习路线规划.md》同档。

## 〇、一分钟结论

- 沙箱选型由两个变量决定:**执行内容的可信度**(可枚举 ↔ 不可信代码)× **agent 决策的自主性**(chain 固定流 ↔ graph 自主循环)。
- 核心原理:**攻击面 = 你暴露给不可信代码的代码量**。所有机制的差异,全在暴露什么、砍什么。
- 一句规则:**ReAct 循环每多一跳自主性,执行面就往隔离光谱右端挪一档**;能用 chain 的业务别用 graph——省下的不只是 token,是整个沙箱层的复杂度。

## 一、决策框架

1. **执行内容可信度**(lesson 0027 判据):任务面可完全枚举(命令形态固定、代码全自家)→ 轻隔离够用;执行面不可枚举(任意 shell)、跑不可信代码、碰敏感数据 → 硬边界必须上。
2. **决策自主性**:chain 的行为可预测 = 决策面可信;graph/ReAct 的"下一步"由模型现场决定 = 决策面本身是不可信输入。
3. **威胁模型假设强度**(lesson 0027):先审自己的假设("被批准的代码不作恶" vs "VM 里的一切都是恶意的"),再选边界。假设越狠,需要的边界越硬。

## 二、四种机制的"性格"档案

| 机制 | 隔离原理 | 性能/兼容 | 失守后果 | 性格 |
|---|---|---|---|---|
| **进程级白名单** | 枚举可执行档位 + `shell=False` + `python -I`;无内核边界 | 零开销、完整兼容 | 打穿即宿主 UID 全暴露 | 管得住"能跑什么",管不住"跑的东西自己作恶" |
| **加固容器** | namespace/cgroup + 权限捆死(非 root/只读 fs/降 cap/断网) | 原生性能、重 I/O 友好、镜像生态成熟 | 共享内核,逃逸面=整个内核 syscall 史 | 纵深全靠"捆死权限",内核 0day 同归于尽(阶段 24 实测) |
| **gVisor** | 用户态内核 Sentry 接管全部 syscall + Gofer 文件代理 | syscall 密集降速明显、兼容性减法 | 逃逸面=Sentry/Gofer 代码量 | **行为会被改写**——对安全业务是致命特性(见 §3.2) |
| **microVM** | 独立内核 + 硬件虚拟化 + 极简设备模型 | 亚秒冷启动、接近原生 | 逃逸面=VMM 本身 | root 满血但宿主零可见、一次性、egress 网关可编程 |

隔离光谱(lesson 0028):`进程 < 容器 < gVisor < microVM`,隔离强度 = 逼攻击者穿过的代码量。

## 三、传统安全业务逐个映射

### 3.1 渗透测试 / 红队自动化 agent → **microVM + CIDR 级 egress 白名单**

业界已验证:XBOW 2025 年登顶 HackerOne 全球榜首(1060+ 有效提交)。这类 agent 必须能攻击,不能"断网保平安",正确用法三件套:

- **每任务一次性 microVM**:攻击工具执行面与公司内网/客户资产隔开——agent 自己先得劫持无效化;
- **egress 白名单 = 授权范围**:microsandbox 网络规则支持 `Destination.cidr`(阶段 23 验证过)——**渗透授权书直接编译成网络策略**,注入后打范围外目标 = 网关拒绝,合规边界从合同条文变成机器强制;
- **全程五要素审计**(阶段 29 字段标准):攻击动作可复盘是渗透报告的法律级要求。

### 3.2 恶意样本分析 / detonation → **microVM(且必须是它)**

VMRay/Any.Run/Joe Sandbox 整个行业建在 disposable VM 上,2026 年 SOC 自动化平台把动态沙箱当标准接入口。

- 一次性 + 独立内核:样本是纯不可信代码,且有内核级逃逸意图(容器/VM 逃逸 PoC 是样本家族之一);
- 快照/回滚:带网/断网各跑一遍的行为对比,亚秒冷启动让重放成本趋近零;
- **为什么不是 gVisor**:样本分析要求**行为保真**,gVisor 的本质是"改写行为"(syscall 被接手、冷门调用不支持),恶意软件最爱检测沙箱痕迹——**gVisor 的优点(行为拦截)在这个业务里恰好是缺点**。全部映射里最鲜明的一条错配。

### 3.3 漏洞 PoC 验证 / 情报复现 agent → **microVM,断网**

情报团队收到的 PoC 来自不可信来源(GitHub/TG/客户提交),跑它验证 CVE 真实性 = 阶段 25 复刻的"不可信候选代码验证"同一问题,结论直接搬用:一次性 microVM、白名单置空(等价 `--network none`)、fail closed 协议。进程级白名单不合格:PoC 本身就是不可信代码,"枚举它能跑什么"无从谈起。

### 3.4 SOC 告警分诊 / 安全运营 agent → **路线 2 配套全家桶 + detonation 外包**

2026 年最热落点(Dropzone AI、Torq 等 agentic SOC)。分诊 agent 本身跑可信编排代码,沙箱需求最易被高估,真正要的是三件事:

- **富化 API 凭证**:VT/MISP/云 API key 全套走凭证代理 + 域名策略表(= 主线阶段 26/27 原样照搬:占位符 + `{{SECRET:}}` + fail closed),被注入也偷不到 VT key;
- **告警内容 = 攻击者可控输入**:告警里的邮件正文/URL/文件名全是攻击者写的,注入直接长在数据里——输入护栏 + "注入得手后拿不到东西"整套是刚需;
- **可疑附件 detonation**:不要自己做,委派给 3.2 的 microVM 沙箱服务。加固容器/gVisor 在此业务的出场方式:**不跑不可信代码,只处理不可信数据**(解析器 0day 炸了也是容器内非 root 限损)。

### 3.5 代码审计 / SAST 分诊 agent → **按代码来源二分**

- 审自家代码、跑构建验证 finding → **加固容器**:重 I/O 编译负载(gVisor 明显拖慢)、代码内部可信、非 root + 只读 fs 够用;
- 审外部提交(赏金分流/供应链审计)→ 提交物升级为不可信代码,构建/测试面进 **microVM**。判据是内容可信度,不是业务名字。

### 3.6 SOAR 剧本执行 / 合规检查 agent → **进程级白名单足够**

固定剧本任务面完全可枚举,枚举每个动作档位 + `shell=False`,几行代码零开销。安全公司大量"伪 agent 需求"落在这档——用 chain 不用 graph,决策面可预测是省沙箱的本钱。

### 3.7 应急响应 / 取证 agent → **轻沙箱 + 审计完整性为主角**

证据是被保护对象而非威胁,取证工具是自家可信代码——进程级/加固容器足矣,只读挂载保证证据不可篡改。业务重点在**审计链完整性**(谁在何时以何理由读了哪份证据),哈希链日志是路线 3 第一课,取证场景是最好的落点。

## 四、Chain vs Graph:自主性换沙箱档位

| | chain(固定流) | graph(自主循环) |
|---|---|---|
| 决策面 | 可枚举 | 不可枚举(每步模型现场决定) |
| 沙箱档位 | 进程级白名单 / 加固容器可接受 | 执行面必须往光谱右端走 |
| 典型业务 | SOAR 剧本、合规检查、报表 | 渗透 agent、样本分析、漏洞验证 |
| 数据资产侧 | 无持久记忆或只读 | checkpointer/memory 持久化 → Presidio 脱敏 + 审计必须跟上(阶段 28/29) |

## 五、沙箱之外的配套(沙箱不是安全架构替代品)

gVisor 安全模型页原话:*"A sandbox is not a substitute for a secure architecture"*。安全公司的 agent 产品要过客户安全评审,四条验收(阶段 31)就是现成卖点:

| 配套 | 主线落点 | 安全业务对应 |
|---|---|---|
| 凭证代理(占位符/域名策略) | 阶段 26/27 | 客户 API key 不进 agent 进程 |
| egress 白名单(+CIDR) | 阶段 23 | 数据外发策略化、渗透范围机器强制 |
| OTel 五要素审计 | 阶段 29 | SOC2/等保的追责要求 |
| 记忆落库脱敏(Presidio) | 阶段 28 | 客户 PII 不落 agent 记忆 |
| 输入护栏 | 路线 1 | 告警/邮件内容里的注入 |
| 四次攻击验收 | 阶段 31 | **对自家 agent 定期 BAS,本身可产品化售卖** |

## 六、总表

| 传统安全业务 | agent 形态 | 沙箱机制 | 一句话理由 |
|---|---|---|---|
| 渗透/红队自动化 | graph | **microVM** + CIDR egress | 要能攻击,范围=网络策略 |
| 样本分析/detonation | graph | **microVM**(快照+一次性) | 行为保真要求独立内核;gVisor 会失真 |
| PoC 验证 | chain/半自主 | **microVM 断网** | 不可信代码,阶段 25 结论直接复用 |
| SOC 分诊 | chain+graph | 容器跑编排 + 凭证代理/白名单;detonation 外包 microVM | 分诊代码可信,危险动作外包 |
| SAST/代码审计 | chain | 加固容器(外部提交则 microVM) | 重 I/O + 内容可信度二分 |
| SOAR 剧本/合规 | chain | **进程级白名单** | 任务面可枚举,零开销够用 |
| 应急响应/取证 | chain | 进程级/容器 + 只读挂载 | 重点在审计链完整性 |

## 七、信源

- 业界事实:XBOW 登顶 HackerOne(Dark Reading,2025-06;2026 年 1060+ 有效提交);VMRay《Best SOC Automation Tools 2026》(动态沙箱接入 SOC 自动化);Dropzone AI / Torq / Conifers.ai(agentic SOC 平台盘点);AgentSOC 多层 agentic 框架(arXiv 2604.20134)
- 精读:Firecracker design doc("does not perform any network traffic filtering")、gVisor 架构指南(intro/platforms/security;systrap 2023 起默认)
- 本项目实测:阶段 21-24 microsandbox 探针、阶段 24/25 加固 Docker vs microVM 边界对照、阶段 31 四次攻击验收(`.scratch/agent-security-learning/issues/09-route2-execution/attack-validation/`)
