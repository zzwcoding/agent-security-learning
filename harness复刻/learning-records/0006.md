# 0006 学习记录:隔离回放引擎(阶段 45 复刻·6)

- **日期**:2026-09-02
- **学了什么**:回放引擎四件套——_load_gate(exec 进干净命名空间,每次加载都是新实例、_pending 互不相通)/_check_contract(三接口 duck typing,先验形状再跑行为)/_make_executor(注入假世界+calls 账本)/_replay_case(备票三式 confirm/confirm_for/use_token=previous;双重核对:状态比对+账本比对)。STABLE 按 importlib 路径加载,稳定版与候选同为"被加载的文本"。
- **卡在哪**:validate_candidate 引用后文函数(load/contract 定义在后面)——Python 调用时解析,无碍;_replay_all 推迟到阶段 7(随正式用例集一起进)。
- **结论**:"挂起绝不执行"从口号变断言靠账本核对(信日志别信口供,呼应主线 44 审计三面);已代跑捣乱实验——放行一切的迷你门禁被引擎抓获(False,'期望 pending_confirmation,实际 executed'),阶段 8 反例原理已预告。demo 阶段 5 段显示改为"打印当前通过项",随阶段自然生长(注释轮换原则的显示版)。
- **产物**:evolution.py(_load_stable_dispatcher+STABLE/_load_gate/_check_contract/_make_executor/_replay_case + validate 接 gate_contract)、demo.py 阶段 6 段、lessons/0006
- **验证**:demo 五行——前三项 ✓/缺签票函数卡契约/无票 0→0/持票 0→1/伪造票与错绑票两用例 ✓。
