# 0003 学习记录:确认门禁本体(阶段 45 复刻·3)

- **日期**:2026-09-02
- **学了什么**:门禁三岔路口(classify → 低风险直行/无票 pending/验票 executed 或 rejected);指纹 = canonical JSON(sort_keys)+ SHA-256 绑定"工具名+完整参数";单次性靠 `_pending.pop` 取出即作废;验票必须 `hmac.compare_digest` 防时序(与主线阶段 42 任务票同款常识)。
- **卡在哪**:教学顺序决策——参考项目把门禁源码嵌在 evolution.py 的 GATE_TEMPLATE 字符串里由管线"生成",复刻改为独立文件 confirmation_gate.py 先手写跑通,阶段 4 提案打包时把它读成候选源码。与参考的 diff(内嵌模板 vs 读文件)记为有意改进,收官时核对。
- **结论**:execute 注入设计(门禁不碰工具)是阶段 6 隔离回放的成立前提,先在这里点题;参考模板 token 是确定性 HMAC(同操作同票),阶段 12 企业版换 secrets 随机票,留作对照伏笔;进程内门禁可被破进程者绕过(自签票),与主线"网关 FGA 不可省"同一结论。
- **产物**:confirmation_gate.py、demo.py 阶段 3 段、lessons/0003
- **验证**:demo ⓪①②③ 四行——read 直行/无票挂起执行器 1→1/签票放行文件消失/复用票拒绝执行器 2→2。
