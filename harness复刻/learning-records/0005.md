# 0005 学习记录:静态闸(阶段 45 复刻·5)

- **日期**:2026-09-02
- **学了什么**:validate_candidate 六项检查骨架(CHECK_NAMES 与参考一致,本阶段只接管前两项)+ 三道静态关卡(64KB → compile → _safe_ast:_safewalk 查 Import/ImportFrom 白名单 + Call/Name 黑名单);fail closed 的实现 = checks 初始化全 False + 就地 return。
- **卡在哪**:签名决策——validate_candidate 暂只收 candidate_source,boundary/retention 两个参数推迟到阶段 7 再扩(守"不超前");CHECK_NAMES 一次定齐是为了与参考最终形态对齐,demo 里明示后四项"尚未验收"。
- **结论**:白名单(导入)管主防线、黑名单(eval 等)做兜底;AST 比正则强的点在字符串陷阱——`x = "import os"` 树上只是常量(已实测 True);静态闸只预筛,真保证在阶段 6 execute 注入。已交互验证四个 _safe_ast 断言全中。
- **产物**:evolution.py(_safe_ast/validate_candidate/CHECK_NAMES/ALLOWED_IMPORTS/FORBIDDEN_CALLS/MAX_SOURCE_BYTES)、demo.py 阶段 5 段(三坏提案梯度 0/1/1)、lessons/0005
- **验证**:demo——我们的门禁过 static_compile+security_scan,三个坏提案分别卡编译/安全扫描。
