# 0009 — 阶段 9 简记(2026-09-01)

**学了什么**
- pipeline.py:regex 先行 → LLM 在脱敏后文本上补扫 → 验收回填,顺序逐层替换(比参考项目的 span 回映射更简,语义等价)。
- schema enum(PII_TYPES 12 类中文)修掉阶段 8 挂账:type 有了区分度,占位符 [REDACTED_住址] 可读;名单故意不含格式型类别(regex 地盘)。
- 占位符污染防御:LLM 补扫的是已脱敏文本,可能把 [REDACTED_*] 检出且能过子串闸——startswith("[REDACTED") 一律不收。

**路线中途修正(本复刻最重要的一次实测推翻设计)**
- 交接文档原策略"regex 零命中才进 LLM"(省成本)被捣乱样本当场推翻:住址+手机号混合日志,regex 命中手机号 → LLM 没跑 → **住址原样泄露**。
- 修正为参考项目 campaign.py:216-226 的真实形态:LLM 在 regex 脱敏后文本上**总跑一遍**(补位不替补)。安全场景保完备 > 省钱(多 ~2-4s/条)。
- 教训:排班策略要拿最刁的组合样本考,不是各自的单类样本。

**关键观察(运行实证)**
- 修正后住址混合样本回归通过:双占位符、类型准确。
- 延迟数量级:regex 0.07-0.2ms vs LLM 2000-4500ms(4-5 个数量级)。
- 负例 1 在管道下:regex 误报脱错 + LLM 补扫又给 1(补扫救不了 regex 已脱错的)——排班边界,阶段 10 量化 utility。
- 非确定性再现:干净基线一次给 1 项误报、复跑 0 项(GPU 浮点),LLM 误报形态=语义猜疑且不稳定。

**卡在哪**
- main.py Edit 一次 old_string 不匹配(阶段 7 注释文本记错),Read 后修正。

**下一步**:阶段 10 campaign 基准——自造 gold 用例集(含负例与语义类),三引擎(regex/LLM/hybrid)对比 exact-span P/R、残余泄露、utility、延迟。
