# 0001 — 阶段 1 简记(2026-08-31)

**学了什么**
- 读完参考项目核心 6 文件(README/regex_sanitizer/samples/config/agent/campaign)+ main.py 前 100 行,架构心法:分层管道——确定性规则打第一线,语义盲区交端侧 LLM,LLM 检出值必须过"原文出现"确定性验收;最后 gold 基准量化三引擎地盘。
- 建项目骨架(MISSION/RESOURCES/NOTES/lessons/learning-records),编号从 0001 起本目录独立。
- 自造 10 条中文 Agent 日志样本:密钥类 4(请求头令牌/连接串+AWS/CI 令牌/JWT+PEM)、PII 类 3(客服对话/CRM 回显/支付日志)、负例 3(纳秒延迟/口令规范语言/干净基线)。结构 (name, category, text) 比参考多了分类字段——阶段 1 打印用,阶段 10 campaign gold 按它组织。

**关键决策**
- 样本分类字段是复刻自造设计(参考项目 SAMPLES 是二元组):分类既是阶段 1 的可观察输出,也是后面 gold 标注的组织维度。
- `13800138000` 一串两用:客服对话里是真手机号、性能日志里是纳秒——同一文本形状的语义反转,留给阶段 3(校验器救不了它)和阶段 6-8(LLM 该救它)验证。
- 纯 stdlib 零依赖,不用 venv,`python3 main.py` 直接跑(规则引擎特性,教学上少一层环境噪音;Ollama 阶段再说 venv 的事)。

**卡在哪**
- 无。主线 memory_guard 无独立测试语料文件(直接嵌在 agent.py 主流程),同风格理解为"中文客服/记忆对话 + CN 手机号/身份证"场景,样本按此自造。

**下一步**:阶段 2 规则引擎上(密钥类 11 条规则 + 带标签占位符),before/after 打印。
