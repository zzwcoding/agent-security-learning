# 0004 — 阶段 4 简记(2026-08-31)

**学了什么**
- PII 类 5 条补齐(email/iban/us_ssn/cn_phone/ip_address),规则引擎 18 条完工。cn_phone 的环视断言 `(?<!\d)1[3-9]\d{9}(?!\d)` 与主线 memory_guard.py cn_mobile 逐字同款——阶段 11 对照的现成锚点。
- Counter 汇总(每类命中数)= 审计报表雏形。

**关键观察(运行实证,合计 15 处)**
- **负例 1 显威**:latency_ns=13800138000 被 cn_phone 误报(命中 1 处)——阶段 1 "一串两用"伏笔兑现。定性:规则引擎的能力边界,不是 bug;三种归宿 = 阶段 10 量化 precision / 阶段 9 混合管道 LLM 补 / 运维接受(宁保守)。
- email vs url_credential 重叠第二次实证:dsn 里 S3cr3t_P4ssw0rd@db.internal 被 email 候选命中,连接串规则优先级高先占坑,裁决弃权。
- 样本 9(口令规范语言)0 命中:secret_assignment 要求键后紧跟 [=:],自然挡住规范语言;无需特判。
- IBAN 0 命中:样本集没有 IBAN(有意,留给阶段 10 campaign 的 financial 用例考)。

**卡在哪**
- 无。阶段 1 样本 2 没放 IP,本阶段补了 host_ip 一行让 IPv4 规则有观察点。

**下一步**:阶段 5 离线回归 pytest(截断 PEM/带空格口令/Basic 不吃普通英文/负例不误报)——规则是资产也是负债,回归护住它。
