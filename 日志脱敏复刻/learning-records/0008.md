# 0008 — 阶段 8 简记(2026-09-01)

**学了什么**
- `_value_appears_in_text`(大小写不敏感子串谓词)+ `accept_and_redact`(过闸替换/拒收带原因;长值先替换防破坏;re.escape 防元字符)。模型输出从"指令"降级为"建议"。
- 数据流闭环纪律:初版 redacted 生成后没打印(有写无读),自查补上。

**关键观察(全部实测)**
- **检出 ≠ 脱成**:样本 7 的 SSN 项检测正确但 value 带 /think 碎片 → 拒收 → **原值留在文本(残余泄露的一种来源)**。阶段 10 residual_leaks 量化。
- **子串 ≠ 干净**:'card: 4111...' 过闸但键名一起被替换。彻底解法 = LLM 给 span 或格式型值不走 LLM(阶段 9 采纳后者)。
- **端侧推理非严格确定**:样本 6 同 prompt 同 seed 两次运行 0 项 vs 2 项(GPU 浮点归约顺序)。阶段 10 基准解读要带此认识。
- **type 字段无区分度**:0.6B 全输出"敏感值",占位符变 [REDACTED_敏感值]——改进挂账:schema 给 type 加 enum(阶段 9+)。
- 幻觉演示 1 收 2 拒符合设计;main.py 旧注释 Edit 不匹配一次(记错注释文本,读文件后修正)。

**卡在哪**
- re.sub(r'\\s+'...) 写成双反斜杠(匹配字面反斜杠),自查修正为 r'\s+'。

**下一步**:阶段 9 混合管道——regex 先行,规则零命中才进 LLM;结果合并去重;每条样本打印"走了哪条路"。顺带:schema 给 type 加 enum 修占位符质量。
