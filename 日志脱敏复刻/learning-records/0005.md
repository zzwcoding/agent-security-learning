# 0005 — 阶段 5 简记(2026-08-31)

**学了什么**
- pytest 回归 7 条:截断 PEM 到 EOF / 普通 BEGIN 一词不碰 / 引号口令空格整值脱 / Basic 凭据头脱+英文散文与 WWW-Authenticate 挑战头不脱 / 校验器真假各半 / 负例两档区分 / 环视防截断。0.01s 全绿,纯离线。
- venv 惯例落地:`uv venv .venv` → `uv pip install --python .venv/bin/python -r requirements.txt`(venv 里没 pip 模块)→ `.venv/bin/python -m pytest`。requirements.txt 从本阶段起存在(此前纯 stdlib)。
- 根 .gitignore 补 `.pytest_cache/`(.venv/ 原已有)。

**关键决策**
- 负例分两档进回归:口令规范语言/干净日志 = 必须干净,锁进测试;纳秒延迟 = 已知误报,故意不锁(阶段 10 量化 precision,锁成干净=撒谎)。
- 参考项目 conftest 还 stub 了 ollama/dotenv(它的 tests 引 agent.py);本项目引擎纯 stdlib,conftest 只挂 sys.path——复刻的依赖面比参考小,是有意为之。

**卡在哪**
- 无。7 个测试一次全绿;断言值全部先在运行/实测里验证过再写。

**下一步**:阶段 6 Ollama 起步(路线 4 热身)——`brew install ollama` + `ollama pull qwen3:0.6b` + 裸 `/api/chat` 冒烟,第一次体感 0.6B 能力边界。
