# 0006 — 阶段 6 简记(2026-09-01)

**学了什么**
- Ollama 0.33.0 经 brew 安装、brew services(launchd)托管常驻;qwen3:0.6b 拉取成功(~500MB)。全局环境,路线 4 复用。
- 裸 /api/chat 冒烟:流式 = 逐 token JSON 行;统计字段 total_duration/prompt_eval_count/eval_count(阶段 7 TTFT 指标来源)。

**关键观察(0.6B 第一次体感)**
- 中文指令跟得住:手机号提取正确,1.93s 总耗时(prompt 46 tok/输出 248 tok)。
- **能力边界 ①:输出包 markdown 围栏**——"只输出 JSON"约束不住小模型,阶段 7 必须用 format schema 在解码层强制(不是提示词求它)。
- **协议形态:qwen3 思考模式**——message.thinking 与 message.content 分字段(参考项目时代的 <think> 文本标签已成历史);248 输出 token 大头是思考,阶段 7 解析要跳过 thinking。
- 0.6B 自我介绍的回答"热爱探索未知的青年"——通用客套,无个体感:小模型生成质量够脱敏检测用,不够当门面。

**环境决策**
- brew services 托管(非会话后台任务):Ollama 是全局常驻环境,路线 4 直接复用,重启机器自动拉起。
- 协议学习选裸 curl(不装 ollama Python 包):贴协议本质,阶段 7 用标准库 urllib 调,依赖面保持最小。

**卡在哪**
- 无。brew 安装→services 启动→pull→冒烟一条龙顺利。

**下一步**:阶段 7 LLM 引擎——Level 3 PII 系统 prompt + JSON Schema format 结构化输出 + 流式 + TTFT/吞吐指标(标准库 urllib)。
