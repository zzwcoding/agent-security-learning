# 0007 — 阶段 7 简记(2026-09-01)

**学了什么**
- llm_engine.py:标准库 urllib 流式调 /api/chat;format=schema 解码层强制 JSON(阶段 6 围栏问题的物理解);TTFT/吞吐计时;temperature 0.1 + seed 42(可复现,阶段 10 基准前提)。
- Level 3 PII 系统 prompt 中文化(参考项目英文版的有意改进:qwen3 中文强,样本也中文);value 逐字摘抄要求写进 prompt。

**关键观察(0.6B 诚实画像,全部实测)**
- **召回不稳**:样本 5 漏手机号(规则抓 2 它抓 1);样本 6 整条 0 检出(思考 384 字后说没有)。
- **value 边界不净**:样本 7 检出 'card: 4111...'(键名混入)与 'holder_ssn: 123-45-6789 /think'(混入思考标签碎片,**已非原文子串**)——阶段 8 验收闸的完美素材:prompt 三令五申"逐字"也没用,防线必须在下游。
- **冷加载支配延迟**:TTFT 1717ms(含模型加载)→ 热 58-131ms。
- **LLM 主场实证**:住址"上海市浦东新区桂花路333弄12号502"整段检出——规则引擎在此类别永远 0 分。

**卡在哪**
- 无。schema 强制下 content 全部合法 JSON(TRY/except 未触发);/think 碎片是模型在 value 里自带的文本,不是解析问题。

**下一步**:阶段 8 回填验收与失败模式——`_value_appears_in_text` 原文出现检查;拿今天的 'holder_ssn: ... /think'(非子串,拒收)和 'card: 4111...'(是子串但含键名,引出"子串≠干净"的更深讨论)当真实教材;造"描述性前缀"失败样例演示拒收。
