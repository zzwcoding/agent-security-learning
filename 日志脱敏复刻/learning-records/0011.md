# 0011 — 阶段 11 简记(2026-09-01,收官)

**学了什么**
- 跨项目对照姿势:借主线 .venv 解释器跑本项目的对照脚本,sys.path 挂主线目录 import memory_guard(只 import 勿改);适配层 3 行(消息结构包装)。
- Presidio 画像(同卷实测):CN 格式 PII/邮箱/信用卡全中(自定义识别器立功);密钥类与语义类全零(实体清单没有);误报形态=纯形状咬数字(SKU→PHONE_NUMBER)+错标签(护照→PERSON,歪打正着脱掉但证据弱)。**NER 错标签,LLM 错内容,regex 错形状**——三家的失败模式互不重合,恰是分层的依据。

**收官交付物**
- `对照复盘-三引擎分工拍板.md`:四方成绩单 + 五维必答 + 三出口拍板(memory.json=Presidio 保留;trace/本地日志=regex 在线全量 + hybrid 离线补扫)。主线阶段 33 可引用。

**收官自查**
- pytest 7 passed(规则引擎收官时仍绿);Ollama 环境保留(路线 4 复用,手动档);主线 MISSION 阶段 30 行已勾。

**本复刻三条最重要的可迁移结论**
1. 分层是"补位不替补":regex 在线打全量,LLM 离线补语义——"零命中才进"的省成本设计被混合样本推翻(阶段 9)。
2. 坐标可验证,主张要审讯:NER 的 span 天然可验收,LLM 的 value 必须过子串闸,且闸不保召回(拒收=残余泄露)。
3. 基准第一作用是暴露失效模式而非排名:占位符幻觉磁铁、SKU 咬电话,全靠 campaign 揪出来。

**下一步**:无(11/11 完工)。收官 commit 前缀 `复刻收官:`;主线阶段 33 收官引用对照表;路线 3 挂账:把本复刻结论移植成 TS 日志脱敏中间件(NOTES.md)。
