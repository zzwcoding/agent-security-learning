# 学习简记 0007:阶段 7 — 容器内语义检查(下)

**日期**:2026-08-31
**学了什么**:
- "必须变"与"不许变"成对:failure_replay(失败别再试)对面站着 temporary_recovery(该重试的还能试)+ old_task_regression(旧场景行为快照:3 次封顶/5 次熔断)。只考新行为会放过"把重试整个禁掉"的偷懒补丁。
- canary_ready 防过拟合:考数据里没出现过的 AUTH_DENIED(表内永久码,即便标 retryable 也要拒+即熔断),确保补丁学会规则而非背题。
- rollback_ready:稳定版也在容器里 exec + 行为体检(VERSION 1.0.0 + 老行为)——回滚不是"有备份",是"备份被验证过"。发布决策敢大胆,因为退路是通的。
- 测试设计:全部是硬编码"输入→期望输出"断言,无模糊地带;检查代码住在容器内 runner(可信根),不放在可被流程修改的一侧。

**卡在哪**:无。

**结论**:十格灯表全绿(容器原始回传七项全 true)。下一步(阶段 8):release_manifest——发布决定模型外做 + 十项检查/哈希/diff/影响预测全量落盘 manifest。

**产物**:sandbox_runner.py(_temporary_recovery + 下四灯)、demo 全绿输出、lessons/0007
