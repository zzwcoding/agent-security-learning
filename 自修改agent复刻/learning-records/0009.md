# 学习简记 0009:阶段 9 — 可信根自证

**日期**:2026-08-31
**学了什么**:
- 可信根 = 6 文件名单:stable(病人)/ trajectories(病历)/ evolution.py(主裁判)/ candidate_sandbox.py(狱吏)/ sandbox_runner.py(阅卷人)/ Dockerfile.sandbox(病房图纸)。全是验证与依据侧,没有一个被验证对象。
- 最毒攻击是改裁判:runner 改成无条件全 True,验证体系给垃圾补丁盖章,比没有验证更危险。防御 = 开工前/收工后 SHA-256 快照对比(trusted_surfaces_unchanged),口说无凭,密码学证据才值钱。
- 证据也要按手印:diagnose 给每条立案轨迹算 trajectory_sha256 进 manifest;repr(sorted(items)) 规范化后再哈希,内容相同则指纹相同(键序/空格无关)——canonical JSON 思路。
- "先确认世界还是你以为的样子"第三次出现:阶段3 恰好匹配一次 / 阶段5 内容寻址 / 本阶段封条对比。
- 补齐阶段 2/5 延期的轨迹哈希(diagnose.source_trajectories + manifest 字段),数据流闭合。

**卡在哪**:无。

**结论**:实测六哈希前后一致(trusted_surfaces_unchanged=True)。下一步(阶段 10):llm_generator 接 MiniMax(OpenAI 兼容),Keychain 注入真 key,.env 假密钥;LLM 输出只写 validation/<run>/candidates/,过同一组门槛。

**产物**:evolution.py(source_trajectories+trajectory_sha256、manifest 增字段)、demo(快照/对比/逐文件 ✓ 打印)、lessons/0009
