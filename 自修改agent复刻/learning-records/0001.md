# 学习简记 0001:阶段 1 — 失败轨迹与病灶

**日期**:2026-08-31
**学了什么**:
- 实验 9-6 的立场:出了"不该重试却在重试"的事故,修控制代码而不是往 prompt 加嘱咐——prompt 是劝(可被稀释/覆盖),代码是闸(落下就落下),且只有代码改动可被系统验证。
- 失败轨迹的数据形态:每条 = {id, tool, error_code, retryable, attempts, outcome, evidence}。重试次数 = attempts - 1。
- 病根两处(`stable/retry_policy.py`):`should_retry` 收了 `retryable` 参数不用,只看次数;`should_open_circuit` 不看错误类型,满 5 次才熔断。均为故意埋的 bug,复刻须原样保留。

**卡在哪**:无。零依赖,系统 Python 直接跑。

**结论**:阶段 1 数据流闭合(文件 → 读取 → 打印圈病灶),`python3 demo.py` 输出"重试 3 次"实锤。下一步(阶段 2)把 ≥2 条同模式轨迹聚合成"修改请求"。

**产物**:`stable/retry_policy.py`、`failure_trajectories.json`、`demo.py`(28 行)、lessons/0001
