# 学习简记 0002:阶段 2 — 轨迹聚合与"修改请求"立案

**日期**:2026-08-31
**学了什么**:
- 自我修改需要点火条件:同一 (tool, error_code) 失败模式 ≥2 条轨迹支持才立案,防单次偶发/单点操纵(如注入假轨迹)。
- 立案与开药分离:根因定位("改 stable/retry_policy.py")由确定性代码写死,是运维者划定的管辖范围,不交给模型推断。
- 过滤三条件:outcome=failure 且 retryable=false 且 attempts>1;健康重试(run-timeout-001)被排除且排除名单可见。
- 新技术点:defaultdict(list) 自动开堆,适合键集合不可知的分组。

**卡在哪**:无。参考版 diagnose() 里的 trajectory_sha256 / change_contract 属于阶段 9(manifest/自证)与阶段 8,按"不超前"纪律暂缓,已记入 lesson 0002 诚实标注。

**结论**:数据流闭合(轨迹 → diagnose → 打印修改请求),输出"根因=stable/retry_policy.py"与 HANDOFF 验收一致。下一步(阶段 3):确定性提案,最小 diff 写 output/candidate/。

**产物**:evolution.py(diagnose,~40 行)、demo.py 接线 + 分组判决打印、lessons/0002
