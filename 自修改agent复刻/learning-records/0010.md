# 学习简记 0010:阶段 10 — 真实 LLM 提案

**日期**:2026-08-31
**学了什么**:
- LLM 进场方式即安全设计:提案权(一段文本,只写 validation/<run>/candidates/)与决定权(release_manifest 公式)分离;prompt 里的约束是沟通,模型外门槛才是执法。
- OpenAI 兼容端点:换供应商只改 base_url + api_key;OpenAI SDK 直接用。
- MiniMax-M2 是推理模型:<think> 思考块计入 tokens 且先于正文,_extract_json 要先剥 <think>;max_tokens 放大到 4096;seed/response_format 探针验证均被接受。
- 行为门槛的意义实测:LLM 补丁与确定性补丁写法不同(注释/docstring/版本号 1.0.1 vs 1.1.0)但行为等价,十格灯表同成绩——验证行为不验文本长相。
- 密钥纪律:agent-key(Keychain)→ scripts/run-with-keychain.sh 注入本进程;.env 假密钥进 git 外;.env 已 gitignore;receipt 记 credential_env 名不记值(credential_value_recorded=False)。
- evidence 回执:request/response 全文+双方 SHA-256+usage+耗时;实测 1592 tokens / 9.3s。

**卡在哪**:无。参考项目 provider 表(ark/openrouter/openai)换成 minimax,已在文件头诚实标注两处适配。

**结论**:LLM 提案 release_to_canary,可信根前后哈希不变。validation/<run>/ 未 gitignore(证据留存,与参考一致)。下一步(阶段 11):run_experiment_9_6 验收入口——负对照(禁用临时重试的坏提案)必拒 + 三方同门槛对比 + artifact_hashes。

**产物**:llm_generator.py、scripts/run-with-keychain.sh、requirements.txt、.env(假密钥)、demo --generator llm 分支、validation/minimax_*/{candidates/,evidence.json}、lessons/0010
