# 学习简记 0011:阶段 11 — 验收入口与负对照

**日期**:2026-08-31
**学了什么**:
- 负对照(should_retry 一律 False 的过度修复)必死于配重三灯(temporary_recovery/old_task_regression/canary_ready)——没有阴性对照的验证体系等于没测。
- 死法即教材:rejected_history(失败检查+理由)喂进真实 LLM 的 prompt;隐瞒失败不是安全,把失败变成防御输入才是。
- behavior_metrics 量化:基线故障调用均值 3.5 → 好候选 1.0;但负对照也是 1.0——单一指标可被"把重试全禁"骗过,必须与 temporary_recovery_rate 组合。
- 16 条验收 gate 全布尔化,accepted=all(gates),退出码 0/1;与阶段 8 发布公式同构(验收也是查表不是评议)。
- 证据链:evidence.json + evidence.sha256 + artifact_hashes.json + validation/latest.json 指针;shasum -c 必须在清单所在目录执行(相对文件名)。
- 推理模型实战坑:完整 diagnosis 下 <think> 更长,4096 max_tokens 被思考块吃光 → 正文空、解析炸;修复 = 8192 + finish_reason=="length" 提前报错(fail loud)。

**卡在哪**:踩一坑修一坑,均如实记录:① latest.sha256 校验的相对路径问题;② LLM 截断导致 JSON 空——探针(简化 diagnosis)复现不了,因为 think 长度随 prompt 复杂度涨。

**结论**:实测 deterministic/real_llm 双 release_to_canary(11/11 灯)、rejected_control 必拒(8/11),16/16 gate,accepted True,证据 validation/real_20260831T134438Z/。下一步(阶段 12):收官对照——同一候选同一组检查换 microsandbox microVM 后端 + probe.sh 双跑 + 复盘必答四维对比。

**产物**:run_experiment_9_6.py、evolution.py(+generate_rejected_control/behavior_metrics/candidate_version 字段)、sandbox_runner.py(+metrics 动作)、llm_generator.py(截断防护+8192)、validation/real_*/、lessons/0011
