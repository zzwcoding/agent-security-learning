# 学习简记 0008:阶段 8 — 发布决定与 manifest

**日期**:2026-08-31
**学了什么**:
- 决定公式:accepted = changed and bool(checks) and all(checks.values())。bool(checks) 堵 all() 空表返回 True 的坑;changed=False 也拒("没改不算修复")。
- release_to_canary ≠ 上生产:scope 写死"shadow traffic only; stable remains unchanged",rollback_trigger 预先声明——灰度是缓冲带,越界要新证据。
- manifest 字段即证据链:失败簇/根因/change_contract/diff/三方哈希(rollback_sha256 == stable_sha256,回滚目标无歧义)/patch_size/checks/failed_checks/provenance/decision+rejection_reason。
- 拒绝也落盘(output/rejected_manifest.json):被拒提案与放行提案同权留档,拒绝理由枚举全部未过的灯。
- 补齐阶段 2/3 延期的三块:diagnose.change_contract、candidate_from_source.patch_size + impact_prediction/generator_metadata(生产者与消费者同阶段闭合)。

**卡在哪**:无。行为指标 behavior_metrics 与 runner 的 metrics 动作属阶段 11 验收入口,按"不超前"延后(参考版 manifest 本身也不含 metrics)。

**结论**:demo 实测 release_to_canary(补丁 10 行)与 reject_candidate 并排;两份 manifest 落盘。下一步(阶段 9):可信根自证——stable/验证器/轨迹输入 SHA-256 前后对比,证明流程没越权改动可信根。

**产物**:evolution.py(change_contract/patch_size/impact_prediction/release_manifest/_short_sha)、demo 双决定输出、output/release_manifest.json + rejected_manifest.json、lessons/0008
