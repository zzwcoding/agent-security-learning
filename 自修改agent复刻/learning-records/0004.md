# 学习简记 0004:阶段 4 — 宿主静态闸

**日期**:2026-08-31
**学了什么**:
- 十项发布门槛 CHECK_NAMES 全 False 起步,过一关亮一灯;发布决定(阶段 8)退化为"数绿灯"。
- 静态闸两关分工:compile() 拦"写错的"(语法),_safe_ast 拦"写得太能干的"(一切 import + eval/exec/compile/open/__import__ 调用)。
- 实测:捣乱候选(尾部 import os)static_compile 仍 ✓,被 security_scan 拦——两关互补。
- AST 拒绝列表是纵深防御快筛,不是安全边界(可被 getattr 拼接等绕过);真正边界是阶段 5 沙箱。
- ast 细节:ast.Call.func 可能是 ast.Attribute(如 os.system),判 is(ast.Name) 防 AttributeError。

**卡在哪**:无。validate_candidate 本阶段签名只收 candidate_source,阶段 5 接沙箱时再扩签名(参考版含 trajectories/stable_source)。

**结论**:数据流闭合(候选 → 静态闸 → 灯表打印 + 捣乱测试被拦)。下一步(阶段 5):加固 Docker 容器跑候选(candidate_sandbox.py + Dockerfile.sandbox 形态)。

**产物**:evolution.py(+CHECK_NAMES、_safe_ast、validate_candidate 静态部分)、demo.py(灯表 + 捣乱测试)、lessons/0004
