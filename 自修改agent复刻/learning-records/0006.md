# 学习简记 0006:阶段 6 — 容器内语义检查(上)

**日期**:2026-08-31
**学了什么**:
- 验证升级:静态闸看"代码长相",沙箱语义检查看"行为表现"——失败案例重放是核心手段,确定性提案和将来的 LLM 提案过同一套行为考试。
- public_api_compatible:inspect.signature 逐字符比对——签名是候选与全部现有调用方的契约,补丁无权重谈。
- failure_replay:只收 outcome=failure 的轨迹(超时轨迹是 success,归阶段 7"临时恢复"考卷);对每个失败轨迹的每个尝试序号问 should_retry,要求全答"别试"。老稳定版 7 问 6 答"再试",必挂——检查的可分辨性。
- nonretryable_circuit:should_open_circuit(1, retryable=False) 必须 True;老版 1>=5=False 必挂。
- exec + 独立命名空间字典:同一操作,权限取决于在隔离边界哪一侧;_NullWriter 吸掉候选打印,协议通道不可污染。
- 内容寻址红利:改 runner → 镜像哈希变 → 自动重建,零手工。

**卡在哪**:无。

**结论**:三灯亮(public_api_compatible/failure_replay/nonretryable_circuit),后四灯待阶段 7。下一步:temporary_recovery/old_task_regression/canary_ready/rollback_ready(rollback 要在容器里再 exec 一遍稳定版)。

**产物**:sandbox_runner.py(SANDBOX_CHECKS/_NullWriter/_namespace/_validate 上三项/action 路由)、evolution.validate_candidate 填表、demo 原始回传打印、lessons/0006
