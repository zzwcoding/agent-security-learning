Agent 安全学习笔记
1. MCP 协议把工具调用变成了跨进程的标准接口,server/client 之间是天然的信任边界。
2. ReAct 循环里,模型生成的 tool_calls 只是"意图",真正执行的是框架层——政策检查点就在翻译层。
3. shell 和 fetch 工具裸奔时,提示注入可以直接转化为命令执行和数据外泄。
4. 防御的关键挂载点:协议边界(MCP 网关)、进程边界(沙箱)、数据边界(脱敏)。
5. 多轮对话的"记忆"是客户端把历史消息重新发给模型,模型本身无状态。
