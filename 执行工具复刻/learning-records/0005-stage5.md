# 0005 — 阶段 5:危险命令硬拦截

## 学了什么

- `execution_tools.py` 加 `DANGEROUS_COMMANDS` 黑名单(照抄参考项目 210-215 行)+ 子串匹配,
  命中即拒、子进程不起;教学注释轮换(4→5)
- 黑名单 = 筛子不是法官:零成本但只认字不认意

## 验证(实跑,四种情况)

- `ls` → 放行执行 ✅
- `rm -rf /tmp/xxx` → 拦截未执行 ✅
- `echo 注意:rm -rf 这个参数很危险` → **误伤**,被拦 ❌(上下文盲)
- `rm -r /tmp/xxx`(不带 f)→ **绕过**,放行了 ❌(变体盲;仅因目录不存在没删成)

## 卡在哪

无。两个失败案例是刻意演示的,效果符合预期。

## 结论

误伤+下漏两个死穴实锤,阶段 6 动机充分:命中黑名单后不硬拒,改送二级 LLM 审批
(request_approval,fail-closed)。
