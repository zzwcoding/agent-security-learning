# 0007 — 阶段 7:code_interpreter

## 学了什么

- `execution_tools.py` 加 `DANGEROUS_CODE`(照抄参考项目 116-123 的 python 列表)和
  `code_interpreter`:语法 → 筛子 → 法官 → `[sys.executable, "-c", code]` 列表形式执行
- `cli.py` 加 `code` 子命令;注释轮换(6→7)
- 意外收获:`open(` 在黑名单上导致"读文件"也送审——参考项目同款粗糙,成了现成教具

## 验证(实跑四案)

- 干净词频代码 → 全过,执行成功
- `def broken(:` → 闸 1 拒,带行号
- `os.system('echo pwned-by-code')` → 法官拒绝,理由点破"pwned 像在试探防线"
- `open('hello.txt').read()` → 送审后被批准放行(法官:只读无副作用)——误伤-救回重演
- 无 key 时危险代码 → fail-closed 拒(顺带验证)

## 卡在哪

第一次演示误用了含 `open(` 的"干净"例子,被 fail-closed 拦下——换例子解决,
反而丰富了教学素材。

## 结论

安检流水线成功复用到第二种输入。下一阶段(8):truncate_and_persist 长输出截断+落盘,
接进两个执行工具。
