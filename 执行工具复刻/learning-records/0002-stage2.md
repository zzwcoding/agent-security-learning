# 0002 — 阶段 2:workspace 边界 + CLI 骨架

## 学了什么

- 新增 `file_tools.py`(_resolve + write_file,~22 行)与 `cli.py`(argparse 骨架,~20 行)
- 边界判断:`resolve() + relative_to()`(组件级),替代表述性强的 `str.startswith`
- 复用 starter 的 `config.WORKSPACE_DIR`,config 本阶段零改动

## 验证(实跑)

- `write hello.txt` → success,落盘 workspace/hello.txt
- `write notes/todo.md` → 子目录自动创建(parents=True)
- `write ../escape.txt` → success=false"路径越界";事后确认项目根和 workspace 均无 escape.txt

## 卡在哪

无。

## 结论

第一条安全数据流闭环:CLI → 边界闸 → 磁盘。下一阶段(3):llm_helper 的
`verify_code_syntax`(仅 compile 分支)接入 write_file,写语法错误的 .py 要被拦。
