# 0003 — 阶段 3:语法门卫

## 学了什么

- 新增 `llm_helper.py`,目前只有 `verify_code_syntax`(compile 分支,~15 行);
  文件先叫这个名,阶段 6 的审批 LLM 也住这里
- `write_file` 加门卫 2:`.py` 后缀先过语法检查;结果 dict 增加 `verification` 字段
  (passed / failed / skipped)
- 教学注释轮换:file_tools.py 模块 docstring 从"阶段 2"更新为"两道门卫"说明

## 验证(实跑)

- 合法 `counter.py` → success + verification=passed,落盘
- `broken.py`(内容 `def broken(:`)→ success=false,"第 1 行: invalid syntax",
  确认 workspace 里无 broken.py
- 普通 `.txt` → verification=skipped,不挡路

## 卡在哪

无。

## 结论

校验层第一块闭环:compile() 本地查证,零 LLM 成本。下一阶段(4):
`execution_tools.virtual_terminal` 裸执行 + cli shell 子命令——先把"能跑命令"做出来,
阶段 5-6 再给它装闸。
