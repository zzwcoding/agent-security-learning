# 0009 — 阶段 9:覆盖审批 + file_edit

## 学了什么

- write_file 覆盖已存在文件 → 送 `request_approval("file_overwrite", ...)`,无 key fail-closed
- 新增 `edit_file`(搜索-替换,old_text 必须一字不差,替换后 .py 再过语法闸)
- 抽出 `_syntax_gate` 小助手供 write/edit 共用;cli 加 edit 子命令

## 验证(实跑五案)

- edit 精确替换成功(replacements=1)
- edit 原文对不上 → 拒("必须一字不差")
- edit 不存在的文件 → 拒
- write 覆盖(无 key)→ fail-closed 拒
- write 覆盖(有 key)→ **法官也拒**:没给旧内容/备份,只能往最坏想
  —— 顿悟 2 的实锤:审批质量取决于递的案情材料
- edit 把 counter.py 改出语法错 → 语法闸拦下("'(' was never closed")

## 卡在哪

无。

## 结论

文件侧闸完整:边界 / 语法 / 覆盖审批。下一阶段(10):cli.py demo 子命令,
把七类场景串成离线端到端演示(验收标准第 1 条)。
