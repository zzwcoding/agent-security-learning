# 0008 — 阶段 8:长输出截断+落盘

## 学了什么

- `truncate_and_persist` 纯函数:阈值 200 行/10000 字符(照抄参考项目 18-21),
  超了留头尾各 50 行 + `tempfile.mkstemp` 全文落盘,返回 (瘦身文本, 路径)
- 接入 code_interpreter / virtual_terminal 的 stdout,结果新增 `stdout_file` 字段
- stderr 暂未截断(参考项目截了,简化处理;真需要时同一函数一行接入)

## 验证(实跑)

- `print('short')` → 原样返回,stdout_file=null
- 打印 1000 行 → 结果里 100 行(头 50+尾 50)+ 省略说明带路径;
  `wc -l` 验证落盘文件完整 1000 行

## 卡在哪

无。

## 结论

输出侧闭环:AI 上下文不爆 + 全文可取。下一阶段(9):file_edit(搜索-替换编辑)
+ 覆盖已有文件送审批——审批从"执行"推广到"写"。
