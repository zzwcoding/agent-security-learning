# 0007: 阶段 6 收官对照(2026-08-29)

**学了什么**:装订对照结论(项目根《对照结论.md》);补打了一发关键实弹。

**实测证据**:
- 缺口①补打:v3 格式规范毒文档(starter-agent/workspace/meeting-notes.txt,llm-guard 块级 0.05 漏判)作为用户输入打进双 rail 壳 → self-check input **拦下**(1 次调用,5.2s,952 tokens)。证实"LLM 语义自检能看穿格式伪装"。诚实限定:实测走输入通道;真实攻击走工具返回,NeMo 不扫该通道内容,接线需自写 action。

**三缺口最终判定**(详见对照结论.md):
- ① 格式伪装注入漏判:**能补**(换 LLM 语义机制,已实测;通道接线自理)
- ② 记忆装载无校验:**不能补**(五闸全在消息链路上,记忆装载在链路外;归路线 3)
- ③ write_file 参数侧:**补一半**(tool call validation 管结构不管语义)

**卡在哪**:无。

**结论**:任务验收三条全齐——五种 rail 讲清(lessons 0002-0004)、最小实践实测表(lesson 0005)、对照结论(对照结论.md)。本任务收官,待用户"提交"后回填 issues/07 阶段 18 状态。
