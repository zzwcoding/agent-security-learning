# 0014:全流程安全总账与迁移指南(收官总结)

- **学了什么**:把 13 课收成一页总账(lessons/0014)——威胁模型(W1 直接注入 / W2 间接注入 / W3 记忆注入两轮剧本)、逐阶段安全收获、六条横切原则、迁移表(7 个场景)、诚实边界(6 条)、上线检查单(10 项)。
- **卡在哪**:没卡。写之前把 result.json、demo.py 行号、workspace 遗留物逐一对过,防止总账里出现落不到证据上的论断。
- **结论/观察**:
  - 全项目的核心论断收成两句:上下文层(D2/D3)是概率性的——记忆注入×D3 在两批之间 0%↔100% 翻转;执行层(D4"目标∈本轮用户消息")是确定性的——比对基准是攻击者唯一写不进的文本。判定铁律:看 executed 清单,不看模型嘴。
  - 新挖出一个此前没写透的边界:D4 管收获不管种植——save_memory 不在管制清单(demo.py:320 只拦 write_file/send_email)。0012 批最后一个 trial(D4×记忆注入)的遗留物是物证:memory.json 毒偏好还在、report.txt 署名"团队协作助理"(毒照样污染被授权的输出)、outbox.jsonl 不存在(外发确实被拦)。已写进总账第 6 节。
  - 诚实边界重点标注:所有数字 N=2 冒烟级;payload 全是低级马脚,没测编码/拆行/变体;D4 有语义误杀与"用户消息引用目标串"式漏放;语义闸误报率无实测数字。
- **验证**:总账引用的每个百分比与 result.json 及 0008-0012 原文核对;代码论断核对到 demo.py 当前行号(与 lesson 写作时的行号有漂移,以总账标注为准);workspace 遗留物时间戳(19:41:31/37)与 result.json(19:41:41)同批,确认是 D4×记忆注入末次 trial 的产物。
