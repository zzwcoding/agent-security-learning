# 0002:阶段 2 —— 工具调用协议

- **学了什么**:function calling 全循环(tools 规格 → msg.tool_calls → 执行 → role="tool" 回填 → 终止条件);步数上限 max_steps。
- **卡在哪**:没卡,一次跑通。
- **结论/观察**:
  - 工具结果与用户消息在上下文中结构平级——"数据/指令不分"是间接注入的结构性根源,阶段 6 攻击、阶段 10 防御都落在这条链上。
  - 模型只"请求"工具,执行权在代码——D4(阶段 11)的立足点。
- **验证**:`scripts/run.sh` 实测,输出含 `[工具调用] read_webpage(...)` + 网页总结。
