# 0011:阶段 11 —— D4 运行时校验

- **学了什么**:runtime_guard 开关;D4 政策"目标必须原样出现在本轮用户消息里";execute_tool 入口拦截;requested/executed 差集 = 运行时防御贡献。
- **卡在哪**:没卡。
- **结论/观察**:
  - 零成本直调验证:被骗场景 requested:1/executed:0(拦截),授权场景 1/1(放行)——双清单分叉实锤。
  - 完整矩阵(MiniMax-M2,N=2):记忆注入 100%→50%→0%→0%,其余攻击全 0。
  - D4 列与 D3 列同为 0:强模型上上下文层已压平,D4 没被"锻炼"到——它的价值在上下文层失效那天显现,这是兜底的意义。
  - "本轮"是政策精髓:授权随轮次过期,防止一次被骗终身借用。
- **验证**:后台 3×4×2 跑批完成 + execute_tool 直调两种场景符合预期。
