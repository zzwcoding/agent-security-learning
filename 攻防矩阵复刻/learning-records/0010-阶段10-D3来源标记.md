# 0010:阶段 10 —— D3 来源标记

- **学了什么**:source_tagging 开关;`_wrap_external(content, source, defense)`;两个挂载点(网页返回 + 系统提示词记忆段);Spotlighting 概念。
- **卡在哪**:没卡,一次跑通。
- **结论/观察**(N=2):
  - 记忆注入 D1 100% → D2 50% → D3 0%——参考项目的关键现象(记忆注入倒在 D3)复现。
  - 直接注入 D2 50% > D1 0%:纯小样本噪声,"越防越漏"不是因果。读矩阵只看方向。
  - D3 的两个挂载点对应两条不可信通道,漏一条等于没防。
- **验证**:后台跑批 3×3×2 完成,D3 列全 0%。
