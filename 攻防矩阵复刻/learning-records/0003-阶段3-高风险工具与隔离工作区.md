# 0003:阶段 3 —— 高风险工具与隔离工作区

- **学了什么**:write_file/send_email 两个高风险工具;绝对路径收敛 + 逃逸检查;outbox.jsonl 隔离发件箱;系统提示词新增规则 2(未经确认不得写入/外发)。
- **卡在哪**:没卡,一次跑通。
- **结论/观察**:
  - 副作用真实落盘但不出 `workspace/`,攻击得逞从"事故"变成"可观察事件"——这是矩阵判定器的物理基础。
  - 本阶段 demo 消息把两个高风险目标(report.txt / team@example.com)都写进了用户消息,与阶段 11 的 D4"目标须在本轮用户消息里"政策天然兼容。
- **验证**:实测跑通,`workspace/files/report.txt` 与 `workspace/outbox.jsonl` 内容均正确。
