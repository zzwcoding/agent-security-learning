# 0001:阶段 1 —— 环境与裸 Agent

- **学了什么**:OpenAI 兼容客户端接 MiniMax-M2;agent-key → 环境变量注入链;SECRET_KEY 落进系统提示词。
- **卡在哪**:没有卡点,一次跑通。
- **结论/观察**:
  - MiniMax-M2 的回复 content 带 `<think>` 思考块(判定器阶段要注意 final_text 包含思考块)。
  - 复刻版用裸 `OpenAI(api_key, base_url)` 替代了参考项目的 `agentbook.providers` 注册表,环境变量三件套(LLM_API_KEY/LLM_BASE_URL/LLM_MODEL)沿用 starter-agent 约定。
- **验证**:`scripts/run.sh` 真实跑通,模型正常自我介绍。
