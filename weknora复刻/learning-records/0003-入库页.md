# 0003 · 阶段 1 步 1.3：入库页（阶段 1 完工）

- 日期：2026-09-10
- 学了什么：Streamlit 运行模型（每次交互整脚本重跑→状态必须落库）；webui 纯表现层分工（写走管线、只读直连 store）；KB 创建走种子脚本的原因（webui 对 store 只读）。
- 卡在哪：①scripts/seed_kb.py 直接跑找不到 store 包（sys.path 引导解决）；②data/ 目录不存在导致 sqlite3 打不开库（init_db 自建父目录解决）。两处都是"测试替身照不出"的环境类坑。
- 结论：阶段 1 闭环——页面上传 txt 见 4 张卡片；Streamlit 冒烟测试绑定 spec 验收第 4 行；modules.md store 卡已回写（create_document +path）；Streamlit headless 冒烟起服正常，端口已交还用户。
- 阶段 1 收尾对账（票 0001 验收 4 条）：test_chunk_fixed_size_512 ✅ / test_reingest_idempotent_chunks ✅ / test_parse_failure_marks_document_failed ✅ / test_webui_smoke_ingest_page ✅。票 0001 待用户页面验证后关票。
- 下一步：阶段 2 票 0002 BM25（手写倒排索引，书 3.2 稀疏嵌入）。
