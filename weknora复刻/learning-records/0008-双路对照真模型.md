# 0008 · 阶段 3 步 3.3：双路对照 + 真模型首跑（阶段 3 完工）

- 日期：2026-09-10
- 学了什么：唯一出网口的第一次实战分红（embedding 余额不足 → 15 分钟换 MiniMax，只动 gateway+env）；MiniMax embo-01 实测（1536 维、vectors 键、type db/query 非对称、不要 GroupId）；duck typing 响应解析。
- 卡在哪：智谱 embedding-3 返回 429 余额不足（错误码 1113）——chat 免费档不受影响。决策：embedding 转 MiniMax（ADR 0003，PRD v1.2）。
- 实踩补丁（同日）：用户直跑 `streamlit run`（未走 keychain 脚本）→ embed key 为空 → httpx 抛 `Illegal header value b'Bearer '` 天书。修复：`_post` 缺 key 先抛人话 RuntimeError（指引用 run-with-keychain.sh），页面捕获展示——**报错信息要 actionable**；补测试 `test_empty_key_fails_with_human_message`。
- 结论：试验台双路对照上线 + 后端标注；真 API 实测 cos(kitty,cat)=0.7448 ≫ cos(kitty,stock)=0.2472；20 测试绿（+embed 配置面/MiniMax 形状解析）、四道门绿。教学落盘 `lessons/0003-3`。
- 待办：用户页面验证真模型（run-with-keychain.sh）后关票 0003。
- 下一步：阶段 4 票 0004 RRF 融合 + 教学版重排（书 3.2 混合检索三阶段）。
