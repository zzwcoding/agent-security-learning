# 0004 · 阶段 2 步 2.1：BM25 手写

- 日期：2026-09-10
- 学了什么：TF-IDF 两毛病（词频线性膨胀/长度无校正，书 3.2 原文）→ BM25 两修正（k1 饱和、b 归一化）；倒排索引=书后术语索引页；IDF 负值按下限 0 截断（书原文"实现中通常给它设一个下限"）；停用词不维护——IDF 自会收拾。
- 卡在哪：长度归一化测试的手算把"虎"的 df 数成 1（实际两张卡都含虎，df=2）——手算对照测试又一次抓到大意。
- 结论：`retrieval` 模块出生（tokenize 中文单字简化 + 真倒排索引 + bm25_search 现查现建）；store 加 `list_child_chunks`（INV-1 执行面）；4 个手算对照测试全绿（spec 第 5–8 行），总 10 测试绿、四道门绿。教学落盘 `lessons/0002-1-BM25手写.md`。
- 参数选型：k1=1.2 b=0.75（Lucene 惯例；书实验 3-5 用 k1=1.5——公式结构是重点）。
- 下一步：步 2.2 页面查询框（检索试验台，页面可观察变化）。
