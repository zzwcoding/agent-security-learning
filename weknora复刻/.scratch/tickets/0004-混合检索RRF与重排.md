# 票 0004: 混合检索 RRF + 教学版重排（学习阶段 4：并行召回 → RRF k=60 → LLM 逐一打分，页面对比视图）

Status: open
Belongs to spec: `specs/rag-core.md`
Touches modules: `retrieval` `llm_gateway` `webui`
Depends on: 0003（两路召回齐备才可融合）
Blocking: 0005（对照视图在融合面上加前缀维度）、0006（qa 经 SearchTrace 透传对比视图数据）

## 验收

- [ ] `test_rrf_fusion_rank_handcalc` —— 源自 spec 验收测试表第 13 行（两路各 top-3 名次 fixture，融合分与名次 = 手算 Σ1/(60+rank) 完全一致）
- [ ] `test_search_trace_intermediate_states` —— 源自 spec 验收测试表第 14 行（`SearchTrace` 四份中间态齐全，fused 名次 = 两路名次 RRF 结果）
- [ ] `test_llm_rerank_reorders_topn` —— 源自 spec 验收测试表第 15 行（fake 打分注入下 top-N 序翻转；打分提示词逐候选一次调用，spy 计数=N）
- [ ] `test_hybrid_search_rerank_off` —— 源自 spec 验收测试表第 16 行（`rerank=False` 结果序与 fused 序逐项一致）

## 范围

- `retrieval`：`hybrid_search(kb_id, query, top_k=10, *, rerank=True) -> SearchTrace`——两路并行召回 → RRF(k=60) 融合 → LLM 对 top-N 逐一打分的教学版重排（经 `llm_gateway.chat`，PRD §8.2 平替跨编码器）；`ScoredChunk`（chunk/score/source）
- `llm_gateway`：本票首次消费 `chat`（重排打分提示词）
- `webui`：页面对比视图——同一次查询的 bm25_hits / vector_hits / fused / reranked 四栏并排
- 书锚点：3.2 混合检索三阶段（"重排不是补救 RRF，是换更强匹配范式"）；WeKnora 锚点：`knowledgebase_search_fusion.go`（加权 RRF）

## 框架红线

- 无新点名框架（numpy/Streamlit 已由 0001/0003 认领）；重排只走 `llm_gateway.chat`，**不许**引入跨编码器模型依赖（PRD §3 阶段 4"不做"）

## 边界红线

- 允许触碰：`retrieval/`、`llm_gateway/`（仅 chat 提示词模板，不改接口）、`webui/`、`tests/`
- import 范围：`retrieval` 只许 import `store`、`llm_gateway`；不许 import `webui`
- RRF 融合必须手写（不调任何 ranking 库，边界规则第 7 条）

## 页面可观察变化

问答区出现四栏对比视图：同一查询能看到"BM25 第 1 名 ≠ 向量第 1 名 → RRF 折中 → 重排再翻序"的完整链路；勾选/取消重排开关立刻看到序差。

## 体量

RRF 约 10 行 + 重排打分循环 + 四栏 UI，一次 L2 会话内完成；真实 API 重排成本（每查询 N 次 chat）只走用户手动演示，CI 全 fake。
