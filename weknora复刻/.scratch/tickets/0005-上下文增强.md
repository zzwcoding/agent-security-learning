# 票 0005: 上下文增强（学习阶段 5：重叠+父子分块 + ContextHeader 标题面包屑前缀，有/无对照视图）

Status: open
Belongs to spec: `specs/rag-core.md`
Touches modules: `ingest` `retrieval` `webui`
Depends on: 0004（对照视图长在融合对比面上）
Blocking: 0006（qa 的父块扩展注入依赖本票的父子分块与 expand_to_parent）

## 验收

- [ ] `test_context_header_generation` —— 源自 spec 验收测试表第 17 行（fixture_headings.txt → ContextHeader = 标题+所属章节面包屑）
- [ ] `test_embedding_input_includes_header` —— 源自 spec 验收测试表第 18 行（fake 捕获 embed 输入 = ContextHeader+正文，非裸正文；书 3.5）
- [ ] `test_context_header_improves_hit` —— 源自 spec 验收测试表第 19 行（指代类查询：含前缀命中 / 裸正文落出 top-5；书 3.5 原文口径"结合 BM25 可将检索失败率降低 49%，再结合重排序器降幅达 67%"）
- [ ] `test_bm25_index_includes_header_terms` —— 源自 spec 验收测试表第 20 行（标题词正文不出现，经 bm25_search 可命中该 chunk）
- [ ] `test_overlap_and_parent_child_chunking` —— 源自 spec 验收测试表第 21 行（相邻 chunk 10% token 重叠；父子指针正确；父块不在 `list_child_chunks`，`INV-1` 入库侧）

## 范围

- `ingest`：分块升级——固定 512 token + **10% 重叠** + **父子分块**（子块进索引、父块只入库）+ ContextHeader（标题+面包屑）生成填入 `Chunk`
  - 落位说明：重叠/父子分块 PRD §3 各阶段行未显式点名，spec 将其归入本阶段（同属 chunk 上下文机制，且阶段 6 父块扩展依赖之）；`INV-1` 自此真正受力，票 0002/0003 的两条哨兵测试转正
- `retrieval`：`index_chunks` 建索引时 embedding 输入与倒排文档均为 ContextHeader+正文（对位 WeKnora `EmbeddingContent()` + `buildKnowledgeIndexContent`，`knowledge_process.go:283` 内）；`expand_to_parent(chunks)` 落地
- `webui`：有/无前缀对照视图（同一查询两套索引结果并排；重建索引切换）
- 书锚点：3.5 上下文感知检索；平替决定（标题面包屑替代 LLM 生成前缀摘要）PRD §8.2，差距进对账报告

## 框架红线

- 无新点名框架；**不做** LLM 生成前缀摘要（PRD §3 阶段 5"不做"，成本原因）

## 边界红线

- 允许触碰：`ingest/`、`retrieval/`、`webui/`、`tests/`、新 fixture（fixture_headings.txt）
- import 范围：`ingest` 只许 import `store`、`retrieval`（本票不触发 graph/wiki 分发）；`retrieval` 只许 import `store`、`llm_gateway`
- 父块不许出现在任何检索结果路径（`INV-1`，违反 = bug）

## 页面可观察变化

对照视图开关"ContextHeader 开/关"：指代类查询（"它的性能如何"）在"开"侧命中、"关"侧落空，一页看尽书 3.5 的失败率降幅叙事。

## 体量

分块改造 + 前缀拼入 + 对照视图，一次 L2 会话内完成；本幕代码量最大一票，优先保证两条手算/对照测试绿再做 UI。
