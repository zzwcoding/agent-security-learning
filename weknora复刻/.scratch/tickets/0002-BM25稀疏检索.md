# 票 0002: BM25 稀疏检索（学习阶段 2：手写倒排 + BM25，页面查询框带分排序）

Status: open
Belongs to spec: `specs/rag-core.md`
Touches modules: `retrieval` `webui`
Depends on: 0001（chunk 布景来源）
Blocking: 0004（RRF 的稀疏路输入）

## 验收

- [ ] `test_bm25_idf_formula` —— 源自 spec 验收测试表第 5 行（3 文档具名语料 fixture，IDF 分量与手算逐项相等）
- [ ] `test_bm25_tf_saturation_k1` —— 源自 spec 验收测试表第 6 行（词频 1 次 vs 5 次，k1=1.2 词频饱和手算对照，边际递减非线性）
- [ ] `test_bm25_length_norm_b` —— 源自 spec 验收测试表第 7 行（同词频、长度差 4 倍，b=0.75 长度归一化手算对照）
- [ ] `test_bm25_results_no_parent_chunks` —— 源自 spec 验收测试表第 8 行（`INV-1`；本阶段尚无父块，作为哨兵测试先行落位，票 0005 起真正受力）

## 范围

- `retrieval`：手写倒排索引（词→chunk  posting 表）+ BM25 完整公式（IDF / TF 饱和 k1=1.2 / 长度归一化 b=0.75，常数取值 PRD §8.2 书 3.2 区间）+ `bm25_search(kb_id, query, top_k=10)`
- `webui`：入库页加查询框，返回带分数的排序列表
- 书锚点：3.2 稀疏嵌入（TF-IDF→BM25，实验 3-5）；WeKnora 锚点：`internal/application/service/knowledgebase_search_fusion.go`（融合前的稀疏路）

## 框架红线

- **不许**引入 rank_bm25 / sklearn 等现成库（PRD §0.2-2 调包红线；modules.md 边界规则第 7 条）——BM25 必须手写，这是本票的学习目标本身
- 无新点名框架引入

## 边界红线

- 允许触碰：`retrieval/`、`webui/`（仅查询框 UI）、`tests/`
- import 范围：`retrieval` 只许 import `store`（读 `list_child_chunks` 建倒排）；不许 import `ingest`/`webui`/`llm_gateway`（本票不需要 embed）
- `webui` 新增 import `retrieval`（走公开接口 `bm25_search`）

## 页面可观察变化

入库页出现查询框：输入关键词 → 看到带 BM25 分数的排序结果；造一份"苹果出现 5 次"的文档能直观看到词频饱和（分数不成 5 倍）。

## 体量

倒排 + 公式 + 查询框，一次 L2 会话内完成；BM25 公式实现约 20 行。
