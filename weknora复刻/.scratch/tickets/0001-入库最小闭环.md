# 票 0001: 入库最小闭环（学习阶段 1：上传 txt → 固定分块 → SQLite → 页面列表）

Status: open
Belongs to spec: `specs/rag-core.md`
Touches modules: `ingest` `store` `webui`
Depends on: （无，本幕起点；阶段 0 CI 四道门已绿）
Blocking: 0002 0003 0004 0005 0006 0007（全部后续票的布景来源）

## 验收

- [ ] `test_chunk_fixed_size_512` —— 源自 spec 验收测试表第 1 行（fixture_2000tokens.txt → 每 chunk ≤512 token、共 4 块；书 3.2 分块为什么必须；WeKnora 锚点 `knowledge_create.go` / `knowledge_process.go:3238 ProcessDocument`）
- [ ] `test_reingest_idempotent_chunks` —— 源自 spec 验收测试表第 2 行（重入库幂等，`INV-2`）
- [ ] `test_parse_failure_marks_document_failed` —— 源自 spec 验收测试表第 3 行（fixture_corrupted.bin 置失败态不阻塞，PRD §7-⑨）
- [ ] `test_webui_smoke_ingest_page` —— 源自 spec 验收测试表第 4 行（Streamlit `main()` 可 import、入库页函数存在）

## 范围

- `store`：`init_db` + KB/文档/chunk 三表 + `create_kb`/`create_document`/`get_document`/`list_documents`/`set_document_status`/`replace_chunks`/`list_chunks`；共享 dataclass `Document`/`Chunk`（parent_id/ContextHeader 字段本票先留位，阶段 5 填值）
- `ingest`：`parse_file`（仅 txt 内置解析）+ 固定 512 token 分块（**不做**重叠/父子，PRD §3 阶段 1）+ `ingest_file`/`reingest_document`
- `webui`：Streamlit 入口 + 入库页雏形（上传 → 文档列表 → chunk 列表）
- KB 创建走种子脚本调 `store.create_kb`（spec 依赖与风险 ③，webui 对 store 只读不写）

## 框架红线

- **Streamlit 必须真引入**：进 requirements.txt 且入库页真调用（本票认领 PRD §0.2-2 点名框架之一）
- pytest 已在阶段 0 CI（不重复认领，本票验收全部落 pytest）
- pymupdf 本票**不许**引入（票 0007 的事）

## 边界红线

- 允许触碰：`ingest/`、`store/`、`webui/`、`tests/`、`scripts/`（KB 种子脚本）、requirements.txt
- import 范围：`ingest` 只许 import `store`；`webui` 只许 import `ingest`、`store`（store 仅只读查询）；跨模块一律走包根 `__init__.py` 公开面（modules.md 边界规则第 4 条）
- `store` 不许 import 任何业务模块（叶子模块，边界规则第 5 条）

## 页面可观察变化

入库页首次出现：上传一份 txt → 文档列表多一行 → 点开看到 chunk 列表（每块 ≤512 token，共 N 块）。

## 体量

新增代码 ≤30 行/文件级小步（learn-by-rebuild）；预计 store 建表 + 分块函数 + 一个 Streamlit 页，一次 L2 会话内完成。
