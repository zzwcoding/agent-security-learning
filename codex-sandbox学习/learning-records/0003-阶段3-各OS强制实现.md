# 0003 — 阶段 3：各 OS 强制实现

- **日期**：2026-08-28
- **学了什么**：`get_platform_sandbox()` 三平台分发。macOS=Seatbelt `(deny default)` 白名单策略（借鉴 Chromium，网络是独立叠加文件）；Linux=**bubblewrap 为默认**（Landlock 已降 opt-in），`--ro-bind / /` + 可写根盖回 + `.git` 再盖只读 + `--unshare-net` 物理断网，WSL2 行 WSL1 不行；Windows=RestrictedToken 默认关，`workspace-write` 静默降级 `read-only`（fail-closed）。
- **卡在哪**：无。
- **结论**：落盘 `lessons/0003-各OS强制实现.md`。下一步阶段 4（approval policy 组合矩阵）。
- **信源落点**：`sandboxing/src/manager.rs`、`seatbelt_base_policy.sbpl`、`linux-sandbox/README.md`、`config/src/config_toml.rs`、`core/src/windows_sandbox.rs`。
